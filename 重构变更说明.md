# 分支同步变更说明（2026-09-23）

**分支**：`agent/主线可用版`
**操作**：用本地「行业研究智能体-全链路系统」全量覆盖该分支
**提交**：单次根提交（orphan 风格），**不保留、不合并原分支历史**

---

## 一、先看结论

| 项目 | 数值 |
|---|---|
| 覆盖前该分支文件数 | 1831 |
| 覆盖后该分支文件数 | 395 |
| 新增文件 | 111 |
| 内容有改动（同名文件） | 66 |
| 内容完全未变 | 218 |
| **删除文件** | **1547** |

覆盖后该分支的文件树 = 本地开发目录，**逐文件一致**（`git diff` 为空）。

⚠️ **这是一次全量替换，不是增量合并。** 原分支上 1547 个文件被移除，其中大部分是「同花顺问财 SkillHub」的评测与技能资产，详见第四节。

---

## 二、覆盖后这个分支是什么

覆盖后的分支承载的是**独立的五智能体全链路行业研报生成系统**：

```
backend/      FastAPI 服务 + 状态机引擎（7 个 REST 端点 + 健康检查）
agents_core/  五智能体算法核心
              ├── data-fetcher/    阶段1 数据获取（问财 SkillHub 调度 + 覆盖度审计）
              ├── data-analysis/   阶段2 数据解读（确定性量化引擎 + 方法论技能）
              ├── chart-generator/ 阶段3 图表生成（6 种出版级 ECharts 图表）
              ├── chapter-writer/  阶段4 章节撰写（7 章 21 节全并发）
              └── report-fusion/   阶段5 研报融合（双模板 HTML/PDF/Markdown）
frontend/     Vue 3 + Element Plus + Vite 协同工作台
scripts/      跑批与运维脚本
data/runs/    运行时快照（state.json + artifacts/）
output/       交付物与设计方案
```

启动方式：

```bash
pip install -r requirements.txt
./start_system.sh          # 后端 :8000 + 前端 :5173
```

无头全自动跑批：

```bash
IMAGE_API_KEY="" PYTHONPATH="$PWD" python scripts/run_pipeline_auto.py \
    --topic 低空经济 --depth standard          # 新建全自动跑，无人工审核门

IMAGE_API_KEY="" PYTHONPATH="$PWD" python scripts/run_pipeline_auto.py \
    --resume-run <run_id> --from-stage chapter_write   # 断点续跑，复用已有阶段产物
```

> `IMAGE_API_KEY=""` 是**故意的**：产业链 AI 生图超时上限 600 秒且成功率低，置空可直接跳过，单次跑批省最多 10 分钟。

---

## 三、新增了什么（111 个）

| 目录 | 数量 | 主要内容 |
|---|---|---|
| `agents_core/` | 38 | 5 套写作/解读 Skill（`geopolitical-risk-writing`、`tech-fundamentals-writing`、`thesis-tracking-writing`、`sensitivity-stress-analysis`、`earnings-revision-analysis` 等）、`chart_generator/image_gen.py`、`industry_chain.py`、`ths_schema.py`、**双报告模板 `templates/{auto,rich}/`** |
| `output/` | 23 | 6 图 SVG 样例、双模板（auto/rich）成品报告、以及 2 份设计方案文档 |
| `frontend/` | 20 | `AgentLiveCockpit.vue`、`AgentLiveTrace.vue`、`NewRunModal.vue`、`RevisionDiffViewer.vue`、`SettingsModal.vue`、`SkillHubDrawer.vue`、`useAgentTrace.ts` 等 |
| `backend/` | 14 | `app/engine/state_machine.py`、`app/agents/adapters.py`、`app/core/{event_hub,setup_env,storage}.py`、`run_server.py`、5 个测试 |
| `scripts/` | 9 | `run_pipeline_auto.py`（自制跑批入口）、`run_solid_state_full.py`、`run_unitree_full_pipeline.py`、`verify_pipeline_health.py` 等 |
| 其他 | 7 | `start_system.sh`、`requirements.txt`、`.env.example`、`.workbuddy/memory/*`、`data/runs/.gitkeep` |

---

## 四、删除了什么（1547 个）

| 目录 | 数量 | 说明 |
|---|---|---|
| `eval/` | **1010** | 评测体系与用例集 |
| `backend/` | **308** | 原 SkillHub 后端（本次只回填 14 个文件，其余移除） |
| `skills/` | 97 | SkillHub 技能包 |
| `docs/` | 65 | 历史方案文档 |
| `frontend/` | 19 | 原前端组件 |
| `contracts/` | 10 | 接口契约 |
| `scripts/` | 8 | 原脚本 |
| `.workbuddy/`、`.workbuddy-ai/` | 11 | 旧记忆文件 |
| `.github/`、`.githooks/` | 6 | CI 与 git hooks |
| 根目录 | 6 | `AGENTS.md`、`TEAM_CONVENTION.md`、`THIRD_PARTY_NOTICES.md`、`commitlint.config.js`、`design-qa.md`、评审器方案 md |
| 其他 | 7 | `config/`、`.trae/`、`.nvmrc`、`.python-version` 等 |

### 这些文件去哪了？能不能找回？

- **1496 个**在 `main` / `agent/chart-mvp-sync` 上**同样存在** → 直接从 `main` 找回。
- **51 个是 `agent/主线可用版` 独有**（`main` 上也没有），已全部由本次覆盖前的备份分支保住。

---

## 五、备份分支（重要）

覆盖前已创建备份分支，指向覆盖前的 `agent/主线可用版`：

```
backup/mainline-20260923-preoverwrite   →   6b03126
```

**回滚方式**（把分支恢复成覆盖前的样子）：

```bash
git fetch origin
git push --force origin backup/mainline-20260923-preoverwrite:agent/主线可用版
```

**只捞回某个被删文件**：

```bash
git checkout backup/mainline-20260923-preoverwrite -- <文件路径>
```

### 那 51 个独有文件里有什么值得关注

- `backend/app/agents/real_core/{adapter,bootstrap,stages,artifacts}.py` + 对应测试
  （`tests/agents/real_core/*`、`tests/integration/test_real_agents_bridge.py`、`tests/workflow/test_real_agent_registry.py`）
- `backend/scripts/run_real_agents_smoke.py`
- `docs/superpowers/{plans,specs}/2026-09-22-real-five-agent-integration*.md`
  —— **真实的五智能体接入方案与设计稿（9/22）**，这套东西没有进本地目录
- `skills/{产业链解读, 竞争格局分析}/`（SkillHub 侧技能包）
- `agents_core/data-fetcher/skills/{产业链解读, 竞争格局分析}/`（agent 侧同名技能包）
- 15 份 `docs/` 方案文档（Agent3 图表、Agent5 报告融合、评审器红蓝对抗、前端人机审核等）

> 如果本地项目后续要用到「真实五智能体接入」，建议从备份分支把这批文件捞回来再合并。

---

## 六、改动了什么（66 个同名文件）

| 目录 | 数量 | 重点 |
|---|---|---|
| `frontend/` | 34 | 组件与视图更新 |
| `agents_core/` | 24 | 五个 agent 的 `agent.py` / `llm.py` / `linter.py` / `models.py` / `render.py`、`report-fusion` 模板与样式 |
| `backend/` | 5 | `api/routes.py`、`core/config.py`、`main.py`、`schemas/workflow.py` |
| 根目录 | 3 | `README.md`、`.gitignore`、`.workbuddy/MEMORY.md` |

其中值得单独点名的修复：

- `agents_core/report-fusion/report_fusion/templates/auto/report.html`
  修掉 `body::before` 抢占 CSS Grid 第一格导致**整页版式错位**的问题（左目录栏与正文宽度互换、封面标题竖排），修法见文件内注释。
- `agents_core/chart-generator/chart_generator/*` 六图选型与自愈修正逻辑。
- `.gitignore` 补齐 `.env`、`node_modules`、运行产物、日志等忽略规则（**请勿删除**，它挡住了真实密钥入库）。

---

## 七、接手注意事项

1. **`.env` 没有进仓**（被 `.gitignore` 挡住），拉下来后需要自己 `cp .env.example .env` 并填 `LLM_API_KEY` / `IWENCAI_API_KEY`。
2. **前端依赖要重装**：`cd frontend && npm install`（`node_modules/` 被忽略）。
3. **运行产物不进仓**：`data/runs/*`、`output/runs/*` 被忽略，只保留 `.gitkeep` 占位。
4. 有一处**仓库卫生问题**待清理：`backend/app/agents/adapters.py.bak-20260923`、`adapters.py.bak-p0` 两个备份文件被误提交进来了，确认无用后可删。
5. 本文档（`重构变更说明.md`）是为说明本次覆盖而新增的，不影响源码。

---

## 八、操作留痕

```bash
# 覆盖前备份
git push origin 6b03126:refs/heads/backup/mainline-20260923-preoverwrite

# 全量覆盖（单次根提交，强制推送）
git push --force origin HEAD:refs/heads/agent/主线可用版
```

覆盖后校验：`git ls-files | wc -l` → **395**，且与本地目录逐文件一致。
