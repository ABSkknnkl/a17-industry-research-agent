# 2026-09-02 工作日志

## 评审器正向语料注入 + 红蓝对抗落地（产出最终方案）

### 任务
用户要把桌面 `未命名文件夹/` 里 7 个金融研报 PDF 的「优秀正向例句」注入 Agent4 评审器，并给出最终方案（含红蓝对抗训练所需正负例）。

### 关键结论（已核实代码）
- 评审器 = `ReadabilityLinter`（确定性 5 规则 R1~R5，`app/agents/chapter_writer/readability_linter.py`）+ `OpenAICompatibleReadabilityModel`（LLM 软分，`app/integrations/llm/openai_compatible.py:835`）。
- LLM 评审器 system prompt（openai_compatible.py:875-882）**极简、无 few-shot、无评分锚** —— 正例注入第一落点。
- 红队样本 `redteam_readability_samples.py` 仅 5 负 + 3 正（全手写）；`eval/readability_calibration.py` **缺失**（运营方案阶段二未做）。
- 评审器默认关闭 `READABILITY_REVIEW_ENABLED=False`，全部线下调优。

### PDF 语料（7 个 PDF 全文字版，165 页 / 17 万字符）
- 抽取 401 条「专业金融句」候选。
- **头号发现**：用项目自己的 Linter 跑 401 条，**147 条(36.7%)被 R2_SENTENCE_TOO_LONG 误杀**——真实研报大量「专业但通顺」句子超 45 字。R1/R3/R4/R5 在真实语料几乎零命中，说明其余四条对齐良好，唯独 R2 阈值需重标（45→55~60）。

### 交付
- 新文档 `docs/评审器正向语料注入与红蓝对抗落地方案.md`：
  - 16 条 PDF 正例（few-shot 8 + 术语已解释 3 + 长句边界 3 + 跨市场 2）
  - 负例按运营方案 §2 五类补齐清单
  - 四落点注入方案（扩 POSITIVE_SAMPLES / few-shot+评分锚 / 补校准脚本 / R2 阈值标定）
  - 红蓝对抗 R0→R3 四轮 + 量化门禁

### 注意
- PDF 文本抽取到 `/tmp/pdf_extract/*.txt`（临时，未入库）。

## 落点 1 + 落点 4 落地（用户说"继续"，实施零 LLM 成本部分）

改动了 2 个文件：
1. `app/agents/chapter_writer/readability_linter.py`：`MAX_SENTENCE_CHARS` 45→60（R2 阈值标定）。
   数据：652 条真实研报句，45 字误杀率 61%，60 字降到 38.5%（剩余命中多为 71+ 字 PDF 粘连/流水账，生产 LLM 输出标点规范不适用）；80 字病句 45~70 字任意阈值均不漏杀。
2. `tests/agents/chapter_writer/redteam_readability_samples.py`：POSITIVE_SAMPLES 3→19 条（3 手写 + 16 条 PDF 真实研报正例，带 `source` 字段，覆盖四语体/四维度/长句边界/跨市场）。

验证：`tests/agents/chapter_writer/` 全套 71 passed, 1 skipped；black + flake8 全净；负样本 RT-01~05 抓取 100%、19 条正例 0 误报。

**沙箱坑**：pytest 在 Agent 模式下报 `PermissionError: EEXIST mkdir pytest-of-unknown`（沙箱 shim 拦截 tmp 目录 mkdir）。解法：`--basetemp=$(mktemp -d /tmp/pt_XXXX)`，每次用全新目录即可。这个要记住，后续跑 pytest 都加 `--basetemp`。

### 剩余（未做，待用户发话）
- 落点 3：补 `eval/readability_calibration.py` + `calibration_samples/`
- 落点 2：few-shot + 评分锚注入 `openai_compatible.py:875` system prompt（与校准集隔离，防数据泄漏）
- R0→R3 红蓝循环（真实 LLM，烧钱，等用户发起）
- prompt.md 规则 16「单句不超过45字」与 Linter 新口径（按标点切分、30 字）语义需再对齐确认。

## R2 句长口径修正（用户纠正：一个标点=一个句子）

用户指出句长判断应看标点，而不是按"句号到句号"数整句字数。修正：

- `readability_linter.py` 拆成两个切分正则：`_SENTENCE_SPLIT`（句末标点，R4 术语堆砌用，口径不变）+ 新增 `_CLAUSE_SPLIT`（全部标点 `。！？；，、：…,.!?;:`，R2 句长用）。
- `MAX_SENTENCE_CHARS` 30（数据：791 条真实研报句 → 4624 小句，87% ≤30 字，30 字是"一口气读完"拐点）。
- 效果验证：带逗号的 56 字长句（最长小句 19 字）→ 放行；无标点 35 字/80 字片段 → 仍命中 R2；RT-03 术语堆砌（R4）→ 口径不变仍命中。
- 全套 `tests/agents/chapter_writer` 71 passed, 1 skipped；black/flake8 全净。
- 方案文档 `docs/评审器正向语料注入与红蓝对抗落地方案.md` 已同步更新（R2 规则表/头号发现/C组长句边界/落点4/实施记录/结论）。

**要点**：之前"45→60"的提法作废，正确结论是"改判断单位（按标点切分），阈值 30 字"，而不是单纯调大字数。

## prompt.md 写作规则同步对齐（用户确认）

- prompt.md 规则 15「单句不超过45字」→「两个标点之间的片段不超过30字；超过时在合适位置补标点拆开（独立意思用句号，同一句内并列用逗号）」。
- prompt 版本 1.2.0 → **1.3.0**，SHA256 → `344c4165...`（prompt_loader.py）。
- test_prompt_asset.py 版本断言 1.2.0 → 1.3.0。
- 全套 chapter_writer 71 passed, 1 skipped；black/flake8 全净。生成端(prompt)与评审端(Linter)句长口径现已一致。

## 真实坏报告负向用例 → Linter 新规则 R6/R7（用户："只要 A"）

用户给了一份真实生成的坏报告（"动力电池行业研究报告"，主题错乱+数据字段泄漏+空壳章节），标注为负向用例。分析后确认当前 Linter 对 6 类坏模式只抓到 1 类（R5），漏抓 5 类。

落地 A（只做确定性规则，用户明确"只要 A"）：
1. `readability_linter.py` 新增两条规则：
   - **R6_FIELD_LEAK**（连贯性/must_fix）：字段名泄漏 `@值`/`@id`、空值占位符 `未提供`、模板占位符 `证据编号相关证据`、结构化数组裸贴 `['…']`（正则 `\['[^\]]*\]`）。
   - **R7_QA_LEAK**（客观性/must_fix）：内部质检日志 `shift异常检测`/`超出突变阈值`/`基线区间`/`CRITICAL`。
   - docstring 规则映射更新为 v1.3.0，注明 R6/R7 是"数据链路泄漏检查，无 prompt 规则对应"。
2. `redteam_readability_samples.py` NEGATIVE_SAMPLES 增 RT-06~RT-11 六条（真实坏报告例句），linter_expected 分别 R6/R7。
3. 验证：坏报告 4 类原始坏句全部命中 R6/R7；chapter_writer 全套 77 passed, 1 skipped；black/flake8 全净。

**注意**：`docs/评审器正向语料注入与红蓝对抗落地方案.md` 被用户覆盖成了那份坏报告内容（untracked，50856 字节）。我未擅自恢复，等用户确认意图（是存负向用例，还是要恢复方案文档）。

未做（用户只要 A）：
- B：⑥⑦ 软判负例（整段数据罗列/空壳章节）进 few-shot。
- C：上游 bug 根治（A1 routing 抓错数据 → 主题错乱；A2 字段清洗 → `@值`/`未提供`/`证据编号相关证据` 泄漏）。这两类是"字段泄漏"的真正根因，评审器只能拦不能修。

## 方案文档恢复 + 坏报告另存（用户选"2"）

- 坏报告已另存到 `docs/负向用例_动力电池坏报告_数据链路泄漏.md`（50954 字节）。
- `docs/评审器正向语料注入与红蓝对抗落地方案.md` 已重建为完整最终方案（整合 R2 口径修正 + 16 正例 + prompt v1.3.0 + R6/R7 + 6 坏报告负样本 + 落地状态表 + 上游根因）。
- 注：该方案文档后续又被用户覆盖成坏报告内容（用户可能持续用它存坏报告）。不再往里写方案内容。

## R8/R9/R10 研报版 AI 味规则（用户：直接做成 R8/R9/R10）

用户给了两个「去AI味」技能（humanizer / ai-flavor-remover），问适不适合 A4/评审器。判断：**核心思想（注入灵魂/口语化/文学笔法）不适合投研客观文体，但特征清单可提炼成确定性 Linter 规则**。用户选定落地。

`readability_linter.py` 新增 3 条确定性规则（零 LLM 成本）：
- **R8_FALSE_TARGET**（客观性/must_fix）：假靶子/空泛强调句式，正则匹配 `值得强调的是`/`值得注意的是`/`毋庸置疑`/`显而易见`/`真正的(重点|关键|本质)在于`。
- **R9_EMPTY_CONCLUSION**（客观性/suggest）：空泛结论词表 `未来可期`/`前景广阔`/`意义重大`/`影响深远`/`发展迅猛`/`势头强劲`/`值得期待`/`持续向好`。
- **R10_AI_RHETORIC**（客观性/连贯性/suggest）：自问自答老师腔 `这说明…？`/`这意味着…？` + 空泛连接词堆叠（`此外`/`综上所述`/`总的来说`/`总而言之`/`由此可见` ≥2）。

**关键教训（误伤）**：「大幅增长」「显著提升」这类词**带量化数据时是有效表达**（如"同比大幅增长12.6%"），不能进确定性词表——它们属于"带数据有效/无数据空泛"，应留给 LLM 软判。R9 词表只保留纯空话词。

负样本 `redteam_readability_samples.py` 增 RT-12~16（5 条研报版 AI 味坏句）。全套 chapter_writer **83 passed, 1 skipped**；black/flake8 全净；19 条正例 0 误报。

## 启动前后端（真实模式）

用户要求"启动前后端"。过程与要点：
- `./scripts/dev_up.sh up` 能通过 fail-closed 校验并打印"启动完成"，但 **Agent 模式下 `nohup ... &` 的子进程随脚本退出被回收**，服务实际没起来（端口无监听）。
- 改用**后台任务**（run_in_background）方式分别启动 uvicorn(8000) + vite(5173) 才真正持久存活。
- **关键坑**：后台任务直接跑 uvicorn 会因 shell 导出的非法 JSON 环境变量 `CORS_ORIGINS` / `API_BEARER_TOKENS` 崩（pydantic-settings 解析失败）。脚本是通过 `unset` 临时忽略，手动启动需 `env -u CORS_ORIGINS -u API_BEARER_TOKENS .venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`。
- 本地 curl 探活需加 `--noproxy '*'`，否则被系统代理拦返回 502（同代打测试 httpx 502 同源）。
- 当前服务：8000 后端（python3.1 pid 35348）+ 5173 前端（node pid 35315）均在监听。真实模式，创建研报消耗 DeepSeek/问财配额。

## Bug 定位：A2 revise/regenerate 必死循环（analysis_input_invalid）

用户报告（run 9cf944cd，已取消）：智能体2 修改条件重跑后报 `analysis_input_invalid`，反复重试后按钮全失效。用 live API 取证（GET /runs/{id}/revisions/{n}），根因链：

1. **主 Bug（后端）** `app/agents/data_interpreter/service.py:269-279`：A2 重跑时若 `context.revision > fetch_result.revision`，会把 `context.input_data` 中的 6 个字段覆盖到 A1 结果上，含 `evidence_items`。而 `ResearchInput.evidence_items` 有 `default_factory=list`（schemas/run.py:27），`model_dump` 后**恒存在且为 []**——A1 实际采到 168 条证据被空列表清空 → `AnalysisRequest.evidence_items min_length=1` 校验失败（pydantic too_short）。revise 和 regenerate 都会 bump revision（graph.py:536）→ **A2 任何重跑 100% 复现**，死循环。
2. **锁死**：每次重试消耗 `max_stage_attempts=3`（runtime/models.py:53），第 4 次 `stage_attempt_limit_exceeded`（recoverable=false）；此后 revise/regenerate 被 graph.py:463 拒绝"runtime budget exhausted; cancel is required"→ 用户只能取消。
3. **次 Bug（前端）** `ReviewActions.vue` regenerate/cancelRun：`await ElMessageBox.confirm()` 用户点取消时 reject('cancel') 未捕获 → Vue warn "Unhandled error..." + console "Uncaught (in promise) cancel"。
4. **关联**：`workflow_deadline_exceeded`（run dd72291e）：`deadline_at` 仅创建时设一次（models.py:105），审核等待计入墙钟——隔夜 run 恢复即死；前端指引"等待审核不计入"文案与实现不符。

修复方向（用户只要方案不动手）：A) service.py 覆盖循环对 evidence_items 加非空判断，或改为只覆盖本次 edited_data 显式提交的键；B) analysis_input_invalid 透出具体字段错误、识别"系统侧清空"不可用户修复；C) 前端 catch confirm 的 cancel、recoverable=false 时禁用重跑按钮并引导取消。

## LLM 切换：DeepSeek 直连 → 火山方舟 ark-code-latest（已完成配置）

用户给火山方舟 key + ark-code-latest + 两个 Base URL（Anthropic 协议 /api/plan、OpenAI 协议 /api/plan/v3）。判断与落地：

1. **选 OpenAI 协议**：项目所有 LLM 装配点（llm/factory.py 分析/章节/评审器、data_fetcher/factory.py 语义路由/意图分解、feedback_interpreter、visuals/factory.py 图表说明）统一走 langchain `ChatOpenAI`（OpenAI 兼容 /chat/completions），无 Anthropic 客户端 → Base URL 用 `https://ark.cn-beijing.volces.com/api/plan/v3`（不带 /v1，langchain 自己拼 /chat/completions）。
2. **改 backend/.env 三行**：`LLM_API_KEY=ark-2d38...`、`LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/plan/v3`、`LLM_MODEL=ark-code-latest`。评审器无 LLM_JUDGE_* 自动回落主配置，无需另改。
3. **实测验证**（curl 直打网关）：chat/completions 200；tools/function calling 200（finish_reason=tool_calls）；response_format json_object 200；`thinking:{"type":"disabled"}` 200 且 reasoning_tokens=0。
4. **关键隐患（未处理，待用户拍板）**：ark-code-latest 经方舟网关实际路由到 **deepseek-v4-flash-ga-260731（推理模型）**，网关**默认开思考模式**（probe 里 40 completion tokens 中 37 个是 reasoning）。项目代码 `_is_deepseek()`（openai_compatible.py:232 + semantic_router/feedback_interpreter 内联判断）只在模型名以 `deepseek-` 开头时才发 `extra_body thinking:disabled` + 走 json_mode；切到 ark-code-latest 后：① 不再关思考 → 每次调用烧 reasoning tokens，8192 max_tokens 被吃大半，结构化输出有截断风险；② 结构化路径从 json_mode 变默认 function_calling（已验证 ark 支持 tools，可行）。**修复选项**：把 thinking disabled 触发条件从 deepseek- 前缀泛化为也含 ark-code-*（三处 extra_body + _is_deepseek，改动极小），或接受思考开销直接试跑。
5. **启动坑**：shell 环境变量残留旧 `LLM_BASE_URL/LLM_MODEL/LLM_API_KEY`（deepseek 值），pydantic 优先级 env > .env 会覆盖新配置 → 启动必须 `env -u LLM_BASE_URL -u LLM_MODEL -u LLM_API_KEY -u CORS_ORIGINS -u API_BEARER_TOKENS`（dev_up.sh 只 unset CORS/API，不会清 LLM_*，直接 ./scripts/dev_up.sh up 会仍走 deepseek）。
6. readiness fail-closed 校验（unset 后）：NONE - pass。改 .env 不重启不生效；服务当前已停止。

## 旧 DeepSeek 清理 + deepseek 特判泛化到 ark-code-*（已落地，选项 1）

用户指令「把旧 DeepSeek 密钥删除只用新的，选项 1：顺手直接改掉」执行完毕：

1. **旧 env 变量无磁盘源头**：grep 全部标准 profile（zshenv/zprofile/zshrc/profile/bash_profile/bashrc）+ ~/.envrc + ~/.config/direnv + ~/Library/LaunchAgents + launchctl getenv 全为空 → 旧 `LLM_BASE_URL=api.deepseek.com` / `LLM_API_KEY=sk-e606...` / `LLM_MODEL=deepseek-v4-flash` 是 **WorkBuddy 宿主进程会话级注入**（佐证：`CODEBUDDY_CURRENT_MODEL_ID=deepseek-v4-flash`，IDE 本身就跑在 deepseek 上），删不掉、只影响本 Agent 派生 shell。旧 key 在磁盘零残留（全仓 grep 仅 2 个测试文件引用 api.deepseek.com 作 mock）。
2. **dev_up.sh 补洞**：原脚本只 unset CORS/API_BEARER，现在 LLM_* 三件套也**无条件 unset**（.env 为 LLM 配置唯一事实来源，注释已写清）；汇总文案 DeepSeek → 火山方舟 ark-code-latest。这是「只用新的」在脚本启动路径的闭环。
3. **代码泛化（6 处判断点，AST + bash -n 全过）**：
   - `llm/openai_compatible.py`：`_is_deepseek` 改名 `_is_deepseek_style`，判断改 `startswith(("deepseek-","ark-code-"))`，8 个调用点（json_mode 决策 1 + _requires_json_instruction 3 + thinking disabled extra_body 3 + def）全随迁，docstring 说明 ark-code-latest 经方舟路由到 deepseek-v4-flash-ga-260731 需同等处理。
   - `data_fetcher/semantic_router.py` 3 处、`agents/common/feedback_interpreter.py` 1 处、`integrations/visuals/openai_compatible.py` 1 处（**此第 6 处此前方案未计入**）→ 内联判断全改 tuple。
   - 未动：`config.py:39` 默认值 deepseek-v4-flash（纯兜底，.env 恒覆盖）；`visuals/mock.py` "mock-deepseek-prompt-compiler" 字面量（test_agent.py:72 断言依赖）；测试文件 deepseek mock 字符串（deepseek-* 前缀仍匹配，测试不受影响）。
4. 服务已停，**重启后生效**；启动仍用 `env -u LLM_BASE_URL -u LLM_MODEL -u LLM_API_KEY`（或直接 ./scripts/dev_up.sh up——脚本现在会自清）。

## 方舟端到端探测（用户要求"测试一次模型是否能被调用正确输出"）

用 backend/.venv + /tmp/ark_probe.py（PYTHONPATH=backend，env -u 旧变量）四步探测，结论：

1. **通过**：Settings 读到方舟三行配置（key 掩码 ark-2d38...2c97）；`_is_deepseek_style` 泛化命中（ark-code-latest=True，gpt-4o=False）。
2. **通过**：模型本体调用正常。裸 ChatOpenAI + `with_structured_output(method="json_mode", include_raw=True)` + `extra_body thinking:disabled` → 200，中文输出贴题，`parsed=ProbeAnswer(answer=..., confidence=0.85)` 全链路含 schema 校验成功，finish_reason=stop。
3. **通过（关键省钱验证）**：`token_usage.completion_tokens_details.reasoning_tokens=0`，completion_tokens=38~48 全为实际输出 → **thinking disabled 生效**（对比改前默认开思考时 40 tokens 里 37 是 reasoning）。
4. **新 Bug（仅一处）**：`OpenAICompatibleSemanticRouter.route()`（semantic_router.py:86 json_mode）实调 400：`at least one message must contain the word 'json' with json_object or json_schema`。方舟网关强制 json_object 模式下 prompt 必须含 "json" 字样，而语义路由 system prompt（L99-105"你是金融数据查询路由器…"）无 json 字样 → **deepseek 直连不炸（deepseek json mode 不强制），方舟必 400**。`.env` 里 `AGENT1_SEMANTIC_ROUTER_ENABLED=true`，问冷门指标时 A1 会踩中。影响面已收敛：llm/openai_compatible.py 三条 json_mode 主路径（analysis L665-670 / chapter L781-783 / readability L910-912）在 deepseek/ark 系会注入含 "JSON" 的契约文本 → 不受影响；IntentDecomposer 是裸 ainvoke+自带 JSON 修复不走 response_format → 不受影响。**修复 1 行**：语义路由 system prompt 末尾加「只输出一个 JSON 对象」（semantic_router.py:103 后），用户已确认，AST 通过；复测 `route()` 成功——光伏行业问题正确归类 `hithink_industry_query` confidence=0.95，reason 合理。探测脚本 /tmp/ark_probe.py 可复用（PYTHONPATH=backend + env -u 旧变量 + .venv/bin/python）。

## 评估：火山 Harness 专业数据集要不要给模型开启（纯咨询，未落地）

官方计费（volcengine.com/docs/82379/2516283）：金融/工商/风险/汽车配置/汽车销量/兜底搜索均 **12 AFP/次**，科研学术 **24 AFP/次**；套餐 Small ¥40=20,000 AFP/月（≈1666 次金融查询）、Medium ¥200=100,000。模型 token 与 Harness **共用同一 AFP 池**。

对比 A1 data_fetcher 现有 15 个 SkillName（全走问财 hithink_* + 自建，schemas/acquisition.py:15）后结论：
- **建议开**：企业风险库 + 企业工商库——投研尽调/排雷（司法诉讼/行政处罚/股权知产）是现有 skill 清单唯一明显缺口，12AFP/次成本可忽略。
- **低频按需**：科研学术（24AFP 贵，仅深度技术趋势报告用）；汽车配置/销量（仅汽车行业报告）。
- **不建议开**：金融数据库/宏观库——与 hithink_finance/quote/index/futures/macro 高度重叠，双源对账 + semantic_router 叠加反而增歧义；兜底搜索官方自己都建议优先其他。
- 接入形态：MCP（Base URL+ark key，复用 Agent Plan），但用户 Agent 是 langchain 自有体系非 MCP 生态 → 若接应拆成独立 tool（勿用其统一 Query 路由）+ 扩 SkillName + normalizer 归一 + telemetry 审计，工程量不小；WorkBuddy 侧替代可对比同花顺 kuaicha-search 连接器（工商/风险类，厂商一致）。

## BUG-5 修复验证（只读回归，未改生成代码）— PASS

用户用另一个模型完成了 BUG-5 两处修复（LangGraph 快照白名单 + max_tokens 弃用），只差回归，我做了只读验证：
- 改动核对：`app/infrastructure/checkpoint/sqlite.py` from_conn_string→自持 aiosqlite 连接 + `JsonPlusSerializer(allowed_msgpack_modules=[StageName,StageStatus])`；`integrations/llm/openai_compatible.py` analysis 与 `visuals/openai_compatible.py` 两处 `model_kwargs={"max_tokens":...}`→显式 `max_tokens=`。（llm 文件的 58 行 diff 混有 `_is_deepseek→_is_deepseek_style` 加 `ark-code-` 前缀的无关改动，系另一任务，未评估。）
- 回归结果：`tests/workflow/test_sqlite_checkpoint.py` + `tests/infrastructure/test_bug5_checkpoint_serde.py` = 7 passed；`tests/workflow/` + `tests/integrations/llm/` + `tests/integrations/visuals/` = 58 passed。
- 全库验证 `backend/scripts/verify_checkpoint_recovery.py`（保留的可复用工具，非临时探针）：231 snapshots / 1740 blobs PASS，白名单外类型恰好只有 writes 里的 StageName(65) + StageStatus(91)，全部可恢复。
- 无残留 `from_conn_string` / `model_kwargs+max_tokens` 调用点。
- 环境坑更新：pytest 沙箱用 `--basetemp=$(mktemp -d /tmp/pt_XXXX)` 最省事（比 TMPDIR 法干净）；config 需 `CORS_ORIGINS='[]' API_BEARER_TOKENS='{}'`。

## Agent 5 报告视觉升级方案（交付前端）— 只读调研产出

文档：`docs/2026-09-02-agent5-报告视觉升级方案.md`（新建）。用户诉求：PDF 太丑要美化、评估"要不要让大模型写 HTML"、前 4 个 Agent 数据用大模型捏造做隔离测试、方案要给前端且留开放问题。

### 链路事实（已核实代码）
- `ReportViewModel`(app/schemas/report.py) → `render_markdown()` / `render_html()` / `render_pdf()` 三条产物同源。
- PDF = Playwright Chromium，A4，`prefer_css_page_size=True`，页边距 14/12/16mm（app/reporting/pdf.py）。
- **前端 `ReportReader.vue` 读的是 markdown（marked 渲染），不是 HTML** ⇒ 浏览器视觉与 PDF 是两套，前端改 CSS 影响不到 PDF。
- `render_html(report: ReportViewModel) -> str` 是**纯函数**（不碰 DB/LLM/网络）⇒ 有 JSON 夹具即可完全跳过 Agent 1-4 做隔离视觉测试。
- **现状零覆盖**：`backend/tests/reporting/` 只有 `test_svg.py`，HTML/PDF 渲染无任何测试、无夹具。

### 头号缺陷（硬事实）
- Chromium 打印**不支持** CSS Paged Media：`@page @bottom-center`、`string-set`、`target-counter`、`position:fixed` 多页重复。
  ⇒ 模板 report.html.j2:99 的页码代码是**死代码（PDF 里根本没页码）**；:45 的 draft 水印用 fixed ⇒ **只在封面出现**。
- 其余：长表/图表跨页切断、中文 `text-align:justify` 字距拉伸、7 列来源表 A4 溢出、封面 245mm 临界溢出、缺 `print-color-adjust:exact`。
- 图表配色硬编码 `app/reporting/svg.py:18 DEFAULT_COLORS`（960×480 固定画布），前端改不动。

### 关键判断
- 美化基线不用从零设计：`docs/agent5-layout-generation-spec.html`（525 行）已是高质量样板（brand token/封面渐变/.stat-grid/.matrix 组件），**只是没移植到 report.html.j2**。
- 三条路线：A 纯 CSS（无页码）/ **B Paged.js（推荐，补 Chromium 缺的页码页眉目录页码脚注）** / C 服务端分页。
- **反对让大模型写整份 HTML**（分页不可控致命、破坏产物 sha256 可复现、绕过 autoescape+StrictUndefined 安全边界、校验成本≈写模板）。推荐 LLM 只做 ① design token 生成 ② 版式编排 plan（组件+尺寸枚举）③ 图表皮肤参数；④ 一次性主题 CSS 属实验项须过沙箱白名单。
- 夹具矩阵 5 套（3 golden 行业 + stress-extreme + degraded-dirty），**必须有脏数据夹具**——只测理想数据会做出生产上崩掉的版式。

## 追加：Agent5 后端问题清单与用户可选项（只读复核，未改生产代码）
交付 `docs/2026-09-02-agent5-后端问题清单与用户可选项.md`。用户要求：把方案里"非美观类"的后端 bug/问题单独列出；并明确"浏览器最终产物应由用户从三种里选（或全要）"。

**新挖出的确定 bug（产出错误内容，非美观）**
- B-1 **模板硬编码 `A17`**：`report.html.j2:99` 页脚 + `:126` 封面 eyebrow。全仓仅模板一处，`backend/artifacts/**/report.html` 11 份已生成报告全带。修法：`ReportViewModel.industry_topic`（schemas/report.py:130，必填）已存在但模板没用。
- B-2 **brief 模式丢 100% 图表**：`markdown.py:87-89` + 模板 `:177` 在 `report_depth=='brief'` 时 `continue` 跳过 section 循环；图表按 section_id 挂载（html.py:63），`unplaced` 只收 `placement_section_id is None`（html.py:64-66），而 `assembler.py:136` 的 placement **与 report_depth 无关** ⇒ 图既不进正文也不进附录。
- B-3 目录 `<ol class="toc"><li>` 无 `<a href>`，章节 `<section class="chapter">` 无 id ⇒ 浏览器/PDF 目录都不可跳转。
- B-4 **print 样式被主题类覆盖**：`@media print{main{max-width:none;padding:0}}` 权重 (0,0,1) < `.visual-deep-research main{max-width:920px;padding:0 76px}` (0,1,1) ⇒ deep_research 打印时版心被吃掉 40mm，`.density-*` 的 padding 同理未清零。
- B-5 `.draft-watermark{position:fixed}` 浏览器里永久悬浮遮挡（pointer-events:none 只防点击）；B-6 `.meta-grid` 3 列却渲染 5 个 meta。

**安全/健壮性**
- S-1 **报告 HTML 在 API 同源域 inline 渲染**：`routes.py:206-252` FileResponse(media_type=text/html, filename=) ⇒ `Content-Disposition: inline`；叠加 `html.py:56 Markup(chart.svg)` 绕过 autoescape。当前 svg.py 自生成且有 escape() 故风险可控，但边界需加固（attachment 或 CSP sandbox + nosniff）。
- S-2 `pdf.py` **无 timeout**（set_content wait_until="load" 可能永不触发 ⇒ 最后阶段挂死）、无并发上限、每次冷启 Chromium。
- S-3 `html.py` 每次调用新建 Jinja Environment（无缓存）；S-4 `html.py:58 split("-")[1]` 无防御，异常被 service.py:272 吞成"HTML导出失败"；S-5 `prefer_css_page_size=True` 与 `page.pdf(margin=)` 双重定义边距，前者会静默忽略后者。

**测试缺口**
- T-1 `backend/tests/reporting/` 只有 `test_svg.py`，render_html/markdown/pdf 零测试零夹具。
- T-2 三源（md/html/pdf）一致性无测试 —— B-2 只有做三源对比才暴露。
- T-3 **产物 sha256 真实运行下不可复现**：`assembler.py:158 generated_at=datetime.now(UTC)` 渲染进封面 ⇒ 每次字节不同。方案 §2.3"重复渲染二进制稳定"只对冻结夹具成立，不能当成真实运行目标。
- T-4 PDF 中文字体全依赖系统字体栈（PingFang/雅黑/Noto CJK），无校验 ⇒ 换 Linux 容器必翻车而测试全绿。
- T-5 生产不落盘 ReportViewModel（白名单仅 report.md/html/pdf/manifest.json）⇒ 夹具只能 LLM 捏造；落盘 `report_view.json` 可零成本拿到**真实夹具**。

**"用户选三种"结论**
- 三种 = 产物格式 markdown/html/pdf。后端 **已支持**：`ReportFusionOptions.output_formats`（schemas/workflow.py:187-190），`service.py:141-145` 空数组默认全给。**缺的只是前端入口**：前端 grep `output_formats`/`visual_style`/`visual_density` **零命中**；根因是 `ReviewActions.vue:194-217 submitRevise()` 只给 data_fetch/data_interpret 构造 edited_data，report_fusion 阶段 edited 恒空。
- 建议三层：① 产物格式 checkbox（用户必选，后端零改动）② 视觉风格/密度/深度 radio（后端零改动）③ `pdf_renderer: native|pagedjs` 作为**降级档不做用户宣传**（Paged.js 翻车时运维一键降级）。
- 后端待办 3 件：① `ReportFusionOptions` 加 pdf_renderer ② **落盘 report_view.json + `POST /runs/{id}/export` 事后重导出**（一次解决 T-5 与"换格式会产生新 revision 致旧链接失效"）③ `pdf.py` 支持注入 Paged.js + wait_for_function。
- 20 个开放问题归类：后端必须参与 **7 个**（Q1/Q2/Q3/Q8/Q12/Q18/Q19），部分参与 2 个（Q9/Q14），纯前端/纯审美 11 个。
- 待用户拍板：① 产物格式默认策略（现在"不选=三种全给"）② report_view.json 落不落盘 ③ Q2 前端读 HTML 还是 markdown（不定则 Q3 与 pdf.py 改动无法收口）。

## 追加：Agent5 报告美化 S-2/方案A/B/S 系列修复的验证 + 补测试（2026-09-02 深夜）
另一模型已按方案实施全部改动，我负责**核对落地 + 补齐缺失的测试**。核对结论：所有改动均已正确落地，无需返工。

**改动核对（全绿，无遗漏）**
- `pdf.py` 重写：常驻浏览器复用 + `asyncio.Semaphore(2)` + 180s 三层超时 + `_close_page` 5s 兜底 + `shutdown_pdf_renderer` + `_renderer` 按"当前事件循环"归属（换循环重建）。
- `main.py` lifespan 收尾接入 `shutdown_pdf_renderer()`。
- `service.py` 方案 A：markdown 恒渲染（前端预览源）；`output_formats` 语义改为"交付格式"，缺省 → HTML+PDF；`formats` 报交付格式，唯一交付格式全失败时回退报实际落盘格式（防契约 min_length=1）。`report_view.json` 落盘（内部夹具源，不进 manifest/artifacts）。落盘顺序在 `advisory_issues.extend(export_issues)` 之前，保证 view-model 落盘失败能进 unresolved_risks。
- `assembler.py` B-2：`effective_depth` 计算提前，brief 时 `_placement_for` 恒返回 None ⇒ 全部图表进附录（三渲染器现有未挂载路径接住，单点修复）。
- `html.py` S-3 模块级 `_ENVIRONMENT`（autoescape=True + StrictUndefined + cache_size=50）；S-4 `_PLACEMENT_PATTERN = ^SEC-(\d{2})-\d{2}$` 正则安全解析，非法回退附录。
- 模板 B-1/B-3/B-4/B-5/B-6/S-5 六处一次改完：eyebrow `{{ industry_topic }} · 行业研究系统`、页脚 `removesuffix('行业')`、目录 `<a href="#chapter-N">` + 章节 `id="chapter-N"`（序号，不泄露 CH-xx）、print 块同权重后置覆盖 visual-*/density-* 版心、水印 absolute 移入封面、meta-grid 5 列、`@page` 删 margin 只留 Python 侧。
- `local.py` 白名单加 `report_view.json`。
- `test_agent.py` 两处 formats 断言已同步：`:113` → `["html","pdf"]`，`:465` → `["html"]`。

**我做的两件事**
1. **修 `test_render_contract.py`（另一模型写的 bug）**：原用 `monkeypatch.setattr("app.core.config.settings", "ARTIFACT_ROOT", tmp_path)` 字符串形式——但 `settings` 是 `app.core.config` 模块里的**对象**（`settings = get_settings()`），不是模块路径，字符串形式 getattr 失败报 `AttributeError`。改为对象形式 `monkeypatch.setattr(settings, ...)`（与 test_agent.py/conftest.py 一致），文件头补 `from app.core.config import settings`。
2. **新建 `tests/reporting/test_pdf_renderer.py`**（6 用例，假 Playwright 单例）：懒启动复用只 start/launch 一次、并发峰值恰为 2（Semaphore(2)）、0.1s 超时硬返回且 page 仍被 close、断线 is_connected 重启、shutdown 关浏览器+幂等、跨事件循环重建。

**回归结果**：`tests/reporting/` + `tests/agents/report_fusion/` + `tests/workflow/` + `tests/test_contracts.py` = **62 passed**（basetemp 用项目内 `.pytest_tmp` 绕沙箱 tmp 限制，用完删）。
**eval 确认**：`eval/scorers/stages.py:62` A5 检查读 `fusion["artifacts"]` 的 `kind` 字段（aliases 映射 markdown/html/pdf/manifest），与 `formats` 字段无关 ⇒ markdown 恒落盘后四种 kind 齐全，默认交付改 HTML+PDF 不影响 eval。
**坑记录**：pytest 沙箱挡 `/var/folders/.../pytest-of-unknown` mkdir（EEXIST）⇒ 必须 `--basetemp=.pytest_tmp` 或 `TMPDIR=` 指向项目内可写目录。
