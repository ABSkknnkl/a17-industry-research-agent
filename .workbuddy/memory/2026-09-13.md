# 2026-09-13

## 4 个外部专家的评估 + 写入桌面方案第十三节

用户要求把历史对话中评估过的专家可借鉴点，写进桌面的方案文件
`/Users/Zhuanz1/Desktop/图表体系精简与复合化方案-20260912.md`（已从 476 行扩到 590 行，新增第十三节共 7 个小节）。

### 四个专家的评估结论（重要，避免重复评估）

| 专家 | 定位 | 判定 | 可借鉴 |
|---|---|:---:|---|
| `mermaid-diagram-expert`（Mia） | Mermaid 图表渲染 | ❌ 不适用 | 仅 ELK.js 布局（评估后**也不建议采纳**：节点少手工坐标够用 + ECharts 自带 force + 新增依赖与项目"零重依赖"原则冲突） |
| `text-to-diagram`（ID `ex_XbLxfuPCLNfR`，鹏城信息AI专家） | 文本→Mermaid/Excalidraw/Canvas | ❌ 不适用 | 0 点（手绘风削弱研报可信度） |
| `lynxce-dataviz-advisor`（灵策，ID `ex_SM88Um6w65B1`） | 选型与看板设计顾问 | ✅ 有用 | **18 条规范**（纯规则，可直接转质检判据） |
| `chart-builder-expert`（图官，ID `ex_HyKc6e22Rsq3`） | 数据图表设计与生成 | ✅✅ **最对口** | 全流程 + 3 条独有增量 |

**核心分野**：前两个是「**文字驱动**」绘图工具，与本项目「**数据驱动**」图表链路不兼容；
灵策给规则不给执行；图官既给规则也给执行，是四个里唯一直接对口 Agent 3 职责的。

**图官 3 条独有增量**：① 质量门三问（图能 5 秒看懂吗？轴有没有骗人？重点有没有被埋没？）
② 换视角复检（本项目 G1–G8 是固定规则，缺自检环节）③ 数据准备独立成环节。

**图官两个使用边界（必须加约束）**：
- 「无素材输入必须直接交付」与项目 **P1/P2/G5（数据不足阻断、不伪造、无数据不绘图）冲突**
  → 本项目**不适用**"最小可用输出"规则
- 「地理→地图」能力 → 默认关闭（中国地图有合规要求）

### 灵策审计的关键数据（写入方案 13.3）

- ✅ 完全满足 4 条 / ⚠️ 部分 7 条 / ❌ 缺失 5 条
- **两条 100% 命中项**：
  1. `line_axis_not_annotated`：**37/37** line 图用 `scale:true` 但 0/37 有起点标注
  2. `color_only_encoding`：**0/87** 有 label/symbol，全部仅靠颜色区分（色盲不友好）
- **三条新风险**：面积表达一维差异、挑选有利区间（cherry-picking）、
  **时间轴 category 轴压缩缺失区间**（xAxis 全部为 category，缺失月份被跳过 → 制造"连续"假象，**定为硬规则**）
- 精妙对照：项目"缺失数据标未提供"**做法对、位置错**（标进了 `yAxis.name` 而非图注）

### 合并后的 9 条质检规则（方案 13.6）

硬规则 2 条：`unit_missing_or_placeholder`（26/87 命中）、`category_axis_gap`
软规则 7 条：`line_axis_not_annotated`、`color_only_encoding`、`title_not_conclusive`、
`axis_not_annotated`、`pie_similar_share`、`area_encodes_one_dim`、`suspicious_range_selection`

> **重叠处理**：灵策与图官规则高度重叠，**取一次即可**（以 13.6 合并表为准），不要双份灌进代码。

### 分工建议（方案 13.7）
- 质检规则来源 → 灵策
- 图表生成与排版执行者 → 图官
- 工程文档配图（架构图/流程图）→ mermaid-diagram-expert
- 产品演示/内部讨论 → text-to-diagram

## 整合现有技能方案 → 桌面方案第十四节（476 → 744 行）

**技能来源**：随 `chart-builder-expert` 专家装的 3 个 skill
（`~/.workbuddy/plugins/cache/experts/chart-builder-expert/1.0.2/skills/`）：
`dataviz`（可视化设计指南）/ `gchart`（结构化图表生成）/ `canvasxpress`（图表+审计追踪），
各约 410-420 行，**共用一套高度规范的文档架构**（7 章：能力边界速查卡 → 场景映射 → 执行流程 →
置信度门控 → 错误码体系 → 反模式对照 → 渐进式披露）。

**最值得借鉴的**：这套**文档架构本身**可作为项目图表文档与质检体系的组织模板。

**12 条可借鉴点分四类**：
- A 补强质检：`[需核实:xxx]` 占位符规范 / 门控触发条件表 / 错误码配"修正步骤" /
  数据体检阈值 / **对比度 ≥4.5:1 (WCAG AA)** / 输出前反模式自查清单
- B 审计与容错：**操作审计日志 audit_log** / **错误报告+断点续跑** / 运行前自检 selftest
- C 文档表达：能力边界表（能做/不能做）/ 渐进式披露路径
- D 旁证：**dataviz 选型表把雷达图标注"慎用"**（第四次独立印证 radar 降级）；
  dataviz 反模式表说"颜色限制 5 种以内"（比灵策的 ≤6 更严）

**冲突检查结果：真实冲突 0 条，需显式标注边界 3 条**：
1. dataviz 的"缺失 ≤20%"是**整表数据质量**判断，项目的 `QuarantinedRecord` 是**单行可信度**判断
   —— 层级不同，不得互相替换
2. **断点续跑 vs 三层级联降级**：降级链是运行中换源（同一轮内），续跑是运行后补跑（跨轮）
   —— 时间维度不同，不得重复建设
3. **颜色上限**：灵策 ≤6 种 vs dataviz ≤5 种 → 取严的 **5 种**（项目现状 87/87 已是 5 色）

**实际执行的 5 处修订**（非仅列待办）：
① 新增 5.5「能力边界表」② 13.6 补规则 10/11 + 输出前自查清单 ③ 13.6 加颜色上限取舍备注
④ 第 8 节追加"出图前自检""失败项续跑"两步骤 ⑤ 14.5「图表生成操作审计」7 字段建议

**跨文件待办 1 条**：《图表专业度对比分析》§7 第 1 条（单位兜底）的修法要细化为
统一占位符 `[需核实:货币单位]`，且只进图注不进轴名。

## 同类技能第二轮检索 + 方案第十五节（744 → 848 行）

**检索方式**：SkillHub 6 组关键词 → 下载 5 个候选到 `/tmp/thx_skills/` **只读分析**
（未安装到技能目录、未执行任何脚本）。find-skills 流程：Step 2 搜索 → Step 5 查本地市场（空的）→ 只读分析。

**5 个候选**：`data-visualization-2`(DL 10784 ★6, 最有料) / `scientific-visualization`(2352 ★2, 777行) /
`chart-designer`(698) / `visualization`(1193, ⚠️含 security/auth.js + cloud/aws_lambda.js，仅领域参考) /
`data-storytelling`(110)

**P0 级增量（两条）**：
1. **标注技术 5 种**（来源 data-visualization-2）：Call-out / **Reference line（目标基准线）** /
   **Shaded region（标记时间段）** / Arrow+text / Before-after line
   ← 本项目 **0/87 有标注**，这是 `color_only_encoding` 规则的**实现手段**
2. **禁用清单（Never Use）**（来源 data-visualization-2）：选型表除"推荐"外标注**禁用图表**
   （如"比类别→条形，永不用折线"）← 本方案只有保留清单，缺禁用清单

**其他高价值**：
- **Okabe-Ito 色盲友好色板**（scientific-visualization，8 色科学标准，优先于 data-visualization-2 的 cb_safe）
- **灰度测试**（黑白打印可读性，本项目无此检查）
- **"Highlight one thing"**（灰掉其余、只给关注点上色）
- **"单指标不画图，用 KPI 卡"**（无时间维度的单点值；有时间维度的仍用折线）
- **多面板合成图**（粗体 A/B/C 标注，项目无此能力）
- **选型决策树**（chart-designer，可对照 router.py 检查覆盖度）

**冲突处理 4 条**（全部已给方式）：
1. **"双 Y 轴 → 拆成两张图"** vs 项目保留 combo → **有条件保留**：默认拆图，显式声明才组合
2. "单指标不画图" vs 项目单指标折线 → 不冲突，**按有无时间维度区分场景**
3. 两套色盲色板 → **优先 Okabe-Ito**
4. 颜色上限 5–7 vs 第14节结论 5 → **维持取 5**，不放宽

**忽略项**：Dark Mode 配方、KPI 卡 HTML 实现、期刊尺寸规范、matplotlib/seaborn 代码、
Chart.js 配置、data-storytelling 商业案例、`visualization` 全部代码（含未验证的鉴权/云端代码）

## 图表落地规格文档扩范围（187 → 425 行）
文件：`docs/plans/2026-09-12-chart-dual-panel-and-broker-style.md`
（标题改为「图表能力落地规格：视觉增强 + 规则修正 + 表达补齐」）

### 代码缺口核实结果（全部确认，含行号）
| 缺口 | 位置 | 实测 |
|---|---|---|
| combo 量纲硬限制 | `router.py:207` `len(units) != 2` | ✅ 确认 |
| 单位占位符泄漏 | `datasets.py:189` `unit=dataset.unit or "未提供"` → `builders.py::_axis_name` 拼接 → 轴名「CNY 未提供」 | ✅ **完整链路确认**，26/87 命中 |
| 去重只告警不抑制 | `service.py:784-786` `if is_duplicate: pass` | ✅ 确认（注释写「不再因预算/重复/章节密度而 continue」） |
| 阈值不一致 | 见下 | ✅ 确认，**且发现重复定义** |

### 数量阈值实测（5 个数字 + 1 处重复定义）
- `chart_generator/service.py:84` `RECOMMENDED_CHARTS_PER_REPORT = (5, 8)`
- `chart_generator/planner.py:18` `RECOMMENDED_CHARTS = (5, 8)` ← **与上一处重复定义**
- `report_fusion/quality.py:97` 硬编码「推荐上限 8 张」
- `report_fusion/visual.py:108` `<=4 low / <=10 medium / >10 high`
- `chart_generator/service.py:91` `HARD_LIMIT_MAX_CANDIDATES = 30`、`HARD_LIMIT_CHARTS_PER_CHAPTER = 10`

### 本轮关键判断（写入文档）
1. **7.3 的根因分析**：抑制被关闭**很可能是被迫的** —— 因为 `build_dedupe_key` 含 LLM 自由文本
   `insight_goal`，措辞一变即误判为不重复。**必须两步一起做**：先把 goal 改归一化语义槽位，
   再恢复抑制。只做一步都会出错（顺序不可颠倒）
2. **8.1 标题结论化归属定案**：**A2 生成 + A3 校验**。理由：A2 有 LLM（能生成结论），
   A3 是确定性层（能做规则校验）。把生成放 A3 无法凭空产生结论；把校验放 A2 则 LLM 自评不可靠
3. **7.4 修正方向**：阈值收敛到 `chart_generator/constants.py` 单一来源，删 planner.py 重复定义；
   以 `(5, 8)` 为基准，`visual.py` 密度档对齐（≤4 low / 5-8 medium / 9-10 high- / >10 high）
4. **方案文档口径冲突**：精简方案写「10–15 张」vs 代码 `(5,8)` → **以 `(5,8)` 为准**

### 文档新增章节
七、规则修正（7.1 combo 量纲 / 7.2 单位泄漏 / 7.3 去重 / 7.4 阈值）
八、表达层能力补齐 7 项（标题结论化 / 编号+目录 / 标签开关 / 截断轴提示 / 语义配色 /
   combo 单轴 / series_meta 放开 2→4）
九、统一实现清单与分期（**P0 规则修正 / P1 表达补齐 / P2 视觉增强**）+ 依赖关系
十、验收总表（14 项）十一、不在范围内 十二、修订记录

**依赖链**：7.3 去重键 → 8.2 编号（需先不重复）→ 8.1 标题（需先不重复）

**本轮仍未改任何生产代码**（纯设计文档）。

## 参照图核对 + 文档修订（425 → 450 行）

用户提供两张参照图问覆盖情况，特别是第二张（券商研报 WTI/CCPI 图3/图4）应为 P0。

### 关键发现：方案的现状描述有误
**图表编号（读者侧编号）其实已经实现！** 位置：`backend/app/reporting/html.py:76-83`
```python
chart_numbers[chapter_number] = chart_numbers.get(chapter_number, 0) + 1
item["display_number"] = f"图{chapter_number}-{chart_numbers[chapter_number]}"   # 如「图3-1」
# 未归属图：item["display_number"] = f"附图-{len(unplaced)+1}"
```
- 模板 `report.html.j2:187/196` 已在渲染 `chart.display_number`
- 原方案 8.2 写「`ChartSpec` 增 `display_number` 字段」→ **错误，已修正**
- 实际缺的是：**「图表目录」章节**（模板只有「附录·图表」）+ **全文连续编号模式**（现为章序制 `图3-1`）

### 另一发现：数据来源标注形态不对
模板已有「数据来源：」位，但渲染的是 **引用编号**（`[1][2]` 跳转来源表，学术风格），
**不是研报式具名来源**。券商研报是「资料来源：Wind，国信证券经济研究所整理」。
→ 新增 8.8「图表资料来源具名标注」（**P0**），来源名从 `evidence_catalog` 的 publishers/source_name 推导。

### 两张参照图的核对结论（写入 8.9 节）
| 特征 | 图A（4 线双图） | 图B（券商研报） | 覆盖 |
|---|:---:|:---:|---|
| 双图并排 | ✅ | ✅ | 第 2.3 节 dual_panel（P2） |
| 图表编号 | ❌ | ✅ 图3/图4 | **已实现**，仅缺目录（P0） |
| **资料来源** | ❌ | ✅ Wind… | 8.8（**P0 新增**） |
| 无网格+细线 | ❌ 有网格/圆点/粗线 | ✅ | broker_thin（P2） |
| 标题含单位 | ❌ | ✅ | 8.1（P1） |
| 图例右上+线型示意 | ❌ | ✅ | 未覆盖 → 并入 8.8 |

**结论**：图 A = dual_panel 布局 + 通用商业风格（无需改造）；
图 B = 券商标准形态，其「图表目录 + 资料来源」**从 P1/无 提至 P0**（属"能否交付"要件）。

### 文档修订（`docs/plans/2026-09-12-chart-dual-panel-and-broker-style.md`）
- 8.2 现状修正 + 拆为 P0（目录）/ P1（编号格式）
- 新增 8.8 资料来源标注（P0）
- 新增 8.9 参照图形态核对
- 9.1 分期表 P0 更名为「**规则修正 + 交付要件**」
- 9.2 改动清单 #14/#15/#15b 更新
- 第 10 节验收表新增 2 项 P0

## 图表能力最终落地方案（新增桌面文档）

产出：`/Users/Zhuanz1/Desktop/图表能力最终落地方案-20260913.md`

### 为什么需要这份
此前积累 4 份文档（诊断 391 行 / 借鉴 848 行 / 规格 450 行 / 样板 HTML），
但**技能与专家的借鉴点大多停在分析层**（只在"借鉴分析"章节，未进落地清单）。
本文档做收敛：**唯一执行入口**，把三路输入合并为可照做的待办清单。

### 盘点结果：之前借鉴但未落地的项（本次全部纳入）
来自技能（dataviz/gchart/canvasxpress/data-visualization-2/scientific-visualization/chart-designer）：
- **标注技术 3 种**（reference line / shaded region / call-out）← 对应 0/87 无标注
- **对比度 ≥4.5:1**（WCAG AA）
- **输出前自查清单**（5 问）
- **Okabe-Ito 色盲色板**、**"Highlight one thing" 高亮法**、**灰度测试**
- **操作审计日志**、**错误报告+断点续跑**、**运行前自检**
- **数据体检阈值**、**禁用清单（Never Use）**、**能力边界表**、**选型决策树对照**

来自专家：
- 图官 **质量门三问**（5 秒看懂？轴骗人？重点被埋没？）

### 最终方案结构
- 〇 与既有文档关系（说明收敛逻辑，避免多份文档打架）
- 一 现状与目标（87 实例关键数字）
- 二 **待落地项总清单 28 项**（P0×6 / P1×8 / P2×7 / P3×7），**每项标注来源**（诊断/规格/技能/专家/参照图）
- 三 分期与依赖（含 3 条关键约束：P0-3 两步不可颠倒、P0-2↔P2-1 耦合、P2-4 依赖 P0-1）+ M1–M4 里程碑
- 四 P0 逐项落地规格（含代码片段）
- 五 验收总表（18 项判据）
- 六 明确不做（7 项，防范围蔓延）
- 七 来源索引

### P0 六项（交付要件 + 规则修正）
P0-1 combo 量纲分支 / P0-2 单位占位符不进轴名 / P0-3 去重键归一化+恢复抑制 /
P0-4 数量阈值单一来源 / P0-5 图表目录 / P0-6 资料来源具名标注

**依赖链**：P0-3 →（不重复）→ P0-5 目录 → P1-2 编号 → P1-1 标题
