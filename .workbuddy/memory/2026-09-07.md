# 2026-09-07 工作日志

## L3 联网数据计算通道 + freshness 动态化方案（待评审）

产出：`docs/plans/2026-09-07-web-official-figure-and-dynamic-freshness.md`（纯方案文档，未改任何生产代码）。

**核实出的关键事实（带行号，方案 §1）**：
- 现行「阶段一」`AGENT2_WEB_NUMERIC_ENABLED` 通道**计算意图 0% 达成**：`_numeric_evidence`（calculations.py:57-67）虽放行，但 formal 期间指标（:90）与 CR 集中度（:701）双道 `period_end is not None` 护栏把 web 证据（normalizer.py:745 period_end=None）全挡住。
- 副作用全发生：`fusion.py:91-98` 图表分组无 tier 过滤 → 同指标多条 web 冲突值生成同标签多柱破图；LLM claims 可引用冲突数字。
- `_extract_web_numeric`（normalizer.py:664）=「全文首个数字+单位」，注释自承「不做语义择优」，单位表含裸 亿/万/%/bp。
- freshness 现状：client.py:203 恒 `oneYear`、count=10；`_clean_hits` 本就不按日期过滤；`fallback_query_for`（executor.py:104）丢用户时间词。

**方案核心决策**：
- 废弃旧开关，新建 `official_figure` 通道：EvidenceItem 加 `source_tier`+`official_figure` 两个可选字段（evidence_tier 恒 web_unverified 不上调）。
- 双闸门：功能总闸 `AGENT1_WEB_OFFICIAL_FIGURE_ENABLED`（默认 false）× 任务级证据清单授权（决策包 `WEB-OFFICIAL-NUMERIC`，与 risk_snapshot SHA 绑定，修订重跑不继承）。
- 语义绑定抽取替换首个数字：锚点子句 ±40 字符窗口 + as-of 期间必填 + 单位归一（外币无汇率拒绝）+ T0 原文页 URL 模式；多候选冲突保留不自动选值。
- 三道隔离：计算双池（official 只补位不混算，CalculatedMetric 带 inputs_provenance）；图表 official 单独系列；claims 强制〔网·官方〕+ audit 校验。
- 来源分级 T0/T1/T2/T3（子域黑名单优先），T0/T1 黄条 ADVISORY、T2 红门 ACKNOWLEDGEMENT_REQUIRED（现状语义）、混合红主导。需补域名：zqrb.cn/financialnews.com.cn/jjckb.cn/cls.cn。
- freshness：query 单通道 + 默认 noLimit（准确性由 tier 架构+人工复核守，不靠 freshness），C-14 翻转，fallback_query_for 拼时间词，文档注记防回改。
- 分期：一期不碰红线（破图修复+旧抽取下线+分级+freshness）；二期动红线 2 需用户签字。待拍板 4 件事列在方案 §8。

## 本地服务启动（演示用）

- 后端 `cd backend && source .venv/bin/activate && uvicorn app.main:app --reload --port 8000`；前端 `cd frontend && npm run dev`（vite 默认 5173，`/api` 已配代理转发 8000，见 vite.config.ts:24）。
- 启动前先 `lsof -iTCP:8000/5173` 查占用——本机常有常驻实例，重复启动会撞端口（vite 会自动退到 5174，后端直接 Address already in use 失败）。
- **验收口径**：`curl localhost:8000/health/ready` 须 `ready:true` 且 `mock_components` 为空才可真实联调；本次实测 environment=production、llm_model=ark-code-latest、skillhub=iwencai、pdf_renderer=playwright_chromium_configured、issues 空。
- 代理连通性用 `curl localhost:5173/api/v1/ping`（该接口与 /health 为公开接口，无需 Bearer）。
- ⚠️ 演示注意：后端若配 `API_BEARER_TOKENS`，前端尚未接入 Token 输入（setup.md:77），页面调受保护接口会 401。

## 对外介绍文档（给老师/答辩用）

产出 `docs/PROJECT_INTRO.md`。**用户明确要求：给老师看的版本不讲技术细节**（不要行号/函数名/配置参数/SHA-256/Pydantic/测试条数堆砌），只讲「五个智能体如何搭建、各自负责什么」。

已确立的叙事框架（后续同类文档沿用）：
- 核心类比：不是"万能实习生"，而是**五人投研团队**——①调研员 ②分析师 ③制图员 ④撰稿人 ⑤主编，每道工序后一道人工签字。
- 每智能体统一用四段式：拿到什么 / 负责什么 / 交出什么 / 硬规矩（不许做什么）。
- 技术亮点只讲三条人话：①每个数字有身份证 ②能算的交给代码（AI 算术一律作废）③缺口坦白、冲突保留。
- 设计哲学一句话：**宁可少给，绝不给错**。

### 录制回放（PROJECT_INTRO 第八节）可复用素材
- 已固化快照实测：`eval/cache/live_content_addressed/` 下 skillhub 725 份 + llm 177 份 = **902 份**（websearch 目录为空，因 L3 真实链路未触发，与 2026-09-06 评测报告一致）。
- 机制：内容寻址（请求内容 SHA-256 作键，`transport.py:60 compute_live_match_key`），改参数自动失效重录；strict miss=fail 未命中抛 `SnapshotMiss` 绝不静默走网络；`StopController` 额度熔断抛 `EvaluationStop`。
- 给外行讲的三个卖点：省费用（902 次调用变 0）、省时间（模型单次实测 170–260s，177 次≈8–13 小时）、免分发密钥（安全红线）。
- 附带价值：回放时可注入 429/超时/截断/字段丢弃/乱序等真实难复现的故障场景。

## A17 赛题原文已确认（README 的引用即本题）

赛题：**【A17】基于同花顺问财 SkillHub 的行业研究报告智能生成与人机协同优化系统设计【同花顺】**（2026 移动应用创新赛/浙江省赛企业命题，题目类别=智能体）。
项目里没有赛题原文文件，只有 README 引用；原文由用户对话提供（2026-09-07）。

赛题六类技术指标 = 多技能协同取数(≥6类技能/组合融合) + 数据解读(关键指标/趋势/异常/交叉验证/结构化解读) + 图表自动选型(line/bar/pie/radar/产业链) + 7章21节分章生成 + 融合一致性(术语/口径/引用统一+摘要结论) + 人机协同(每阶段审核/确认修正补充/反馈优化)。用户期望另含 md/html/pdf 多格式输出。

**对齐核查要点**：真实报告 HTML 含「执行摘要 + 七章(21节) + 数据质量附录 + 来源索引」，agent3 五类图齐、ReviewView/RunsView/HomeView 三视图已实现。2026-09-06 真实评测范围是 A1–A4 **不含 A5 融合**，A5 全真实链路需补端到端确认（重要待办）。

## Skill「真数据缺口」登记源（用户区分真缺口 vs 已修 bug 时用）

- **`backend/config/research_boundary_terms.yaml`** = 真缺口显式登记处（61 条压测确认无数据源，命中永不硬路由→披露/澄清）：
  ① 公司级无源：产销率、自给率、良率、外采比例、单瓦成本、单GWh成本、进口依赖度
  ② 行业口径无源（公司口径有但行业口径 Skill 不提供）：行业出口占比/出口占比、行业库存周转水平（2.11，公司级 inventory_days 有 `requires_company_entity`）
  ③ 语义边界（派生判定，非缺口）：爬坡周期/爬坡进度
- **`metric_registry.py` `penetration_rate` = unsupported=True**（渗透率，数据源未验证，注释"待 pywencai 反向探测"，缺口待确认）。
- **结构性技能缺失（E2E/真实评测实证，非词表类）**：资金流向（E-49 无专项技能）、板块级聚合 PE/PB（A14-01，概念标签选股跨行业异质）。
- 公司级库存/开工已有别名：inventory_days 别名含「库存水平/去库/累库」（metric_aliases.yaml）、capacity_utilization 别名含「产线跑满程度/生产负荷」——所以「公司」口径这些词可路由，「行业/泛称实体」才成缺口。
- cases_v7.json 的 17 条 group=intercept 是「拦截类」（注入/编造/非法），非数据缺口，勿混入。
