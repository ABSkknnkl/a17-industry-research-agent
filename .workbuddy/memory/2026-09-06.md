1	# 2026-09-06 工作日志
2	
3	## 三层级联降级最终落地方案（已定稿 + 测试骨架）
4	
5	**产出**：
6	- `docs/plans/2026-09-06-agent1-cascade-fallback-FINAL.md`（最终落地版，整合 doc-channel-fallback + 联网选型 + 设计稿 + 博查实测）
7	- `backend/tests/agents/data_fetcher/test_cascade_fallback.py`（S2 测试骨架，红着提交）
8	
9	**9 个关键决策（D1-D9，方案 §0）**：
10	- D1: L2 = 2a 结构化替代优先 + 2b 文档通道兜底（从 SKILL_CAPABILITIES 的 entity/metric 交集推导，非拍脑袋）
11	- D2: L2 命中覆盖率封顶 partial，跨口径强制 caliber 标注（防 P0-6 静默降级重演）
12	- D3: L3 定稿**默认博查**（实测合格，Tavily 仅海外主题，本版不实现留配置位）
13	- D4: 联网证据恒 web_unverified + qualitative_only；前端弹窗 + 决策包 WEB-SOURCE-UNVERIFIED 留痕（弹窗可关、留痕不可抵赖）
14	- D5: `acquisition_level`(1/2/3) 与 `evidence_tier` 正交（L2 命中结构化替代时 tier=structured 但 level=2）
15	- D6: `auth_required`/`permission_denied` 全局熔断（跳过全部 L2 同花顺候选，直达 L3/兜底）
16	- D7: `tool_call_blocked` 禁止任何降级（安全红线）
17	- D8: L3 `freshness=oneYear` 必带（实测不过滤混入 2022/2025 旧闻）
18	- D9: 落地顺序 = 先开 AGENT1_FALLBACK_CHAIN 验证 L2 挽救率 → L2 扩展 → L3
19	
20	**博查实测结论（真实 API，sk-8e78...，2026-09-06）**：19 条命中全权威财经源；freshness=oneYear 必带（不过滤混旧闻）；totalResults=10000000 是固定假值不可用于覆盖率；summary 字段含关键数值无需二次抓正文；多源交叉能纠偏单源日期漂移（60GWh 订单实际 04-27 非摘录的 06-24）。BOCHA key 在 ~/.workbuddy/skills/bocha-search__skillhub/config.json，进后端需复制到 backend/.env 的 AGENT1_BOCHA_API_KEY。
21	
22	**测试骨架验证**：`pytest test_cascade_fallback.py -v` → **6 passed / 8 skipped**。修正了初版 importorskip 放模块顶部导致全部跳过的问题，改为仅跳过 L3 相关——红线用例（C-10 document/web_unverified 不进计算链、C-13 覆盖率封顶、FB3）即刻转绿固化，未实现的 S3/S4/S5/L3 用例自动跳过不拖垮测试集。
23	
24	**实施顺序**：S0 开 FALLBACK_CHAIN 验证 → S1 快照 → S2 测试骨架（已完成）→ S3 Schema → S4 planner._l2_candidates → S5 executor 编排 → S6 WebSearchClient → S7 normalizer → S8 service 汇总块 → S9 熔断/预算/缓存 → S10 契约 → S11 前端 → S12 全量回归。
25	
26	## 其他
27	- 火山方舟新 LLM key（ark-6a61...30309）已写入 backend/.env，鉴权通过（404 UnsupportedModel=过了认证层，错key=401 对照）。但发现 `.env` 里 `LLM_BASE_URL=api/plan/v3` 与 `LLM_MODEL=ark-code-latest` **不匹配**（ark-code-latest 不支持 agent plan 端点），待用户确认正确模型名/端点。
28	
29	## L3 联网兜底「启用但未触发」根因定位（2026-09-06 晚）
30	
31	用户反馈 Agent4 质量门大量数据不足，问"联网插件启用了没"。实测结论：**配置全开、接口可用，但 L3 从未被触发**。
32	
33	- 配置已开：`AGENT1_WEB_FALLBACK_ENABLED=true`、`AGENT1_BOCHA_API_KEY` 已配、`AGENT1_FALLBACK_CHAIN=true`、budget 15/20。
34	- 代码已通：factory 建客户端 → executor L3 触发 → normalizer 打标 `web_unverified`，链路完整。
35	- **博查真实冒烟（4 组查询）全部有结果**：装机量 8 条（含"2025年国内动力电池装车量769.7GWh 同比+40.4%"）、三家市占率 3 条、碳酸锂 1 条、中创新航财务 8 条 → 缺的数据联网全能拿到。
36	- 但 16:37 那次运行产物：`report.md` 中"博查/联网/网络检索"出现 **0 次**，`report_view.json` 无 `web_sources` 块 → L3 零产出。
37	
38	**根因 1（主）：触发条件错位——降级只看「调用成败」，不看「数据是否真满足需求」**
39	- `executor.py:187-191` `can_degrade = main.record.status != "succeeded" and ...`
40	- 问财对查不到的指标**不返回空，而是静默回退行情/宏观占位数据**；两道防线均 fail-open 放行：
41	  - `field_relevance.py:141` `_field_relevance_check` 只对 BUSINESS/BASIC_INFO/STOCK_SELECTOR 生效，MACRO/INDUSTRY 一律放行；
42	  - `field_relevance.py:257-258` `_rows_usable_precheck` 列名对不上目标字段时 `return True`。
43	- 已用模拟脚本证实：查"碳酸锂价格"返回 CPI → `status=succeeded` → L3 永不触发（与报告"仅有CPI宏观数据"吻合）；查"装机量"返回空壳(值=None) → 同样 succeeded。
44	
45	**根因 2：时间盒饥饿**——`AGENT1_DEGRADATION_TIME_BUDGET` 默认 20s（.env 未设），而 `TOOL_TIMEOUT_SECONDS=600`。L2 一次慢调用就吃光 20s，`executor.py:226` `not self._time_box_exceeded(started)` 直接把 L3 挡在门外。
46	
47	**根因 3（设计红线，非 bug）**：L3 证据恒 `qualitative_only=True`，被 `data_interpreter/calculations.py:56` `and not item.qualitative_only` 排除在计算外 → 只能补定性、不能补数值。"数值未参与计算"是红线设计结果。
48	
49	**附带发现（非联网问题）**：报告里的 `宏观@值`、`期货@交易单位数量` 是原始 KV 字段名直漏（A2 字段清洗缺失，对应 ReadabilityLinter R6）；`shift异常检测（CRITICAL）`、`基线区间[0.8,0.8]` 是内部质检信息泄漏（R7）。这两类修了联网也不会消失，需单独修字段清洗。
50	
51	**待确认**：REQ-02（市占率 missing）模拟显示 0 行应触发 L3，但产物无痕迹 → 需确认 planner 是否压根没生成该 task，还是被时间盒挡掉。
52	
53	## A14-01「新能源车板块PE-PB估值」20 条警告归因（同上问题，但成因不同）
54	
55	用户追问该最差样本的 20 条 data_quality_issues 是否也因联网兜底。**结论：不是，20 条里最多 1 条与联网有关。**
56	
57	实测最新 transcript（`eval/transcript/agent14_live_20260906T045545Z/A14-01.json`）：
58	- `acquisition_level` 全部为 1（107 条），**零条 2（文档）、零条 3（联网）** → 一次降级都没发生；
59	- `evidence_tier` 全 `structured`（93 条）、`qualitative_only` 全 false；
60	- `status: succeeded` 出现 14 次 → **14 个 task 全部判成功** → `can_degrade` 全 False → L2/L3 均未触发（与动力电池 case 同一根因）；
61	- `bocha` 0 次、`market_quote_fallback` 0 次；web_search 的 4 次出现均为技能枚举与 prompt 模板文本，非真实调用。
62	
63	**20 条构成拆分**：12 条 shift 异常检测（方法论假阳性）+ 4 条「同数据点择优保留」（正常去重被误计）+ 4 条真实缺口。
64	
65	**Bug A：shift 检测对累计值/比率指标全面误报（anomaly.py:148）**
66	`change = (values[i] - values[i-1]) / abs(values[i-1])`
67	1. 产量是**累计口径**，序列 16524000→1604000→3015000→4290000→5776000→7399000。跨年重置（1652.4万→160.4万）被判 -90.3% CRITICAL；其后单调递增的累计值又被逐个判 +88%/+42.3%/+34.6%/+28.1%。**6 条全假阳性**。
68	2. PPI 同比是**百分点**指标，代码却用比率算：-0.9%→+0.5% 应记 +1.4 个百分点，被算成 +155.6% CRITICAL。**负值/跨零时公式彻底失真**。
69	
70	**Bug B：去重择优按「被淘汰证据条数」刷 issue（reconciliation.py:131-146）**
71	对每条非 winner 的重复证据都发一个 medium impact 的 DataQualityIssue → 3 证据冲突点刷出 2 条 issue，问题数量被人为放大。属正常流程，不应计入质量门。
72	
73	**真根因（与联网无关）**：Agent1 用「新能源车概念标签」选股，返回**最新价/涨跌幅**（行情数据）而非 PE/PB，样本混入高新发展（建筑装饰）且无比亚迪/宁德时代。即 P0-6 静默回退行情 + 概念标签污染。`market_quote_fallback` 0 次说明未拦住——因 `_field_relevance_check` 只覆盖 BUSINESS/BASIC_INFO/STOCK_SELECTOR，而本 case 主要走 SECTOR/INDEX（sector 257 次、index 125 次），**不在校验范围**。
74	
75	**修复优先级**：① anomaly.py 增加「累计口径」与「比率类指标（用百分点差）」判定分支；② reconciliation 按冲突点聚合而非按淘汰条数；③ `_field_relevance_check` 扩到 SECTOR/INDEX。另注：`calculated_metrics=0` 是红线设计（web 证据恒 qualitative_only，被 calculations.py:56 排除），估值类诉求即使 L3 触发也进不了计算链。

## L3 根因修复落地（2026-09-06 深夜，AI 实施完成）

按上文诊断的 5 项方案全部落地，代码级回归 + 真实环境验证双通过。

**改动清单**：
1. **field_relevance.py**：①新增语义相关性校验（仅意图任务 task_origin != baseline）：期望指标锚点与返回数据（数据列名+实体列取值——宏观/期货的指标名在实体值里）零交集 → field_mismatch；②行情回退校验对意图任务扩展到 SECTOR/INDEX（A14-01 根因）；③预检兜底：列名对不上时要求至少存在非空业务值（空壳数据判失败）。基线任务（P0 全量扫描）维持 fail-open 不受影响。
2. **planner.py**：意图子需求任务把 sub.metrics 指标名追加进 expected_fields 作为语义锚点（基线任务不追加）。
3. **executor.py**：①失败原因细分 field_mismatch/market_quote_fallback；②L3 检索词构造并入 GENERIC_PROFILE_FIELDS 泛型字段剔除（L1/L2 不再把「指标名称 指标值」发给博查）；③**文档通道对指标型诉求只是部分覆盖**（D2 封顶 partial 语义落地）：L2 文档通道挽救后若任务携带指标锚点，继续尝试 L3 补数值——L3 命中则替换，失败保留文档挽救。
4. **config.py/.env**：AGENT1_DEGRADATION_TIME_BUDGET 20s→360s（上限 120→600）；新增 AGENT2_WEB_NUMERIC_ENABLED（默认 False 红线不变，本机 .env=true 用户已授权）。
5. **normalizer.py**：①field_mismatch 行整批隔离+data_gap 披露（QuarantinedRecord Literal 扩展 field_mismatch）；②联网证据数值抽取（用户授权后取摘要首个「数字+单位」组合，value 变 float、unit 真实单位、qualitative_only=False、原文+来源标注保留在 notes）；③原始 KV 字段名展示清洗（宏观@值→值，R6）。
6. **calculations.py/service.py**：_numeric_evidence 提取，web_numeric_enabled 开关放行 web_unverified 数值（document 定性仍恒拒，FB4 红线不变）。

**真实环境回归（碳酸锂价格 case，SkillHub+博查真打）**：L1 问财返回空 → L2 NEWS 文档定性（指标诉求继续）→ **L3 博查命中 level=3**，产出 3 条碳酸锂价格证据（94864/94366/84472 元/吨，SMM 指数/生意社基准价），qualitative_only=False 参与计算，url+站点+原文标注完整。CPI 占位路径由单测覆盖（test_field_relevance_macro_cpi_vs_lithium_intent_task_fails 等）。

**测试**：新增 test_field_mismatch_degradation.py（14 用例全过：语义判定/预检兜底/L3 触发/基线保护/隔离披露/数值授权开关/红线默认）；backend/tests/agents 全套通过；eval/tests L0 13 项全过。附带发现并修复 test_factory 旧断言问题（.env 开 L3 后真实博查打破 evidence==[]，测试已显式关闭 L3 隔离，非生产缺陷）。

**遗留**：①R7 质检信息泄漏（shift异常检测/基线区间进报告）待单独修——需排查 anomaly.py 检测结果流向章节写作的路径；②anomaly.py 累计口径/百分点误报（A14-01 Bug A）与 reconciliation 按淘汰条数刷 issue（Bug B）未动；③方案 1 完整版（A2 需求未满足回灌 L3 的跨 Agent 回路）未做，当前以「意图任务指标锚点语义校验」作为 Agent 1 内轻量替代。
