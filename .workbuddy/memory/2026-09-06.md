# 2026-09-06 工作日志

## 三层级联降级最终落地方案（已定稿 + 测试骨架）

**产出**：
- `docs/plans/2026-09-06-agent1-cascade-fallback-FINAL.md`（最终落地版，整合 doc-channel-fallback + 联网选型 + 设计稿 + 博查实测）
- `backend/tests/agents/data_fetcher/test_cascade_fallback.py`（S2 测试骨架，红着提交）

**9 个关键决策（D1-D9，方案 §0）**：
- D1: L2 = 2a 结构化替代优先 + 2b 文档通道兜底（从 SKILL_CAPABILITIES 的 entity/metric 交集推导，非拍脑袋）
- D2: L2 命中覆盖率封顶 partial，跨口径强制 caliber 标注（防 P0-6 静默降级重演）
- D3: L3 定稿**默认博查**（实测合格，Tavily 仅海外主题，本版不实现留配置位）
- D4: 联网证据恒 web_unverified + qualitative_only；前端弹窗 + 决策包 WEB-SOURCE-UNVERIFIED 留痕（弹窗可关、留痕不可抵赖）
- D5: `acquisition_level`(1/2/3) 与 `evidence_tier` 正交（L2 命中结构化替代时 tier=structured 但 level=2）
- D6: `auth_required`/`permission_denied` 全局熔断（跳过全部 L2 同花顺候选，直达 L3/兜底）
- D7: `tool_call_blocked` 禁止任何降级（安全红线）
- D8: L3 `freshness=oneYear` 必带（实测不过滤混入 2022/2025 旧闻）
- D9: 落地顺序 = 先开 AGENT1_FALLBACK_CHAIN 验证 L2 挽救率 → L2 扩展 → L3

**博查实测结论（真实 API，sk-8e78...，2026-09-06）**：19 条命中全权威财经源；freshness=oneYear 必带（不过滤混旧闻）；totalResults=10000000 是固定假值不可用于覆盖率；summary 字段含关键数值无需二次抓正文；多源交叉能纠偏单源日期漂移（60GWh 订单实际 04-27 非摘录的 06-24）。BOCHA key 在 ~/.workbuddy/skills/bocha-search__skillhub/config.json，进后端需复制到 backend/.env 的 AGENT1_BOCHA_API_KEY。

**测试骨架验证**：`pytest test_cascade_fallback.py -v` → **6 passed / 8 skipped**。修正了初版 importorskip 放模块顶部导致全部跳过的问题，改为仅跳过 L3 相关——红线用例（C-10 document/web_unverified 不进计算链、C-13 覆盖率封顶、FB3）即刻转绿固化，未实现的 S3/S4/S5/L3 用例自动跳过不拖垮测试集。

**实施顺序**：S0 开 FALLBACK_CHAIN 验证 → S1 快照 → S2 测试骨架（已完成）→ S3 Schema → S4 planner._l2_candidates → S5 executor 编排 → S6 WebSearchClient → S7 normalizer → S8 service 汇总块 → S9 熔断/预算/缓存 → S10 契约 → S11 前端 → S12 全量回归。

## 其他
- 火山方舟新 LLM key（ark-6a61...30309）已写入 backend/.env，鉴权通过（404 UnsupportedModel=过了认证层，错key=401 对照）。但发现 `.env` 里 `LLM_BASE_URL=api/plan/v3` 与 `LLM_MODEL=ark-code-latest` **不匹配**（ark-code-latest 不支持 agent plan 端点），待用户确认正确模型名/端点。
