# 2026-09-11

## 误删会话卡片恢复（运维类，非代码改动）

现象：用户报告「刚刚把工作任务删除了」，侧边栏对话卡片消失。

排查与结论：

- 首次用 `sqlite3 ...?mode=ro&immutable=1` 读 `~/.workbuddy/workbuddy.db`，得出「同花顺 0 条被删」的**错误结论**。
- 根因：该库 WAL（~1MB+）远大于主库（~200KB），近期改动几乎全在 WAL；`immutable=1` / `mode=ro` 都不回放 WAL → 读到过期快照。
- 修正：把 `.db` + `-wal` + `-shm` 三个文件一起 `cp` 到临时目录，用可写连接打开副本，自动回放 WAL。
  实测 41 条 → 70 条已删，今天新删的 29 条全部浮出。

实际删除情况：今天 10:21–10:27 批量删除 29 个会话，其中同花顺 4 个：

| 会话 | id | 删除时间 |
| --- | --- | --- |
| 优化团队Git工作流与代码管理 | 4da1593c-d1a3-4445-989d-22d2cea4b768 | 10:24:15 |
| 了解项目智能体1和2测试方案 | 97f60209-0ed9-434b-8673-8da6f7ef9a38 | 10:24:17 |
| 检查代码MCP启用与索引 | a797c6d0-3d91-4dd3-8f1e-47b1d68f09f2 | 10:25:43 |
| 查看 GitHub 仓库当前状态 | d01138de-f3d9-4093-ba4e-9104ef850bf8 | 10:25:50 |

处理：备份 `~/.workbuddy/workbuddy.db*` 到 `~/.workbuddy/backup-20260911-104441/`，
对 `sessions` 表执行 `UPDATE ... SET deleted_at=NULL WHERE id IN (上述4个)`，4 行全部恢复。
对应 `~/.workbuddy/projects/.../<id>.jsonl` 记录文件均完好（45KB–3.2MB），
`~/.workbuddy/tasks/4da1593c/` 下 3 条任务清单文件也在 → 卡片点开内容完整，无数据丢失。

遗留：其余 25 个今日被删会话（WorkBuddy 工作区 / automation 等）按用户要求未处理，仍处于 `deleted_at` 有值状态。

---

## 2026-09-11 晚 ~ 09-12 凌晨：人机审核 + 五类历史问题（本轮核心产出）

### 产出文档
- `docs/任务清单-五类已记录问题-20260911.md`（在 `agent/chart-mvp-sync` 上）：16 条任务 + 任务×智能体对照 + **修复深度评估**章节。
- `frontend/prototype/review-workbench-revamp.html`：按现有「精品研报风」复刻的审核工作台改动预览（6 处改动，含现状对照模式）。

### ⚠️ 分支状态（重要）
工作区已从 `main`（归档版 `tag: archive/main-before-20260909`）**切到 `agent/chart-mvp-sync`**（开发分支）：
docs 恢复 43 份历史文档、backend 恢复完整实现。**切换导致我在 main 上新建的未提交文件丢失**（任务清单已重建；prototype html 需复查是否还在）。
`.workbuddy/memory/` 也被 git 跟踪 → 同样被切换覆盖。今后读代码默认走工作区即可，不必再 `git show agent/chart-mvp-sync:`。

### 现有人机审核机制（实测）
- 双轨契约：结构化轨 `ReviewRequest.edited_data` → 五阶段专属 ReviewEdits（`validate_stage_edit_whitelist`，extra=forbid，仅 revise/regenerate 允许）；自由文本轨 `comment`(≤2000) → `review_feedback`。
- `DataInterpretReviewEdits` 含 `evidence_items`≤200 + `rejected_claim_ids`≤100 → **"删数据/否结论"的原生落点**。
- 对话框解析器 `agents/common/feedback_interpreter.py`(714 行)：`FeedbackStage` **只支持 data_fetch / chart_generate 两阶段**；op 白名单 7+6 项（**无"删除证据"op**）；阈值 `confidence_accept=0.90` / `confidence_review=0.75`（落 [0.75,0.90) → `pending_review` **只标记不执行**）。
- 生效条件极严（`data_fetcher/service.py:154-155`）：`parser_mode=="llm" and any(status=="applied")`。
- **失败兜底 = 代码自认的"粗暴拼接 root cause"**（`planner.py:90-99` 注释原文）：结构化失败则把用户原话前 500 字拼进每个查询的 suffix。
- `ReviewView`(423 行) **不渲染** pending_review/rejected/reject_reason → 用户看不到改动为何没生效。

### 现有前端形态（复刻依据）
设计语言「精品研报风」（`style.css` 覆盖 Element Plus）：藏青 `#1e3a5c` / 金铜 `#a9853f` / 米白 `#f6f4ef` / 卡片 `#fffefb`；衬线标题 Georgia+宋体；圆角 卡片4/按钮3/tag2。
工作台三栏 grid `250px/1fr/360px`（左 ProjectTree、中 StageStepper+阶段卡片、右 el-tabs 四页签），≤1100px 塌单列。
**非纯阻塞**：`ReviewView` 有 `POLL_INTERVAL_MS=3000` 轮询；仅提交动作同步等待 + 全屏遮罩；运行状态在组件 ref，**无 Pinia run store**。

### 五类问题归属（详见文档）
- 问题集中在 **Agent 2（7 条）与 Agent 4（10 条）**；**Agent 3/5 确定性，完全不涉及**模型类问题。
- 三条传播路径：Agent2→Agent4（假阳性进正文，根在 2）、Agent1→Agent2（选股污染→结论无法回答）、LLM 适配层共用→1/2/4 同时受影响。
- **已修未深究 7 条**，最危险：**T5.1 关 thinking 可能牺牲深度（零质量数据）**、**T1.2 确定性修复静默丢弃 claim（报告变薄无报错）**。
- 未修 5 条：T3.1 shift 假阳性、T3.2 去重刷 issue、T3.4 R7 泄漏、T2.2 评审器无 few-shot、T2.3 校准脚本缺失+线上从未启用。
