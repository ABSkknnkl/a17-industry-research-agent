完整方案前，我需要把要改动的几个文件的精确现状再核对一遍（字段名、函数名、行号），避免方案里写错。先并行读取核心文件。


用户要完整的方案。我已经深入理解了项目现状。现在需要给出一份完整、可落地的方案文档。

用户之前的诉求是：给 Agent 4（chapter_writer）的 LLM 结构化输出做"双层契约"——LLM 出宽松半结构化 → normalizer 确定性收紧成严格 Schema，而不是让 DeepSeek 直接死磕严格 Schema。

我已经读过的关键文件：
1. `openai_compatible.py`：LLM 集成层，`_structured_output`/`_coerce_structured_response`/`generate_chapter`/`_normalize_known_schema_aliases`
2. `schemas/chapter.py`：`ChapterDraft`/`SectionDraft`/`ParagraphDraft`/`SectionVisualSemantics`，有多个 model_validator 硬约束
3. `chapter_writer/service.py`：StageAgent 入口，graph.ainvoke 失败 → build_fallback_writing 整单 fallback
4. `chapter_writer/graph.py`：LangGraph（prepare section_planner select_chapter_claims generate audit accept），每章 repo.save_chapter
5. `chapter_writer/fallback.py`：build_fallback_writing
6. `chapter_writer/prompt_adapter.py`：technical_output_contract
7. `data_fetcher/normalizer.py`（范本，我只读了 docstring 前 60 行？实际上我读了头部）

等等，让我回顾我实际读到的内容。我读过：
- chapter_writer/service.py（完整，228-239 行的 catch）
- chapter_writer/graph.py（完整）
- chapter_writer/fallback.py（部分，L41-52）
- chapter_writer/prompt_adapter.py（L135-170 technical_output_contract）
- schemas/chapter.py（L36-40, L43-65, L68-128, L141-149, L163-169）
- integrations/llm/openai_compatible.py（多处）
- data_fetcher/service.py（上上轮）
- data_interpreter/service.py + graph.py（上轮）

实际上，我有些引用是来自我自己的 thinking，需要确认我是否真的读了这些文件。从对话历史看，我在前几轮通过 run_mcp Exec 读了：
1. graph.py (chapter_writer) - 读到了 _audit_chapter L75-198, _review_gate L296-492
2. state.py, stages.py（更早轮次，从 memory 而非本次会话实际读取？）

实际上前面的对话是连续的多轮，我在最初一轮读过 graph.py、state.py、stages.py。后面几轮我读了 chapter.py、openai_compatible.py、service.py、fallback.py、prompt_adapter.py、data_fetcher/normalizer.py、data_interpreter/service.py、data_interpreter/graph.py。

但等一下——我现在看到的是完整的对话历史，而实际我的记忆是这些轮次的内容。关键是我已经充分理解了代码结构，能给出准确的行号引用。

现在用户要"完整的方案"。我应该给出一份**完整、结构化、可落地的方案文档**。这是方案设计，不是要我立即改代码。用户在 Plan/Spec 语境之外的 Default mode 下，要求"完整方案"。

我应该问自己：要不要用 NotifyUser？不，NotifyUser 是 Plan/Spec mode 或 web-dev 用的，现在是 Default mode 且用户只是要方案文档，不是要我进入执行。

要不要写文件？用户说"给我完整的方案"，可能想要我直接在对话里输出，也可能想要一份文档文件。考虑到 memory 里有一条："NEVER proactively create documentation files (*.md)"，除非用户明确要求。用户说"给我完整的方案"——这个表述偏口头，倾向于在对话里给。但"完整方案"也可能希望落成文档。

我认为在对话里输出完整方案即可，不主动创建 .md 文件。如果用户之后想要文件，会说"写进文件"。

让我组织完整的方案。需要包含：

1. **背景与目标**（为什么做）
2. **现状诊断**（精确到文件+行号，说明"死磕严格 Schema"在哪、失败如何放大）
3. **方案总览**（双层契约 + 分层拦截的架构图/流程图）
4. **详细设计**（四层，每层：改哪个文件、改成什么、怎么改、验收标准）
5. **数据流变化**（改前 vs 改后）
6. **Schema 定义**（ChapterDraftLoose 的具体字段）
7. **normalizer 的具体逻辑**
8. **降级逻辑变化**（整单 → 单章）
9. **落地顺序与迁移计划**
10. **测试/验收**（回归 + 真实 LLM 冒烟）
11. **风险与回滚**
12. **边界**（什么不改，为什么——尤其 Agent 2 红线、截断/非JSON例外）

我需要确保方案具体、可执行，引用真实文件路径和行号。

让我先回顾一下我确实掌握的代码细节，确保引用准确：

**schemas/chapter.py 的关键约束（我从之前读到的）：**
- `ParagraphDraft.validate_analysis_references`（L36-40）：analysis 段落必须有 claim_ids 和 evidence_ids
- `SectionVisualSemantics`（L43-65）：visual_type 枚举 + density ge=0/le=1
- `_infer_visual_semantics`（L68-128）：content_type="auto" 时确定性推断
- `ChapterDraft.validate_section_ids`（L163-169）：section_id 顺序 SEC-XX-01/02/03
- 还有 L141-149 之类可能有其他 validator

实际上我之前引用了 chapter.py 的 L36-40、L43-65、L68-128、L141-149、L163-169。这些行号我有一定的信心，但为了方案严谨，我在写方案时应该用这些文件引用链接（file:///），并标注行号范围。不过我之前是凭 thinking 引用，可能有偏差。为了方案质量，我应该重新精确读取这几个文件的当前内容，确认行号和字段名，避免方案里写错字段名或行号。

实际上，我已经在前几轮读了这些文件。但由于要写"完整方案"，准确性和可落地性最重要。我应该重新精确读取关键文件，确认：
1. schemas/chapter.py 完整内容（ChapterDraft 所有字段 + 所有 validator）
2. openai_compatible.py 的 generate_chapter / _coerce / _normalize 完整逻辑（尤其是 ChapterModel 的 __init__、_structured_output、_json_from_content、_coerce_structured_response、_normalize_known_schema_aliases）
3. chapter_writer/service.py 完整
4. chapter_writer/graph.py 完整
5. chapter_writer/fallback.py 完整
6. chapter_writer/prompt_adapter.py 完整
7. data_fetcher/normalizer.py（范本）

这样方案才能精确到字段名、函数名、行号。

但是——这会消耗很多 context。我已经在前几轮读过了大部分。权衡：为了"完整方案"的准确性，我至少需要精确确认 schemas/chapter.py 和 openai_compatible.py 的 ChapterModel 部分（generate_chapter、_coerce、_normalize、_structured_output），以及 service.py 和 graph.py 的关键部分（这些是方案改动的主战场）。

我决定：重新精确读取这几个核心文件，确保方案准确。用并行 Exec 读：
1. schemas/chapter.py（完整）
2. openai_compatible.py 的 ChapterModel 相关段（__init__ 830-856、_structured_output、_coerce、_normalize_known_schema_aliases、generate_chapter 858-924）
3. chapter_writer/fallback.py（完整）
4. chapter_writer/prompt_adapter.py（完整）

实际上 openai_compatible.py 我前面只读了一部分行号区间。让我读完整文件？可能很长。我用 Grep 定位关键函数然后读。

让我并行读关键文件。为了效率，我批量读：

1. schemas/chapter.py 全文
2. chapter_writer/fallback.py 全文
3. chapter_writer/prompt_adapter.py 全文
4. openai_compatible.py 中 ChapterModel 相关（先 grep 定位）

让我先并行读 1、2、3 和 grep openai_compatible.py 里 Chapter 相关函数定义行号。

实际上，让我高效一点。我先并行：
- Read schemas/chapter.py 全文
- Read chapter_writer/fallback.py 全文
- Read chapter_writer/prompt_adapter.py 全文
- Grep openai_compatible.py 里 "def _normalize_known_schema_aliases"、"def _coerce_structured_response"、"class OpenAIChatChapterModel"、"def generate_chapter"、"def _structured_output"、"def _json_from_content"、"def _validate_payload" 的行号

然后用一次 Exec 读多个文件。由于 global rules 要求通过 MCP Exec，我用 run_mcp Exec。

让我一次性在 Exec 里读多个文件 + grep，减少往返。

我写一个 Exec 脚本：
- 读 schemas/chapter.py
- 读 chapter_writer/fallback.py
- 读 chapter_writer/prompt_adapter.py
- 读 openai_compatible.py（可能很长，但为了准确，读全文？先读，若太长后续再分段）

实际上为了避免输出过长，我先读 chapter.py、fallback.py、prompt_adapter.py 全文，以及 openai_compatible.py 用 grep 定位后分段读 ChapterModel 部分。

让我先做第一批并行读取。

考虑到我在前几轮已经读过 graph.py、service.py（chapter_writer）、data_interpreter 等，我主要需要补齐的是：
- schemas/chapter.py 精确内容
- openai_compatible.py 的 ChapterModel 精确实现
- fallback.py 精确内容
- prompt_adapter.py 精确内容
- data_fetcher/normalizer.py 精确内容（范本）

让我一次性读这些。

我先做一个 Exec 调用读 4 个文件全文（chapter.py, fallback.py, prompt_adapter.py, normalizer.py），再另一个 grep 定位 openai_compatible.py 关键函数。

实际上可以并行多个 run_mcp Exec call。让我并行发 2 个：
1. Exec: 读 chapter.py + fallback.py + prompt_adapter.py + normalizer.py
2. Exec: grep openai_compatible.py 关键函数行号

然后根据 grep 结果再读 openai_compatible.py 相关段。

开始。

核心 schema 和 fallback 已确认。最后读 openai_compatible.py 里 LLM 解析层的关键实现（`_structured_output` / `_coerce_structured_response` / `_normalize_known_schema_aliases` / `ChapterModel`）。


我已经掌握了所有关键文件。现在需要读 openai_compatible.py 的关键实现段，尤其是：
- `_structured_output` (240-325)
- `_json_from_content` (326-427)
- `_normalize_known_schema_aliases` (428-528)
- `_validate_payload` (529-558)
- `_coerce_structured_response` (559-609)
- `OpenAICompatibleChapterModel` (830-856) + `generate_chapter` (858-924)

让我读这些段。这些是"死磕严格 schema"的真正实现位置，方案必须精确到这些函数。

我需要读 openai_compatible.py 的 240-609 和 830-940 段。让我并行读。

最后确认 chapter_writer 的 graph 节点拓扑和 service 的失败处理精确结构（方案"单章降级"改动要落到具体节点）。


我现在已经完整掌握了所有关键代码。可以写一份精准、可落地的完整方案了。

关键事实梳理：

**LLM 解析链路（openai_compatible.py）：**
- `_structured_output`：DeepSeek 用 `json_mode` + `include_raw=True`（L246-250）
- `generate_chapter`（L858-924）：system prompt 附加 JSON 指令 → `_invoke_structured` → `_coerce_structured_response(response, ChapterDraft)`
- `_coerce_structured_response`（L559-573）：provider 截断检查 → parsed 非 None 用 parsed → 否则 json 解析 → `_validate_payload`
- `_validate_payload`（L529-556）：`_normalize_known_schema_aliases` 后 `schema.model_validate`，失败分语义/结构两类抛 `StructuredOutputError`
- `_normalize_known_schema_aliases`（L428-526）：对 ChapterDraft 删 extra 字段、中文枚举转英文、密度"高中低"→数值/%→小数、paragraph_id 前缀 PARA-/PAR- → P-、kind 中文→英文
- 失败后一次 repair turn（L893-908，冻结事实只改结构），再失败 raise → service catch → build_fallback_writing 整单

**严格 schema（chapter.py）：**
- `ParagraphDraft.validate_analysis_references`（L36-40）：analysis 段落必须同时有 claim_ids 和 evidence_ids
- `SectionVisualSemantics`（L51-65）：content_type 枚举（默认 "auto"）、density 0-1、key_metric_count 0-100
- `_infer_visual_semantics`（L68-128）：确定性推断 content_type + density
- `SectionDraft.complete_visual_semantics`（L141-149）：调用 infer，auto 时确定性推断
- `ChapterDraft.validate_section_ids`（L163-169）：section_id 顺序必须 SEC-XX-01/02/03
- `ChapterWritingResult.validate_complete_outline`（L219-223）：7 章顺序 CH-01~07

**fallback.py：**
- `build_fallback_writing`：7 章全部用 Agent 2 claims 生成确定性段落，claim 无 → methodology 占位

**service.py（chapter_writer）：**
- graph.ainvoke 失败 → build_fallback_writing 整单（前几轮已读，L227-239）

**graph.py（chapter_writer）：**
- 每章 generate → audit → accept，accept 时 repo.save_chapter（前面已读）

现在我要写完整方案。方案结构：

# Agent 4 双层契约改造方案

## 0. 目标与总览
## 1. 现状诊断（精确到函数/行号）
## 2. 方案架构（双层契约 + 分层拦截）
## 3. 详细改动（分文件、分步骤，含伪代码/关键代码片段）
   3.1 新增 ChapterDraftLoose（宽松中间态）
   3.2 LLM 集成层改 generate_chapter（指向宽松 schema）
   3.3 新增 chapter_writer/normalizer.py（确定性收紧）
   3.4 schema/chapter.py 下移可修复约束
   3.5 graph.py 单章降级（不击穿整单）
   3.6 service.py 合并 fallback
## 4. 数据流对照（改前 vs 改后）
## 5. 落地顺序
## 6. 测试与验收
## 7. 风险与回滚
## 8. 明确不改的边界

这个方案需要非常具体。用户明确要"完整方案"，我应该给出足够详细、可执行的方案，包括关键代码结构。

但要注意：这是"方案"，不是"立即改代码"。用户没有说要我动手改，是"给我完整方案"。所以我输出方案文档（在对话里），不写文件、不改代码。

关于是否写文件：memory 规则说 NEVER proactively create documentation files。用户说"给我完整的方案"，在对话里给即可。如果写文件反而违反规则。所以我在对话里输出。

关于是否用 code block 展示新增 schema/normalizer 的代码骨架：这些是"方案里的提案代码"，不属于 codebase，可以用 markdown code block。但要克制，只给关键骨架，不要写完整实现（方案阶段）。

让我组织。我会给出：

1. 总览（一段话 + 一层一图，用文字描述流程）
2. 现状诊断（3 个硬抛点 + 1 个放大点，精确行号）
3. 方案详细（四层，每层：改什么文件、怎么改、关键代码骨架、验收点）
4. 数据流对照
5. 落地顺序
6. 测试验收
7. 风险回滚
8. 不改的边界

我需要精确，行号引用用 file:/// 链接。

关键改动点的精确描述：

### 第 0 步：新增 `ChapterDraftLoose`（在 schemas/chapter.py 里，或新文件）
放在 chapter.py，紧挨 ChapterDraft 定义。字段：
- chapter_id: str（不强制 pattern，normalizer 会锚定，但可保留 pattern 因为 outline 决定）
- 实际上，宽松态应该连 chapter_id/section_id/paragraph_id 都允许 LLM 不填或乱填，由 normalizer 按 outline 生成。

设计宽松态字段：
```python
class LooseParagraph(ChapterContract):
    # 无 model_config extra forbid；这里仍继承 ChapterContract 会 forbid
    kind: str | None = None          # 允许空，normalizer 归一化为枚举
    text: str = ""                    # 允许空？不行，text 是核心，至少要非空才值得要这个段落
    claim_ids: list[str] = Field(default_factory=list)
    evidence_ids: list[str] = Field(default_factory=list)
    numeric_refs: list[dict] = Field(default_factory=list)
```

其实宽松态的设计哲学：LLM 只保证"有标题、有正文"，其余全部可空/可错，由 normalizer 收紧。所以 `ChapterContract`（extra="forbid"）不能用——宽松态要允许 extra 字段直接丢弃，字段要宽松校验。

我建议宽松态用 `model_config = ConfigDict(extra="ignore")` 甚至不证实类型，让 normalizer 做全部收紧。但完全无约束的 dict 不利于 prompt 和 trace。折中：宽松态字段全 Optional + extra="ignore"，枚举用 str。

### 第 1 步：LLM 集成层
- `_structured_output` 对 DeepSeek 用 `json_mode` + `include_raw`，其中 `include_raw=True` 是关键——它已经让 provider 的原始 content 保留，供 `_json_from_content` 确定性解析。这个机制要保留。
- 改 `generate_chapter`：schema 从 `ChapterDraft` 换成 `ChapterDraftLoose`。
  - 但注意：`_structured_output(chat_model, ChapterDraft, ...)` 在 `__init__` 里构建（L856）。换成 ChapterDraftLoose。
  - 实际上更彻底：对宽松态，可以不用 `with_structured_output`，直接用普通 chat + json_mode 解析。但保留 with_structured_output(json_mode) 来拿到 provider 的 finish_reason 等 diagnostics 是有价值的。所以保留，只换 schema。

### 第 2 步：新增 `chapter_writer/normalizer.py`
核心函数：
```python
def normalize_loose_chapter(
    loose: ChapterDraftLoose,
    *,
    outline: OutlineChapter,        # 章节大纲，锚定 section_id/title/purpose
    chapter_index: int,             # 1-7，生成 chapter_id CH-xx
    allowed_claims: list[AnalysisClaim],   # select_chapter_claims 结果
    allowed_evidence_ids: set[str],
    revision: int,
) -> ChapterDraft
```
职责（对应现有 `_normalize_known_schema_aliases` 里针对 ChapterDraft 的分支，但更完整 + 可降级而非抛错）：
1. 锚定 ID：chapter_id = CH-{index:02d}；section_id = SEC-{index}-{01/02/03}（按 outline.sections 顺序）；paragraph_id = P-{index}-{section}-{序号}（按段落顺序重排）
2. 段落 kind 归一化：中文→英文，非法→按规则推断（含 claim/evidence 且非风控词 → analysis；否则 narrative/methodology）
3. 引用回填/校验：analysis 段落的 claim_ids/evidence_ids 若为空，尝试从正文数字/claim.text 匹配 allowed_claims 回填；仍空 → 降级 kind=methodology（用 fallback.py 的诚实文案）
4. claim_ids/evidence_ids 过滤到 allowed 白名单；引用未知 ID → 剔除并记 issue
5. visual_semantics：跑 `_infer_visual_semantics`（已有），补 density/content_type
6. missing_inputs：聚合"缺证据"信号
7. 最后 `ChapterDraft.model_validate` 一次——此时应通过（因为 normalizer 已保证所有硬约束）

### 第 3 步：schema/chapter.py 下移约束
- `ParagraphDraft.validate_analysis_references`：保留作为"终极防线"，但 normalizer 保证不会触发；或者改为"降级"——但 model_validator 不能降级（只能抛）。所以正常 keep，由 normalizer 保证。实际上我们可以选择删掉这个 validator，把校验逻辑放进 normalizer（normalizer 里 analysis 段必须有引用，否则降级）。删掉 validator 更干净——因为 strict model 的语义约束移到了 normalizer。但这个 validator 也保护"手工构造"的 ChapterDraft。权衡：保留 validator（作为防线），normalizer 负责让它不触发。这样改动最小、最安全。
  - 决定：**保留 schema 的 model_validator 不动**（它们是对的防线），把"修复/降级"逻辑放 normalizer。这是最小改动且不破坏现有 fallback.py（fallback 生成的 ChapterDraft 也会过这些 validator）。

等等，这里要重新想清楚。之前我说"把 model_validator 下移为修复规则"。但仔细看：
- `validate_analysis_references` 抛错，是因为 fallback 之外，LLM 生成的 analysis 段落忘了填引用。normalizer 若能回填/降级，就不会触发这个 validator。
- 所以其实**不需要改 schema**——只要 normalizer 在 model_validate 之前把段落修好，validator 就只是"最终防线"。这大幅降低改动风险和回归面。

结论修正：**schema/chapter.py 基本不动**（只保留 validator 作防线），核心改动是：
1. 新增 ChapterDraftLoose（宽松态）
2. 新增 chapter_writer/normalizer.py（确定性收紧，吸收并扩展 `_normalize_known_schema_aliases` 的 ChapterDraft 分支）
3. 改 openai_compatible.py 的 ChapterModel：generate_chapter 出 ChapterDraftLoose，然后调 normalizer 收紧成 ChapterDraft（或 normalizer 放 graph 层？）
4. 改 graph.py：单章降级
5. 改 service.py：合并 fallback

normalizer 放哪一层？有两个选择：
- A. 放 LLM 集成层（openai_compatible.py 内或调 chapter_writer/normalizer.py），generate_chapter 直接返回收紧后的 ChapterDraft —— 这样 graph/service 完全不用感知宽松态，改动最小最安全。
- B. 放 graph 层（generate 节点拿到 loose 后 normalize）。

我倾向 A：`generate_chapter` 内部保持"返回 ChapterDraft"这个对外契约不变，内部从"宽松 schema → normalizer → 严格 ChapterDraft"。这样 graph.py 的 generate 节点、service.py 都不用改接口，只是失败语义要改（normalizer 也失败 → 降级而非抛）。

但 normalizer 需要 outline + allowed_claims 这些信息，这些在 runtime_prompt 里已经有了（prompt_adapter 构建），但在 generate_chapter 的签名里没有。generate_chapter 现在只有 system_prompt + runtime_prompt。normalizer 需要结构化输入（outline、claims），不能从 runtime_prompt 字符串反解。

所以更干净的设计是：**normalizer 放在 graph 层**，因为 graph 层有 outline（state 里有 chapter 配置）、analysis（有 allowed_claims）。让 generate 节点：
1. 先调 model 的"宽松生成"方法（新方法 `generate_chapter_loose` 返回 ChapterDraftLoose）
2. 再调 normalizer 收紧成 ChapterDraft

或者拆成两层：
- protocol/集成层：`generate_chapter_loose` 返回 ChapterDraftLoose（宽松，基本不会因 schema 失败而 raise，只有 json 语法/截断/非 json 才 raise）
- graph 层：normalizer 收紧 → 失败则单章 fallback

这样职责最清晰：
- LLM 集成层：只负责"拿到可解析的宽松 JSON"（json 语法/截断层面的健壮性，已有 _json_from_content）
- graph 层 + normalizer：确定性收紧 + 单章降级

这个分层最符合项目现状（graph 层已经做了 audit/accept/save，有 outline 和 analysis 上下文）。

所以最终方案的分层：

**改动点 1：新增 `ChapterDraftLoose` 宽松态 schema**（schemas/chapter.py）
**改动点 2：LLM 集成层**（openai_compatible.py）
- `OpenAICompatibleChapterModel` 增加 `generate_chapter_loose`（或用参数控制 schema），返回 `ChapterDraftLoose`
- `_normalize_known_schema_aliases` 的 ChapterDraft 分支可以简化为"宽松态本就不 strict"，大部分归一化下沉到 normalizer
- 保留 repair turn（但针对宽松态，失败率已大幅下降）
**改动点 3：新增 `chapter_writer/normalizer.py`**（确定性收紧 Loose → ChapterDraft）
- 吸收 select_chapter_claims 的结果做引用回填
- 吸收 _infer_visual_semantics
- 吸收 fallback.py 的"诚实占位"做段落降级
**改动点 4：改 graph.py**（generate 节点：腰间生成 → normalizer → 成功则 audit/accept，失败则单章 fallback 标记，继续下一章）
**改动点 5：改 service.py**（去掉"graph 抛异常→整单 fallback"，改为读取 graph 产出"已成功章节 + fallback 章节"合成 ChapterWritingResult）

还要注意：现在 graph 的 generate 节点直接 `model.generate_chapter` → draft，然后 audit（`_audit_chapter`）。我前面读过 graph.py L75-198 是 _audit_chapter。让我回忆 graph.py 结构：

graph.py（chapter_writer）节点：prepare → section_planner → select_chapter_claims → generate → audit → accept → route（下一章 or finalize）。每章循环。

实际上我需要重新确认 chapter_writer/graph.py 的完整结构，因为我只在前几轮部分读过。让我读 graph.py 全文，确保方案里的"单章降级"改动点精确。

实际上前面第一轮我读过 graph.py，看到了 `_audit_chapter`（L75-198）和 `_review_gate`（L296-492）和 save_chapter（L331-339）。但我没有 graph 的完整节点拓扑（prepare/section_planner/select_chapter_claims/generate/audit/accept 这些节点名）。为了方案精确，我应该读 graph.py 全文。

让我读 chapter_writer/graph.py 全文 + service.py 全文（确认当前的 catch 和落盘逻辑）。

这是必要的，因为"单章降级"改动必须精确到 graph 的 generate/audit/accept 节点和 service 的 catch 块。

让我读这两个文件。

我现在完整掌握了所有代码。可以写完整方案了。

关键结构总结：

**graph.py（chapter_writer）节点拓扑：**
- `generate`（async）：`model.generate_chapter()` 返回 ChapterDraft → dump 到 state["draft"]
- `audit`：`_audit_chapter` → issues 到 state["current_issues"]
- `route_after_audit`：无 issues → accept；有 issues 且 attempts < 1 → revise；否则 accept
- `revise`：attempts+1，revision_count+1
- `accept`：`aggregate_chapter_references` → `repo.save_chapter`（持久化是接受的一部分，写失败必须阶段失败）→ chapters[chapter_id] = draft
- `route_after_accept`：current_index >= len → finalize，否则 generate（下一章）
- `finalize`：合成 ChapterWritingResult

**关键：generate 节点调用 `model.generate_chapter()` 如果抛 StructuredOutputError，会冒泡到 service.py 的 `graph.ainvoke` catch → 整单 `build_fallback_writing`。**

注意 generate 节点里，`model.generate_chapter` 内部已经有 repair turn 和最终 raise（openai_compatible L918）。所以 schema 失败沿路径：`_coerce_structured_response` raise → generate_chapter repair 后再 raise → graph generate 节点抛 → graph.ainvoke 抛 → service catch → 整单 fallback。

**关键洞察：fallback 只发生在"graph.ainvoke 抛异常"这一层**。而 runtime_prompt 的构建（build_chapter_runtime_prompt）在 generate 节点内部完成，包含 `technical_output_contract`（严格 schema 要求）和 `select_chapter_claims` 的 allowed_claims。

现在方案设计，关键要处理：normalizer 放哪里，以及如何拿到 allowed_claims/outline（这些在 graph 层有）。

我的最终方案设计：

## 核心架构决策
**把"宽松生成"和"确定性收紧"拆在两层：**
1. LLM 集成层（openai_compatible.py）：`generate_chapter` 改造成先产出 `ChapterDraftLoose`（宽松，容 json 语法/截断，只在这些真失败才 raise）
2. graph 层（chapter_writer/graph.py）：新增 normalizer 调用，把 Loose → ChapterDraft（用 outline + allowed_claims 收紧），失败则单章 fallback

但等等，有个更简单的方案：让 `generate_chapter` 内部也做 normalizer 收紧，返回 ChapterDraft。但 normalizer 需要 outline 和 claims，这些 generate_chapter 签名里没有。而 graph 的 generate 节点有。

所以两个选择：
- **方案 X（改动小）**：graph 的 `generate` 节点改成：调 `model.generate_chapter_loose()`（新增方法，返回 ChapterDraftLoose）→ 调 `normalizer.normalize_loose_chapter()`（graph 层调用，传入 outline/claims）→ 得到 ChapterDraft。若 normalizer 失败 → 标记降级。
- **方案 Y（更彻底但动 protocol）**：改 ChapterWritingModel protocol，把 generate_chapter 签名改成 return Loose，normalizer 全在 graph。

我选 **方案 X 的变体**，最小侵入：
1. `protocol.ChapterWritingModel` 增加 `generate_chapter_loose`（或把 `generate_chapter` 的返回类型改为 Loose，但会破坏 mock 和其他实现——需评估）。

实际上让我看看 mock 是怎么实现的。memory 里说 mock.py 有 MockAnalysisModel。chapter model 的 mock 呢？service 里 `_normalize_charts` 检查 `chart_result.data.get("mock")`。chapter model 的 mock 可能在 protocol 里 or 单独 mock 文件。

我不确定 chapter model 的 mock 实现位置。方案里我应该提示"需要同步改 mock 实现"。但为了不过度展开，我在方案里标注这个影响点。

让我确定 protocol.py 的 ChapterWritingModel 定义。前面没读 protocol.py 的 chapter 部分。让我快速确认，避免方案里写错接口名。

实际上，从 openai_compatible.py 和 service.py 的 import 看：
- `from app.integrations.llm.protocol import ChapterWritingModel`
- `model.generate_chapter(system_prompt=..., runtime_prompt=...) -> ChapterDraft`

所以 `ChapterWritingModel` Protocol 有 `generate_chapter` 返回 `ChapterDraft`，`model_name` 属性。

我需要读 protocol.py 确认 ChapterWritingModel 和 AnalysisModel 定义，以及 mock 在哪。让我读 protocol.py 和找 mock。

实际上为了方案完整性和准确性，我应该读 protocol.py（ChapterWritingModel）和 mock.py（chapter model mock）。但方案阶段，我可以：
1. 读 protocol.py 确认接口
2. 用 Grep 找 ChapterWritingModel 的所有实现类（openai_compatible + mock）

让我做这两个，然后写方案。这是最后一批读取。

其实我已经读得够多了。让我读 protocol.py（可能不长）和 grep mock 里的 chapter model。

让我并行读 protocol.py 全文 + grep "generate_chapter" 在整个 backend 的分布（找所有实现）。

我现在完整掌握了所有相关信息。关键点：

`generate_chapter` 的实现有几处：
1. `OpenAICompatibleChapterModel.generate_chapter`（openai_compatible.py L858）
2. `MockChapterWritingModel.generate_chapter`（mock.py L106）
3. `RuntimeAwareChapterWritingModel.generate_chapter`（model_gateway.py L41）—— 包装器，调用 self._model.generate_chapter

所以如果我要改 protocol 的 `generate_chapter` 返回类型或新增 `generate_chapter_loose`，需要同步改：
- protocol.py 的 ChapterWritingModel Protocol
- openai_compatible.py 的 OpenAICompatibleChapterModel
- mock.py 的 MockChapterWritingModel
- model_gateway.py 的 RuntimeAwareChapterWritingModel（包装器）

这是 4 处。改动面可控。

现在，我决定最终方案。为了最小侵入 + 清晰的分层，我采用这样的设计：

**核心方案：在 graph 层做"宽松生成 + 确定性收紧 + 单章降级"，保持 protocol 层的最小变化。**

具体：

### 决策点：normalizer 放哪、怎么拿 allowed_claims/outline

graph 的 `generate` 节点已经有了：
- `chapter = _OUTLINE_BY_ID[chapter_id]`（outline）
- `build_chapter_runtime_prompt` 内部调 `select_chapter_claims` 拿到 allowed_claims

所以 normalizer 的逻辑自然放 graph 层（或 chapter_writer 内部模块）。

但我注意到：`select_chapter_claims` 和 `allowed_evidence_ids` 的逻辑目前分别在 prompt_adapter.py 和 graph.py `_audit_chapter` 里各算一次。normalizer 需要同样的 allowed_claims + allowed_evidence_ids，可以复用 `select_chapter_claims`。

### 最终方案设计（5 个改动点）

**改动点 A：新增宽松态 schema（schemas/chapter.py）**

新增 `ChapterDraftLoose` 及相关 `SectionDraftLoose`/`ParagraphDraftLoose`，字段宽松、extra="ignore"、无 model_validator 硬约束。

```python
class LooseParagraph(ChapterContract):  # 注意要 extra="ignore" 而非 forbid
    model_config = ConfigDict(extra="ignore")
    kind: str | None = None
    text: str = ""
    claim_ids: list[str] = Field(default_factory=list)
    evidence_ids: list[str] = Field(default_factory=list)
    numeric_refs: list[dict[str, object]] = Field(default_factory=list)

class LooseSection(ChapterContract):
    model_config = ConfigDict(extra="ignore")
    section_id: str | None = None
    title: str = ""
    purpose: str = ""
    key_points: list[str] = Field(default_factory=list)
    paragraphs: list[LooseParagraph] = Field(default_factory=list)
    chart_ids: list[str] = Field(default_factory=list)
    uncertainties: list[str] = Field(default_factory=list)
    visual_semantics: dict[str, object] = Field(default_factory=dict)

class ChapterDraftLoose(ChapterContract):
    model_config = ConfigDict(extra="ignore")
    chapter_id: str | None = None
    title: str = ""
    summary: str = ""
    sections: list[LooseSection] = Field(default_factory=list)
    claim_ids: list[str] = Field(default_factory=list)
    evidence_ids: list[str] = Field(default_factory=list)
    chart_ids: list[str] = Field(default_factory=list)
    missing_inputs: list[str] = Field(default_factory=list)
    revision: int | None = None
```

其实宽松态甚至可以更简单：用 `dict` 不验证。但用 Loose schema 的好处是：启用 json_mode 的 with_structured_output 需要一个 schema 来让 provider 生成匹配 JSON，且便于 trace/snapshot。所以保留 Loose schema，但所有字段可空/可错。

**改动点 B：LLM 集成层（openai_compatible.py）**

1. `_structured_output` 对 ChapterDraftLoose 也走 json_mode（DeepSeek）——已有逻辑，不用改 `_structured_output`，只要传入 ChapterDraftLoose。
2. `OpenAICompatibleChapterModel.__init__`：`self._structured_model = _structured_output(chat_model, ChapterDraftLoose, model_name)`；但保留 repair 逻辑针对 loose（修复成本更低，因为宽松态几乎必过）。
3. `generate_chapter` 返回类型改为 `ChapterDraftLoose`。
4. `_normalize_known_schema_aliases` 里的 `ChapterDraft` 分支：大部分（中文枚举/密度/前缀）可以下沉到 normalizer，但 type normalize 仍在这里对 loose 做一次"尽力归一化"也行。为减少改动，我建议：`_normalize_known_schema_aliases` 增加对 `ChapterDraftLoose` 的分类（但目前它 else 分支直接 return payload）。因为 Loose extra="ignore" + 字段宽，其实不需要 alias 归一化，normalizer 会做。所以这层可以几乎不动。

关键点：LLM 集成层职责收敛为"拿到可解析的宽松 JSON，非 JSON/截断才 raise"。repair turn 针对宽松态（几乎不会因 schema 失败触发，只有 json 语法/截断触发 repair）。

**改动点 C：新增 normalizer（chapter_writer/normalizer.py）**

核心函数：

```python
def normalize_loose_chapter(
    loose: ChapterDraftLoose,
    *,
    outline: OutlineChapter,
    allowed_claims: list[AnalysisClaim],
    revision: int,
) -> ChapterDraft
```

职责（确定性收紧，全在此收敛）：
1. 章节级锚定：chapter_id = outline.chapter_id；title = outline.title（标题必须与大纲一致，不强求 LLM 填对，直接覆盖）；summary 若空 → fallback 文案。
2. 小节级锚定：按 outline.sections 顺序，逐个生成 section_id = outline.section.section_id；title/purpose 从 outline 取（LLM 提供的内容用于正文，标题以大纲为准）。LLM 若提供了对应小节的正文，则用；否则该小节降级。
   —— 这里要处理 LLM sections 数量 != 3 或顺序乱：normalizer 按 outline 的 3 个 section 遍历，从 loose.sections 里"尽力匹配"（按 title 或顺序）。
3. 段落级：对每个确定 section，取其 loose paragraphs，逐段：
   - paragraph_id = P-{chapter}-{section}-{序号}（确定性生成，忽略 LLM 给的）
   - kind 归一化（中文→英文，非法→推断：有 claim/evidence 且无风控词→analysis，否则 methodology）
   - text 空 → 丢弃该段（或 padding）
   - claim_ids/evidence_ids 过滤到 allowed 白名单 + 剔除未知
   - analysis 段若过滤后 claim/evidence 为空 → 降级 kind=methodology，text 用 fallback 诚实文案
   - numeric_refs 保留（audit 层会用 classify_number 重新校验）
4. visual_semantics：解析 loose 的 dict → 构造 SectionVisualSemantics，跑 `_infer_visual_semantics`（复用现有）补全 content_type/density。
5. 节级 claim_ids/evidence_ids/chart_ids 从段落聚合。
6. 章节级 claim_ids/evidence_ids/chart_ids 聚合；missing_inputs 聚合。
7. `ChapterDraft.model_validate` 一次通过（normalizer 已保证硬约束满足）。

失败语义：normalizer 若产出不了合法 ChapterDraft（例如所有段落 text 都空、无法形成 min_length 满足的 sections），抛一个明确的 `ChapterNormalizationError`，由 graph 层捕获做单章 fallback。

**改动点 D：graph.py（chapter_writer）**

`generate` 节点改为：
```python
async def generate(state):
    ...
    loose = await model.generate_chapter(...)  # 返回 ChapterDraftLoose
    try:
        chapter = normalize_loose_chapter(
            loose,
            outline=chapter,
            allowed_claims=select_chapter_claims(analysis, chapter_id, set(rejected)),
            revision=state["workflow_revision"],
        )
    except ChapterNormalizationError:
        # 单章降级：用 fallback 构建该章的确定性 ChapterDraft
        chapter = build_single_chapter_fallback(...)  # 从 fallback.py 抽出单章版本
        state["quality_issues"] 追加 "chapter_id:normalization_fallback"
    return {"draft": chapter.model_dump(...)}
```

关键：**generate 节点内部 try/except，不再把 schema/normalizer 失败抛出去**，从而 `graph.ainvoke` 不会因单章失败而整体抛异常 → 不再触发 service 层整单 fallback。

但注意：`model.generate_chapter` 内部若仍 raise `StructuredOutputError`（json 语法/截断彻底失败，not provider error 也 raise），需要在这里也 catch。

所以 generate 节点 catch 的范围：`StructuredOutputError`（json 层彻底失败）+ `ChapterNormalizationError`（normalizer 失败）→ 都走单章 fallback。只有 provider 网络错误（也包成 StructuredOutputError PROVIDER_ERROR，retryable）——这个要不要单章 fallback？provider 错误可能只是暂时，但为了不击穿整单，也应该单章 fallback（该章占位，其余章继续）。这样更稳健。

但要保留一个"彻底失败"的逃逸：如果 generate 遇到的不是这些已知错误（例如意外的 bug），仍让它 raise，避免静默掩盖真实 bug。这符合 memory 里"Agent 2 失败必须终止"的精神，但这里 Agent 4 的定位是"单章降级"，所以：
- 已知可降级错误（StructuredOutputError 全类 + ChapterNormalizationError）→ 单章 fallback
- 未知异常 → 继续 raise（保持 fail-closed，不吞 bug）

**单章 fallback 需要从 fallback.py 抽出**：现在 `build_fallback_writing` 是全 7 章的。抽出一个 `build_single_chapter_fallback(outline, claims, revision, reason)` 返回单个 ChapterDraft，供 graph 单章降级用；`build_fallback_writing` 改为循环调用它（或保留）。

实际上 fallback.py 里已经有一段 per-chapter 的构建逻辑（L36-109 的 for outline 循环内）。抽出单章版本很自然。

**改动点 E：service.py**

现在 graph.ainvoke 不再因单章 schema/normalizer 失败而抛异常（已单章降级），所以 service 的 `except` 整单 fallback 只在"未知异常"时触发（真兜底兜底），这是合理的最终防线，保留即可。

但 service 还需要处理一种情况：graph 正常返回，但 result 里某些章是 fallback（quality_issues 里有标记），此时 status 应如何？看现有 finalize：quality.passed = not issues。若单章 fallback 会在 quality_issues 里追加标记，则 finalize 的 collaboration_requests 会带上 CHAPTER-QUALITY，质量门 passed=False → 走 review。这已经是合理的（诚实暴露降级）。

所以 service.py 基本不用改，只需确认 `build_fallback_writing` 仍在"未知异常"时触发（保留）。实际上可以微调：`_safe_failure_reason` 保留。

等等，还有一个细节：graph 里 `accept` 节点 `repo.save_chapter` 写失败会 raise（L328 注释说"写入失败必须使阶段失败"）。这个 raise 会冒泡到 service 整单 fallback，这是对的（持久化失败是基础设施问题，不该单章降级，应该整单失败让上层处理）。所以 accept 节点的 raise 语义保留，不动。

### 数据流对照

改前：
```
generate: model.generate_chapter → ChapterDraft (严格校验，失败 raise)
  → 失败冒泡 graph.ainvoke 抛 → service except → build_fallback_writing 整单(7章全确定占位)
```
改后：
```
generate: model.generate_chapter → ChapterDraftLoose (宽松，几乎不失败)
  → normalize_loose_chapter → ChapterDraft (normalizer 收紧)
     - 成功 → audit → accept → save (正常)
     - 失败 → build_single_chapter_fallback 该章单章降级 → 继续下一章
  → 未知异常 → 才冒泡 → service except → build_fallback_writing 整单(终极兜底)
```

### 落地顺序

分 3 个 PR/阶段：
1. 阶段1（最小，先做）：graph.py 单章降级（改动点 D 的 try/except + 单章 fallback 抽取）+ fallback.py 抽单章函数。这一步不引入 Loose schema，先解决"一章失败击穿七章"。它把现有 `model.generate_chapter` 抛的 StructuredOutputError 在 generate 节点 catch 掉做单章降级。
   - 收益：立即消除"七章全丢"。
2. 阶段2：引入 Loose（改动点 A/B/C）。LLM 集成层出宽松态，normalizer 收紧。这一步解决"DeepSeek 反复 schema 失败"的根因。
3. 阶段3：清理（可选）。把 `_normalize_known_schema_aliases` 的 ChapterDraft 分支收敛/移到 normalizer，prompt 里 technical_output_contract 精简。

阶段1 与阶段2 可以独立验证，降低风险。

### 测试验收

1. 单元：`test_chapter_normalizer.py`——给 normalizer 喂各种"宽松但有问题"的 loose（漏 claim、section_id 乱、density 中文、extra 字段、kind 中文、analysis 缺引用），断言产出合法 ChapterDraft 且引用/降级正确。
2. 集成：`test_chapter_model.py`（已有 generate_chapter 测试）改类型断言。
3. agent 层：`test_agent.py` 里 `FailingChapterModel`/`RepairingChapterModel` 的测试行为要更新——现在它们测"失败→整单 fallback"，改成"失败→单章降级"。
4. 真实 LLM 冒烟：`LLM_USE_MOCK=False` + 真实 key，跑 core 用例，断言不再出现"整单 fallback"、报告章节齐全。
5. 回归：跑 backend/tests/agents/chapter_writer 全量 + test_pipeline.py。

### 风险与回滚

- 风险1：normalizer 的"标题以大纲为准直接覆盖"可能丢失 LLM 对标题的合理改写——但大纲是固定契约（audit 里本来就会因标题不一致报 issue），所以覆盖是对的。
- 风险2：单章降级可能掩盖 LLM 系统性失败（每章都 fallback）——通过 quality.passed=False + collaboration_requests + review 门暴露，不静默。
- 风险3：normalizer 引用回填可能误回填（把正文数字匹配错 claim）——回填要严格（claim_id 明确 + evidence_id 属于该 claim），否则宁可降级不硬凑。
- 回滚：阶段化，每阶段独立可回滚；改动集中在 chapter_writer + openai_compatible + chapter.py，不影响 Agent 1/2/3/5。

### 不改的边界（重要）
- Agent 2 的 schema 不动（本轮只做 Agent 4）。
- `ChapterWritingResult` / `ChapterDraft` 严格 schema 本身不动（它们是下游 Agent 5 + 持久化的契约，normalizer 负责产出合法规格的 ChapterDraft）。
- `accept` 节点的持久化 fail-closed 语义不动。
- `_audit_chapter` 的证据溯源/数字校验不动（那是 audit 层，normalizer 之上）。

现在我把这些组织成完整方案输出。要控制篇幅，但用户明确要"完整方案"，所以可以详细，但要结构清晰、可扫读。

关于是否用代码块：新增 schema 和 normalizer 的关键骨架用 markdown code block（提案代码，不在 codebase）。其余用文字 + file 引用。

我会写得完整但精炼。用清晰的层级标题。

让我写最终方案。不用 TodoWrite（这是输出方案，不是执行任务）。

关于长度：这个方案确实需要完整，但我要避免冗余。重点给出：现状、方案架构、5 个改动点的具体内容（含关键代码骨架）、数据流、落地顺序、测试、风险边界。

开始写最终回复。
代码已全部核对完毕。下面是你项目的完整改造方案（方案，未改任何代码）。

---

# Agent 4「双层契约」完整改造方案

## 0. 目标与结论先行

把 Agent 4 的 LLM 结构化输出从「让 DeepSeek 直接命中严格 `ChapterDraft`」改成「**LLM 出宽松 `ChapterDraftLoose` → 确定性 normalizer 收紧成严格 `ChapterDraft` → 单章降级不击穿整单**」。

一句话原则：**LLM 只负责「内容对、结构宽松」，normalizer 负责「补全/归一化/降级/校验」，schema 校验是最后一道防线而非第一道闸门。**

---

## 1. 现状诊断（精确到函数与行号）

### 1.1 LLM 集成层（[openai_compatible.py](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/integrations/llm/openai_compatible.py)）

- `__init__` 直接 `_structured_output(chat_model, ChapterDraft)`（[L856](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/integrations/llm/openai_compatible.py#L856)）→ DeepSeek 走 `json_mode` + `include_raw`（[L240-250](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/integrations/llm/openai_compatible.py#L240-L250)）。
- `generate_chapter`（[L858-924](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/integrations/llm/openai_compatible.py#L858-L924)）→ `_coerce_structured_response` → `_validate_payload`，一次 repair turn 后再失败 `raise`。
- `_validate_payload`（[L529-556](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/integrations/llm/openai_compatible.py#L529-L556)）里只有 `_normalize_known_schema_aliases` 一道「薄」归一化（删 extra、中文枚举、密度「高中低」→数值、`PARA-`→`P-`），然后 `model_validate` 硬抛。

### 1.2 严格 schema 的三个硬抛点（[chapter.py](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/schemas/chapter.py)）

| 约束 | 位置 | 可确定性修复？ |
|---|---|---|
| `analysis` 段落必须**同时**有 claim+evidence | [L36-40](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/schemas/chapter.py#L36-L40) | ✅ 可回填/可降级 |
| `section_id` 顺序精确 SEC-XX-01/02/03 | [L163-169](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/schemas/chapter.py#L163-L169) | ✅ 可重排 |
| density 0-1、content_type 枚举 | [L51-65](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/schemas/chapter.py#L51-L65) | ✅ 已有 `_infer_visual_semantics` |

### 1.3 放大伤害点（[graph.py](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/graph.py) + [service.py](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/service.py)）

`generate` 节点（[L255-274](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/graph.py#L255-L274)）调 `model.generate_chapter`，一旦 schema 失败 `raise`，冒泡到 `graph.ainvoke` → [service.py L227-239](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/service.py#L227-L239) 捕获后 **`build_fallback_writing` 整单覆盖，已落盘的成功章节一并作废**。

---

## 2. 方案架构（三层拦截）

```
                          ┌─────────────────────────────────────────────┐
                          │  第2层：normalizer 确定性收紧（新增）          │
  LLM 输出(宽松 JSON) ───►│  ChapterDraftLoose → ChapterDraft            │
  （第1层：json_mode       │  锚定ID / 回填引用 / 推断密度 / 段落降级      │
   + include_raw，        └──────────────┬──────────────────────────────┘
   语法/截断仍 fail-closed）              │
                          ┌──────────────▼──────────────────────────────┐
                          │  第3层：单章降级（graph 改造）                │
                          │  normalizer 失败 → build_single_chapter_fallback│
                          │  → 继续下一章，不击穿整单                    │
                          └──────────────┬──────────────────────────────┘
                                         │
                          ┌──────────────▼──────────────────────────────┐
                          │  终极防线：未知异常才整单 fallback（保留）    │
                          └─────────────────────────────────────────────┘
```

---

## 3. 五个改动点

### 改动 A — 新增宽松态 schema（`schemas/chapter.py`）

新增 `ChapterDraftLoose` + `SectionDraftLoose` + `ParagraphDraftLoose`，`extra="ignore"`、字段全可空、无 `model_validator`：

```python
class LooseParagraph(BaseModel):
    model_config = ConfigDict(extra="ignore")
    kind: str | None = None
    text: str = ""
    claim_ids: list[str] = Field(default_factory=list)
    evidence_ids: list[str] = Field(default_factory=list)
    numeric_refs: list[dict[str, object]] = Field(default_factory=list)

class LooseSection(BaseModel):
    model_config = ConfigDict(extra="ignore")
    section_id: str | None = None
    title: str = ""
    purpose: str = ""
    key_points: list[str] = Field(default_factory=list)
    paragraphs: list[LooseParagraph] = Field(default_factory=list)
    chart_ids: list[str] = Field(default_factory=list)
    uncertainties: list[str] = Field(default_factory=list)
    visual_semantics: dict[str, object] = Field(default_factory=dict)

class ChapterDraftLoose(BaseModel):
    model_config = ConfigDict(extra="ignore")
    chapter_id: str | None = None
    title: str = ""
    summary: str = ""
    sections: list[LooseSection] = Field(default_factory=list)
    claim_ids: list[str] = Field(default_factory=list)
    evidence_ids: list[str] = Field(default_factory=list)
    chart_ids: list[str] = Field(default_factory=list)
    missing_inputs: list[str] = Field(default_factory=list)
    revision: int | None = None
```

> 说明：宽松态仍用 schema 而非裸 dict，是为了让 `json_mode` 的 `with_structured_output` 有 schema 可对齐、trace/snapshot 有结构。但**所有字段允许错、漏、多余都删**。

### 改动 B — LLM 集成层（`openai_compatible.py` + `protocol.py` + `mock.py` + `model_gateway.py`）

1. `OpenAICompatibleChapterModel.__init__` 改为 `_structured_output(chat_model, ChapterDraftLoose, model_name)`。
2. `generate_chapter` 返回类型改为 `ChapterDraftLoose`，repair turn 保留但只针对 `json_syntax_invalid` / `output_truncated`（宽松态几乎不会 schema 失败）。
3. `_normalize_known_schema_aliases` 的 `ChapterDraft` 分支（[L428-526](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/integrations/llm/openai_compatible.py#L428-L526)）**下沉到 normalizer**——此处不再做 Chapter 别名归一化。
4. 同步改协议与实现（共 4 处，均已定位）：
   - [protocol.py L24-30](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/integrations/llm/protocol.py#L24-L30) `generate_chapter` 返回 `ChapterDraftLoose`
   - [mock.py L106](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/integrations/llm/mock.py#L106) `MockChapterWritingModel.generate_chapter`
   - [model_gateway.py L41-52](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/runtime/model_gateway.py#L41-L52) `RuntimeAwareChapterWritingModel.generate_chapter`（透传包装器，仅改类型标注 + 透传）

### 改动 C — 新增 normalizer（新文件 `chapter_writer/normalizer.py`）

对齐 [data_fetcher/normalizer.py](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/data_fetcher/normalizer.py) 的「动态输入 → 稳定契约」范本：

```python
class ChapterNormalizationError(Exception):
    """normalizer 无法产出合法 ChapterDraft 时抛出，由 graph 单章降级。"""

def normalize_loose_chapter(
    loose: ChapterDraftLoose,
    *,
    outline: OutlineChapter,               # _OUTLINE_BY_ID 锚定章节/小节
    allowed_claims: list[AnalysisClaim],    # select_chapter_claims 结果
    revision: int,
) -> ChapterDraft:
    ...
```

收紧步骤（全确定性）：
1. **锚定 ID**：`chapter_id = outline.chapter_id`，`title = outline.title`，`summary` 空则用 fallback 文案；`section_id`/`title`/`purpose` 从 `outline.sections` 覆盖（LLM 给的小节正文按标题/顺序匹配进来）。
2. **段落归一**：`paragraph_id = P-{ch}-{sec}-{序号}`（忽略 LLM 给的）；`kind` 中文→英文、非法→按「有无引用 + 是否风控词」推断；`text` 空段丢弃。
3. **引用回填/白名单**：`claim_ids`/`evidence_ids` 过滤到 `allowed_claims` 白名单；`analysis` 段过滤后仍空 → **降级 `kind=methodology`**，复用 [fallback.py L41-52](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/fallback.py#L41-L52) 的诚实占位文案。
4. **visual_semantics**：由 loose 的 dict 构造 `SectionVisualSemantics`，再跑现有 [`_infer_visual_semantics`](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/schemas/chapter.py#L68-L128) 补全 density/content_type（把「LLM 必须填对」变「填错就推断」）。
5. **聚合**：节/章级 claim_ids/evidence_ids/chart_ids 从段落聚合，missing_inputs 聚合。
6. 末尾 `ChapterDraft.model_validate` **一次通过**（normalizer 已保证硬约束）；无法产出时抛 `ChapterNormalizationError`。

### 改动 D — graph 单章降级（`chapter_writer/graph.py`）

改造 `generate` 节点（[L255-274](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/graph.py#L255-L274)）：

```python
async def generate(state):
    ...
    try:
        loose = await model.generate_chapter(          # 返回 ChapterDraftLoose
            system_prompt=prompt.content,
            runtime_prompt=build_chapter_runtime_prompt(...),
        )
        chapter = normalize_loose_chapter(
            loose,
            outline=chapter,
            allowed_claims=select_chapter_claims(analysis, chapter_id, set(state["rejected_claim_ids"])),
            revision=state["workflow_revision"],
        )
    except (StructuredOutputError, ChapterNormalizationError):
        # 单章降级：不冒泡
        chapter = build_single_chapter_fallback(
            outline=chapter,
            claims=select_chapter_claims(...),
            revision=state["workflow_revision"],
        )
        state["quality_issues"].append(f"{chapter_id}:chapter_normalization_fallback")
    return {"draft": chapter.model_dump(mode="json")}
```

同时从 [fallback.py](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/fallback.py) 抽出一个 `build_single_chapter_fallback(outline, claims, revision, reason)`（把现有 `build_fallback_writing` 里的单章循环体抽出来，`build_fallback_writing` 改为复用）。

> 关键边界：只 catch `StructuredOutputError`（json 语法/截断/provider 全类）+ `ChapterNormalizationError`。**未知异常仍然 raise**，避免吞掉真实 bug；`accept` 节点 `repo.save_chapter` 的 fail-closed（[L328-339](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/graph.py#L328-L339)）保持不动——持久化失败仍应整单失败。

### 改动 E — service 层（基本不动，只确认语义）

`graph.ainvoke` 不再因单章 schema/normalizer 失败抛异常，[service.py L227-239](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/service.py#L227-L239) 的 `except` 退化为「未知异常」的终极兜底，保留即可。单章降级通过 `quality.passed=False` + `CHAPTER-QUALITY` collaboration_request 暴露到 review 门（[graph.py finalize L365-384](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/graph.py#L365-L384)），不静默。

---

## 4. 数据流对照

| | 改前 | 改后 |
|---|---|---|
| LLM 返回 | `ChapterDraft`（严格校验，常失败） | `ChapterDraftLoose`（宽松，几乎不失败） |
| 收紧 | 无（只有薄 alias 归一化） | `normalizer.normalize_loose_chapter`（确定性收紧） |
| 单章失败 | `raise` → 整单 fallback 覆盖 | 单章 fallback → 继续下一章 |
| 未知异常 | 整单 fallback | 整单 fallback（保留作终极防线） |

---

## 5. 落地顺序（分两阶段，各自可独立验证/回滚）

- **阶段 1（先做，解决放大伤害）**：只做改动 D（graph 单章降级）+ fallback 抽单章函数。不引入 Loose，先把现有 `model.generate_chapter` 抛的 `StructuredOutputError` 在 generate 节点 catch 做单章降级。**收益：立即消除「一章失败→七章全丢」。**
- **阶段 2（解决根因）**：改动 A + B + C，引入 Loose schema + normalizer。**收益：DeepSeek 不再死磕严格 schema。**
- **阶段 3（可选清理）**：把 `technical_output_contract`（[prompt_adapter.py L135-170](file:///Users/Zhuanz1/PycharmProjects/同花顺/backend/app/agents/chapter_writer/prompt_adapter.py#L135-L170)）里的 schema 硬要求精简为「normalizer 收紧参考」，删掉 prompt 里逼 LLM 逐字段命中的表述。

---

## 6. 测试与验收

1. **新增 `test_chapter_normalizer.py`**：喂各种「宽松但有毛病」的 loose（漏 claim、section_id 乱、density 中文、extra 字段、中文 kind、analysis 缺双引、空 text），断言产出合法 `ChapterDraft` 且引用/降级正确。
2. **更新 `test_agent.py`** 里 `FailingChapterModel`/`RepairingChapterModel` 的断言：从「整单 fallback」改为「单章降级」。
3. **更新 `test_chapter_model.py`**、`test_mock_chapter_model.py` 的返回类型断言为 `ChapterDraftLoose`（或转成 `ChapterDraft` 经 normalizer）。
4. **真实 LLM 冒烟**：`LLM_USE_MOCK=False` + 有效 key，跑 core 用例，断言「不再整单 fallback、报告七章齐全」。
5. **回归**：`backend/tests/agents/chapter_writer` + `backend/tests/workflow/test_pipeline.py` 全量。

---

## 7. 风险与回滚

- **风险 1（标题覆盖）**：normalizer 用大纲标题覆盖 LLM 标题——这是对的，因为 `_audit_chapter` 本就把标题不一致当 issue。
- **风险 2（引用误回填）**：回填必须严格（claim_id 属于 allowed + evidence_id 属于该 claim），否则宁可降级不硬凑，避免触碰「数据伪造」红线。
- **风险 3（掩盖系统性失败）**：每章都 fallback 时，靠 `quality.passed=False` + review 门暴露，不静默。
- **回滚**：阶段化改动集中收敛在 `chapter_writer/` + `openai_compatible.py` + `chapter.py`，不触碰 Agent 1/2/3/5；每阶段独立可 revert。

---

## 8. 明确不改的边界

- **Agent 2 的 schema 本轮不动**（它的 FAILED/WAITING_REVIEW 大量是刻意的 fail-closed 业务判死，不能放宽，见上一轮结论）。
- **`ChapterDraft` / `ChapterWritingResult` 严格 schema 本身不动**——它们是下游 Agent 5 与持久化的契约，normalizer 负责产出合法规格。
- **`_audit_chapter` 的证据溯源/数字校验不动**（那是 audit 层，normalizer 之上）。

---

方案到此完整。需要我按**阶段 1** 先出可落地的 diff（只动 `graph.py` + `fallback.py`，最小回归面），还是直接三阶段一起给出一份带 patch 的实现清单？