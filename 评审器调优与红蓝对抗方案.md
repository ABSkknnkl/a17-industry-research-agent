# 评审器调优与红蓝对抗方案（运营期）

> 前置文档：[评审器方案（6a914b52 版）](./6a914b52ee2c07afd5e3c063_2026-08-28-评审器方案.md)
> 适用对象：`ReadabilityLinter`（确定性锚）+ `ReadabilityLLMReviewer`（LLM 软分）
> 现状：评审器默认关闭（`READABILITY_REVIEW_ENABLED=False`），全部调优在线下进行，**不影响生产门禁**

***

## 0. 定位澄清：什么是"训练"，什么不是

| 想法                                               | 结论         | 原因                                                                                    |
| ------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------- |
| 把 PDF 直接"喂给"评审器让它学会                              | ❌ 不可行      | LLM-as-Judge 是提示词驱动的无状态裁判，没有记忆/训练通道，喂了也不会"变聪明"                                        |
| 微调（fine-tune）一个评审模型                              | ❌ 当前不建议    | ①无训练基建；②有效标注需 >3k 条，手里几个 PDF 达不到；③每次切 `LLM_JUDGE_MODEL` 都要重训；④评审错误已被红队门禁+外部验真兜住，收益不划算 |
| **PDF → 人工标注校准集 → 提示词 few-shot + 阈值标定 + 红蓝对抗回归** | ✅ 这是本方案的路线 | 成本低（百级样本）、可复现、每次换 judge 都能重跑复用                                                        |

**什么时候才考虑真微调**（触发条件，本期不满足）：judge 与人工一致率长期 <80% 且仲裁模型也救不回；或需要把 judge 换成本地小模型以省 token；或评审调用量达到每天 10 万段落级。

***

## 1. 你的金融 PDF 怎么用：构建「双人标注校准集」

PDF 的价值是当**地面真值（ground truth）来源**，不是当训练数据。管线如下：

### 1.1 处理管线

```
PDF（行业/策略/公司/事件点评四类语体）
  → pymupdf 按段落抽取（跳过目录/图表/页眉页脚/免责声明）
  → 粗筛（去掉 <30 字碎段、纯数字表格行）
  → 双人独立标注（A/B 两人背靠背）
  → 分歧段仲裁（第三人，或分歧即弃）
  → 入库 calibration_samples/
```

### 1.2 标注字段（每次标注产出一条）

```json
{
  "sample_id": "CAL-PDF-001",
  "source": "真实研报/自研报告/拦截库/合成",
  "text": "原文段落",
  "gold_label": "readable | hard_to_read | borderline",
  "dimensions": ["通顺度", "通俗度", "连贯性", "客观性"],
  "issues": ["R1_DOUBLE_SUBJECT", "裸标签", ...],   // hard 时填写
  "audience": "professional",
  "annotators": ["A", "B"],
  "resolved": true
}
```

### 1.3 抽样规则（每个 PDF → 30~~50 条，总量目标 150~~250 条）

| 分层                  | 比例  | 说明                                           |
| ------------------- | --- | -------------------------------------------- |
| 正样本（readable）       | 50% | 原文可直接当蓝队正样本，**术语密集的专业句是重点**                  |
| 负样本（hard\_to\_read） | 30% | 从 PDF 里找真坏句；不够就从拦截库补充（见 §2）                  |
| 边界样本（borderline）    | 20% | **最珍贵**：45 字长句边界、术语 2 个 vs 3 个、专业但难读——用于标定阈值 |

四个语体都要覆盖：宏观策略（术语密度低、逻辑链长）、行业深度（中密度）、公司深度（数字密集）、事件点评（短句、结论化）。缺一类，judge 就在那一类上失准。

### 1.4 为什么必须人工标注

judge 的正误标准必须来自人，否则就是"自己考自己"。双人背对背 + 分歧仲裁是给 judge 造一个可信的考卷。

***

## 2. 负向用例选取（红队，5 类 × 每类 8\~15 条）

| 类别                        | 样本来源（按优先级）                             | 举例                                    |
| ------------------------- | -------------------------------------- | ------------------------------------- |
| ①确定性必杀（Linter 必须 100% 全中） | 拦截库真实失败 + 现有 RT 系列扩展                   | 双主语病句、>45 字句、裸标签拼接、自夸                 |
| ②LLM 软判（Linter 判不了）       | 业务 bug 库 / 用户反馈 / 人工编写                 | 语义断裂（三句合法、读不出主旨）、术语堆砌不解释、数字轰炸不解读、指代混乱 |
| ③**对抗样本**（核心）             | 人工按逃逸假设构造                              | 见下表                                   |
| ④跨文体负向                    | 人工/采集                                  | 口语化、营销体、机翻腔、公告腔混写                     |
| ⑤真实失败回流                   | `REAL_CHAIN_BUG_REPORT.md`、上线报告被审驳回的段落 | 任何被用户/审核打回的坏段落，**永久入库**               |

### 2.1 对抗样本设计清单（红队攻击面）

| 攻击名  | 手法                            | 防守着力点                            |
| ---- | ----------------------------- | -------------------------------- |
| 词表逃逸 | 自夸换措辞："本报告基于严谨方法"（绕过"深入剖析"词表） | R3 词表扩展为句法模式："本报告+积极动词"          |
| 分段逃逸 | 把双主语长句拆成两个短句，每句单独合法           | 段落级连贯性检查（R5 扩展或 LLM 软判强调跨句逻辑）    |
| 引号伪装 | 把自夸句放进引号："所谓'本报告深入剖析'"        | 词表命中不只扫正文，含引号内                   |
| 术语稀释 | 单句塞 10 个术语但每个都不在词表            | 术语密度改为**比例**判定（术语字数/句长），不依赖词表全集  |
| 垃圾长句 | 用重复填充制造 45 字以上但结构"合法"的句子      | R2 引入信息密度（去重字比例）                 |
| 数字洪水 | 一句话塞 5 个百分比且不解读               | 数字密度规则（新增 R6）                    |
| 跨段指代 | 第一段"该公司"第二段才出现公司名             | LLM 软判上下文（当前只单段评审，留为已知盲区，评分降权处理） |

> 设计红线：对抗样本可以"骗过当前实现"——这正是它们入库的目的。但**不得**靠"删掉难样本"来提高得分（一票否决，见 §6）。

### 2.2 负向获取的总成本控制

真实失败优先、人工编写次之、合成变形兜底。每轮红蓝对抗新增对抗样本 ≤ 30 条，防止无限扩张。

***

## 3. 正向用例选取（蓝队防误杀，5 类 × 每类 8\~15 条）

| 类别          | 来源                                         | 防的是什么误杀                      |
| ----------- | ------------------------------------------ | ---------------------------- |
| ①真实研报高质量段落  | **你的 PDF（主力）**                             | "专业表达=堆砌"的整体误判               |
| ②术语密集但已解释句  | PDF + 人工改写                                 | R4 误杀（估值锚/利润池/景气度密集但句内带白话解释） |
| ③长句边界样本     | 40\~45 字、主谓清晰的长句                           | judge 把句长当唯一信号               |
| ④跨市场合规比较段   | 自研报告（含币种/会计准则/报告期披露）                       | 项目特有的口径披露被误判啰嗦               |
| ⑤黄金段（上线即真值） | `backend/artifacts/**/report.md` 中已通过审核的段落 | 已在线产出是最高价值的正样本               |

> 蓝队任务不是"让 judge 给高分"，而是**保证正样本的分数分布在合理区**。红蓝对抗的后验目标是：正样本 judge score ≥ 阈值，且分布在高分段（0.75+），而不是贴着阈值爬过。

***

## 4. 红蓝对抗机制（要做，按四轮循环）

### 4.1 循环结构（每轮必须产出量化指标）

```
R0 基线：现有红队样本 + 校准集首测
  输出：linter 抓取率 / LLM 误杀率 / judge-人工一致率
R1 红攻：按 §2.1 攻击面造对抗样本 → 打 judge，记录逃逸清单
R2 蓝防：按逃逸/误杀清单改 judge 提示词 or Linter 规则（只改提示词与词表，不改评分公式）
  正样本全集回归，确认没有按下葫芦浮起瓢
R3 复评固化：逃逸样本 → 注入 redteam_readability_samples.py 成回归集
```

**一个关键区分**：Linter 规则按"新样本是否能被确定性判断"分流——能确定性判的进 Linter（R6/R7…），判不了的进 judge 提示词 few-shot。

### 4.2 量化门禁标准（全部达标才发版启用评审器）

| 指标                           | 门禁                                                                              |
| ---------------------------- | ------------------------------------------------------------------------------- |
| Linter 对确定性类负样本（①类）抓取率       | **100%**（少一个都不许上线）                                                              |
| LLM 对软判负样本（②③类）              | score < `READABILITY_THRESHOLD`(0.6) 至少 90%                                     |
| LLM 对正样本                     | score ≥ 0.6 至少 95%，且平均分 ≥ 0.75                                                  |
| judge-人工标注一致率（Cohen's kappa） | ≥ 0.9 为优，0.8\~0.9 观察，**<0.8 触发引入仲裁模型**（DeepSeek 系交叉评，由指标触发不预先引入，沿用原方案 Task 6.2） |
| 误杀率（正样本被判 below）             | < 5%                                                                            |

### 4.3 成本预算

* 每轮对抗循环评审调用 ≤ 200 条段落（控制 token 成本）

* 红蓝对抗全程离线，用 `READABILITY_REDTEAM_LLM_GATE=1 pytest tests/agents/chapter_writer/test_readability_redteam.py` 显式触发，**永不混入日常确定性套件**（现有 opt-in 机制已具备）

***

## 5. 调优杠杆（按性价比排序，逐步启用）

| 层级 | 动作                                                                        | 落地位置                                                                                                                                              | 成本                |
| -- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| L1 | Linter 词表/规则增量（从逃逸样本提炼 R6 数字密度、R3 句法泛化、术语密度改比例）                           | [readability\_linter.py](../backend/app/agents/chapter_writer/readability_linter.py)                                                              | 零 LLM 成本          |
| L2 | judge 提示词 few-shot：四个维度各嵌入 2 正 2 负样例 + score 语义锚（0.9=可读，0.6=勉强可读，0.3=读不通） | [openai\_compatible.py](../backend/app/integrations/llm/openai_compatible.py) `OpenAICompatibleReadabilityModel.review_paragraph` 的 system prompt | 低（只加 prompt 不变结构） |
| L3 | 阈值标定：0.55/0.6/0.65 三档在边界样本上画准确率曲线，选拐点                                     | [config.py](../backend/app/core/config.py) `READABILITY_THRESHOLD`                                                                                | 低                 |
| L4 | 仲裁模型（仅当一致率 <0.8 被指标触发）                                                    | 新增 judge 配置位 + 分歧样本集                                                                                                                              | 高，**暂不做**         |

**评分配方（本期不引入新模型，只调 judge prompt）**：9:1 原则不变——Linter 判"客观可判定"的（病句/红线/裸标签/数字洪水），LLM 只判规则判不了的"通顺/通俗/连贯"软维度。凡能被 Linter 判的维度，judge prompt 明确要求"不重复给分"，避免权重翻倍。

***

## 6. 一票否决（命中共任一则，对抗循环作废重来）

1. **数据泄漏**：校准集里人工标注过的样本混进了 judge 的 few-shot 示例 → 作废
2. **删难样本提分**：靠从测试集删除难样本或改 labels 来提高一致率 → 作废
3. **正样本"全安全"造假**：正样本集不含任何术语密集/长句/跨市场样本（没挑战性的蓝队是假蓝队）→ 作废
4. **门禁绕过**：Linter 100% 抓取率未达标仍启用评审器 → 禁止发版

***

## 7. 落地排期（四阶段）

| 阶段     | 内容                                     | 产出                                                |
| ------ | -------------------------------------- | ------------------------------------------------- |
| 一 数据基建 | PDF 抽取 + 双人标注 + 边界样本                   | `calibration_samples/` 150\~250 条（正:负:边界 = 5:3:2） |
| 二 门禁脚本 | 校准集跑批脚本（judge-人工一致率/kappa/误杀率报表）       | `eval/readability_calibration.py`                 |
| 三 首轮红蓝 | R0→R1→R2→R3 循环至 §4.2 门禁达标              | 新规则/新提示词/红队回归集增量                                  |
| 四 运营化  | 周度重跑红队门禁；审驳回段落自动回流样本库；换 judge 模型必跑全套回归 | 运营规范                                              |

***

## 8. 结论

* **你的 PDF 要"喂"，但喂的是人工标注校准集 + few-shot 样例，不是喂进模型训练**

* **负向：5 类分层（必杀/Linter 软判/对抗/跨文体/真实失败），对抗样本是核心**

* **正向：5 类分层（真实研报/术语已解释/长句边界/跨市场合规/上线黄金段），专防误杀**

* **红蓝对抗：要做，四轮循环 + 量化门禁 + <0.8 触发仲裁；全程离线，一票否决防作弊**

* **调优杠杆按 L1→L4 顺序启用，本期只动 L1/L2/L3，不引入微调**

