S‑2 PDF 导出(pdf.py + main.py):常驻浏览器复用——懒启动一次 Chromium,断线(is_connected)自动重启；asyncio.Semaphore(2) 限并发；180 秒三层超时(外层 wait_for + set_content(timeout=180s) + page.set_default_timeout)。两个关键防御：①Playwright 句柄和 asyncio 原语不能跨事件循环(pytest 每个测试新循环)，状态按“当前循环”归属，换循环即重建；②超时取消后 page.close() 再包一层 5 秒 wait_for,防止清理动作本身挂死导致 180 秒上限失效。lifespan 收尾调 shutdown_pdf_renderer() 正常关浏览器。  
report_view.json 落盘:local.py 白名单加一项;service.py 把 ReportViewModel.model_dump(mode="json") 存盘。不进 manifest/artifacts 列表——那是交付契约(有精确的 kinds/数量断言)，view model 是内部夹具源，失败只记 export_issue。  
方案 A:markdown 无条件渲染(前端 ReportReader 预览源，预览永不断);output_formats 语义改为"交付格式”，空缺省 → HTML+PDF;formats 字段报交付格式(全失败时回退报实际落盘格式，防契约 min_length=1 炸掉)。默认运行落盘产物与今天完全一致(md/html/pdf),只有 formats 从三变二。更新 2 处测试断言。  
连带修(B/S 系列，按文档修法微调)

B‑1 A17:页眉改 {{ report.industry_topic }} · 行业研究系统;页脚 removesuffix('行业') 拼接避免“XX行业行业研究报告”。  
B‑2 简报丢图：在 assembler 里把 depth 计算提前，brief 时 placement 全部置空 → 三个渲染器现有的“未挂载图表进附录”路径自然接住，单点修复、三源一致，渲染器零改动。  
B‑3 目录锚点：章节 id="chapter-N"(用序号，不泄露 CH‑01 内部 ID——有测试断言内部 ID 不得出现)+ 目录 <a href>。  
B‑4 print 优先级:print 块用同权重后置规则枚举覆盖 visual-*/density-* 的版心规则。  
B‑5 草稿水印:position:absolute 移入封面(封面已 position:relative),不再全屏悬浮。  
B‑6 封面 5 项元信息：明确 5 列网格。  
S‑3 Jinja Environment 提到模块级 + cache_size=50(保留 mtime 检查，模板改了不用重启进程)。  
S‑4 placement 解析改正则安全解析，非法回退附录。说明：pydantic 的 ^SEC-\d{2}-\d{2}$ pattern 其实已让 IndexError 不可达，所以不加跨层 export_issues 管道。  
S‑5 边距去重：删模板 @page 的 margin,只留 Python 侧。  
排除：S‑1、T‑4(按你的指示)。  
测试：新增 test_render_contract.py(默认交付契约、brief 图表进附录、A17/锚点/print/水印/五列/report_view.json 回读、pdf-only 失败回退)+ test_pdf_renderer.py(假 Playwright:复用只启动一次、并发峰值≤2、0.1s 超时硬返回、shutdown 关浏览器、跨循环重建)。

开工。

执行工具 1 次  
pdf.py 重写完成。接入 main.py lifespan 收尾。

执行工具 2 次  
S-2 完成。现在改 service.py 的格式语义 + report_view.json 落盘，和白名单。

执行工具 3 次  
现在改产物落盘段落：report_view.json 保存 + formats 字段语义。

执行工具 1 次  
顺序有缺陷：view-model 落盘在 advisory_issues.extend(export_issues) 之后，它的问题进不了 unresolved_risks。把它挪到 extend 之前。

执行工具 1 次  
最后把 ReportFusionResult 构造里的 formats=generated_formats 换成 reported_formats。

执行工具 1 次  
更新 test_agent.py 两处 formats 断言以匹配新契约。

执行工具 2 次  
方案 A + 落盘完成。开始 B 系列：先 assembler.py 的 B-2(depth 提前计算，brief 时 placement 置空)。

执行工具 2 次  
B-2 完成。改 html.py:S-3 模块级 Environment + S-4 安全 placement 解析。

执行工具 1 次  
html.py 完成。现在改模板——一次性做 B-1/B-3/B-4/B-5/B-6/S-5 共六处。

执行工具 4 次  
CSS 六处完成。改模板 body:水印移入封面、eyebrow 去 A17、目录锚点 + 章节 id。

执行工具 3 次  
模板完成，任务 3 收尾。先快速确认没有其它地方消费 output_formats/formats 语义，然后写两个新测试文件。

执行工具 1 次  
注意到 eval 期望 required_artifacts: ["markdown","html","pdf","manifest"]——需要确认 eval 是按落盘产物还是按 formats 字段校验，否则默认交付改 HTML+PDF 会让 eval 误判失败。

执行工具 2 次

终端命令 已运行  
· cd /Users/Zhuanz1/PycharmProjects/同花顺 && grep -rn "required_artifacts" eval/ --include="*.py" | head; echo "---"; grep -n "required_artifacts" -A 20 eval/scorers/rules.py | head -50

终端命令 已运行  
· sed -n '40,90p' eval/scorers/stages.py

已思考  
· The eval checks fusion.get("artifacts", []) — the artifacts LIST (kinds), NOT the formats field. Since markdown is al...  
eval 按 artifacts kinds 校验而不是 formats 字段——markdown 恒落盘后四种 kind 都在，eval 不受影响 ✓。现在写两个新测试文件。








