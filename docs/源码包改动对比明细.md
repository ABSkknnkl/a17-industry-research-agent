# 源码包 vs 本项目（系统 4）改动对比明细

> 生成时间：2026-09-26 14:30
> 对比对象：`~/Downloads/行业研究智能体-全链路系统-源码包`（454 文件，最新文件 09-26 11:21）
> 基准：`~/Downloads/行业研究智能体-全链路系统 4`（本项目）
> 图例：**「我的」= 本项目独有**　**「源码包」= 对方独有**　`<` 代表本项目行、`>` 代表源码包行

---

## 0. 一句话结论

**两边是"分叉演进"，不是"谁新谁旧"**：本项目做了一批修复（D-01/D-04/D-06/D-08 等），
源码包做了另一批**新增功能**（JSON 自愈、图表章节均衡、依赖自愈、实体消歧、宏观解析、Remotion 视频工程）。
**没有任何一方是另一方的超集** → 需要择优合并，不能整车覆盖。

| 差异结构 | 数量 |
|---|---|
| 仅源码包有 | 15 项（含 1 个全新工程、7 个测试、2 个空目录） |
| 仅本项目有 | 27 项（含 `eval/` 评测层、`docs/` 交付文档、`backend/tests/l1/`） |
| 两边都有但内容不同 | **40 个文件** |

---

## 1. 【源码包独有】新增功能（这才是"别人新加的"）

### 1.1 全新独立工程：`remotion/`（2470 行 / 17 个 ts+tsx）

| 项 | 内容 |
|---|---|
| 技术栈 | React 19 + Remotion 4.0.529 + Tailwind 4 |
| 6 个 composition | `01_Overview`(430 行) + 五智能体各一个：DataFetcher 183 / DataInterpreter 174 / ChartGenerator 180 / ChapterWriter 191 / ReportFusion 175 |
| 公共组件 | `FlowConnector`（流程连线）、`ReportHeader`、`EngineBox`、`ContractCard`、`AnimatedBadge` |
| 用途 | 用代码生成**架构动画视频**（A17 汇报素材） |
| 耦合度 | **零耦合**，独立 `package.json`，不影响主项目 |

### 1.2 五个智能体的新增能力（逐个列）

#### A1 数据获取（data_fetcher）

| 文件 | 源码包新增 | 说明 |
|---|---|---|
| `agent.py` | `INTENT_SYSTEM_PROMPT` **重写** | 从"需求理解模块"改为"**需求理解与实体抽取**模块"，要求识别用户指定的重点公司 |
| `models.py` | **新增字段 `must_include_entities: list[str]`（≤20）** | 用户点名的公司，必须进检索任务 |
| `models.py` | `extra="forbid"` → **`extra="ignore"`** | ⚠️ 放宽了未知字段校验（2 处） |
| `skillhub.py` | **依赖自愈（自愈剥离失败依赖）** | 上游任务失败时，若下游任务自带有独立 `query`，则剥离失败依赖继续跑，"**避免级联击穿核心数据采集**" |
| `fusion.py` | **macro 专用解析分支** | 宏观表格记录（指标值/数值/时间/国家）单独解析，含 `_split_value_unit` 拆值+单位、未来日期丢弃 |

#### A2 数据解读（data_interpreter）

| 文件 | 源码包新增 | 说明 |
|---|---|---|
| `agent.py` | **新增 `industry_chain_segments` 解析回填** | 从 LLM 响应解析产业链分段，写回 `industry_chain_segments` |
| `engine.py` | `_entity(record, **context_map**)`、`resolve_ticker(code, context_map)` | **实体消歧上下文映射**：同一批次内用上下文修正实体名 |

#### A3 图表生成（chart_generator）

| 文件 | 源码包新增 | 说明 |
|---|---|---|
| `agent.py` | **提示词新增第 6 条「章节合理分布」** | 明确 7 章各自该放什么图，**"严禁将所有图表堆积在单一章节！单章节图表数建议 2~4 张"** |
| `agent.py` | 超时放宽 | LLM 超时 `45~60s` → **`120~180s`**；单次调用 15s → **45s** |
| `compiler.py` | **按 series 取单位** | `node_dict["margin"]` 的单位从 `table.unit` 改为 `table.series_units.get(primary_series_name)` |
| `render.py` | `resolve_ticker(..., context_map)`；散点 size 兜底 `or 8.0` | None 值不再炸 |

#### A4 章节写作（chapter_writer）

| 文件 | 源码包新增 | 说明 |
|---|---|---|
| `llm.py` | **JSON 自愈（新功能）** | ① 剥离 JSON 前的 Markdown 标题/引言；② **启发式修复字符串内未转义的双引号**（`"summary": "xxx政策转"放量"阶段"` 这类） |
| `agent.py` | 提示词第 5 条加**「严禁捏造未挂接图表占位符」** | `chart_ids` 为空时不得出现 `[CHART-xx]` |
| `agent.py` | 提示词第 9 条加**「极其重要规范」** | 输出必须是**纯 JSON**：禁 Markdown 标题、禁 ```json 代码块、内部引号用中文双引号 |
| `config.py` | `llm_timeout_seconds = **max(240.0, ...)**` | 超时下限抬到 240s |

#### A5 报告融合（report_fusion）

| 文件 | 源码包新增 | 说明 |
|---|---|---|
| `agent.py` | **从 `comps_matrix.total_market_cap` 推导 `canonical_wan_yi`** | 统一"样本总市值"口径（万亿元），再据此做全文替换 |
| 测试 | 新增孤儿图表占位符清理用例 | `[CHART-06-01]` 这类不存在的占位符必须被清掉 |

### 1.3 新增测试（7 个文件，401 行）

| 文件 | 行数 | 覆盖的新能力 |
|---|---|---|
| `data-fetcher/tests/test_dependency_healing.py` | 87 | 依赖自愈 |
| `report-fusion/tests/test_dynamic_cross_chapter_consistency.py` | 77 | 跨章动态一致性 |
| `chart-generator/tests/test_chart_binning_and_routing_balance.py` | 59 | 图表分箱与路由均衡 |
| `data-analysis/tests/test_engine_dynamic_refactor.py` | 59 | 引擎动态重构 |
| `chapter-writer/tests/test_dynamic_segment_and_scale_consistency.py` | 53 | 动态分段与量级一致性 |
| `data-fetcher/tests/test_macro_fusion_refactor.py` | 34 | 宏观融合重构 |
| `backend/tests/test_adapters_entity_extraction.py` | 32 | 实体抽取 |

既有测试内也补了用例：`test_render_scatter_and_bubble_with_none_values`、`test_clean_and_parse_json_heals_unescaped_quotes_and_markdown_headers`、`test_must_include_entities_passes_task_validation`、`test_auto_attach_charts_balances_across_sections`、3 个状态机用例（数据清洗全领域 / 单章重写重置融合 / resume 前置校验）。

### 1.4 新增脚本与文档

| 项 | 行数 | 说明 |
|---|---|---|
| `scripts/run_live_frontend.js` | 190 | 前端实跑脚本 |
| `项目深度技术调查报告.md` | 327 | 技术审计，含真实运行实例 `run-20260926092741-655` 分析 |
| `backend/app/exporters/` | — | ⚠️ **空目录**（无文件） |
| `frontend/e2e/` | — | ⚠️ **空目录**（无文件） |

---

## 2. 【本项目独有】源码包里没有的东西

| 文件/模块 | 本项目独有内容 | 性质 |
|---|---|---|
| `backend/app/core/storage.py` | `_RUN_ID_RE` / `_safe_run_dir()` / `_safe_file_name()` | 路径穿越防护（D-01） |
| `backend/app/agents/adapters.py` | `_resolve_delivery_status()`（读 report_view.json 真实交付状态）、`_expected_outline_units()`（从大纲推导章节数） | D-04 修复 |
| `chart_generator/data_formulation.py` | `_RATIO_FIELD_TOKENS` + `_ratio_field_for_metric()` | **D-08 修复**：图表比值改为对齐 A2 计算值 |
| `data_interpreter/engine.py` | `NON_COMPANY_ENTITY_NAMES` / `NON_COMPANY_ENTITY_PATTERNS`（提到模块级做单一事实源） | 非公司实体过滤 |
| `chapter_writer/agent.py` | `AEROSPACE_CANONICAL_SEGMENTS`（商业航天产业链归属字典） | 领域定制数据 |
| `chart_generator/models.py` | 生图 8 字段（`render_mode`/`image_uri`/`generation_prompt`/`chain_graph`…） | AI 生图契约 |
| `chart_generator/agent.py` | 产业链 AI 生图调用（`generate_industry_chain_image`） | AI 生图链路 |
| `data_interpreter/skills/*/_meta.json`（6 个） | `requires_signal: false` | 补齐元数据字段（D-06） |
| `eval/` | 评测层（L0 自检 + 判分器 + 用例库） | 本项目新增 |
| `backend/tests/l1/` | 判据测试 8 个文件 | 本项目新增 |
| `docs/` | 测试报告 / 训练记录 / 执行台账 / 任务书 | 本项目新增 |

---

## 3. 【两边都有但不同】40 个文件——逐项差异

> 完整逐行 diff 可按需生成；此处列出**每一处改动的实质内容**。

### 3.1 提示词类（措辞差异，需择优）

| 文件 | 本项目写法 | 源码包写法 | 建议 |
|---|---|---|---|
| `chapter_writer/agent.py` 第 7 条 | 点名具体案例："**22.14万亿 与 2.21万亿 的10倍冲突**" | 泛化："严禁出现数量级矛盾**或臆造数据**" | **合并**：保留具体案例 + 吸收"或臆造数据" |
| `chapter_writer/agent.py` 第 5 条 | 无"禁止捏造占位符"约束 | **有**（chart_ids 空时禁插 `[CHART-xx]`） | **采用源码包** |
| `chapter_writer/agent.py` 第 9 条 | 简短 | **详细**（纯 JSON / 禁 Markdown / 中文引号） | **采用源码包** |
| `chart_generator/agent.py` | 无章节分布约束 | **有**（7 章均衡、单章 2~4 张） | **采用源码包** |
| `data_fetcher/agent.py` | "默认保留全部七个领域" | "需求理解**与实体抽取**"（含 `must_include_entities`） | **采用源码包**（配套 models 新字段） |

### 3.2 参数/阈值类

| 项 | 本项目 | 源码包 | 说明 |
|---|---|---|---|
| `chapter_writer` LLM 超时 | 300s（缺省） | **max(240, 300)** | 源码包设了下限 |
| `chart_generator` LLM 超时 | 45~60s | **120~180s** | 源码包大幅放宽（长内容不易超时） |
| 单次 LLM 调用超时 | 15s | **45s** | 同上 |
| 证据分箱上限 | 40 条 | **80 条** | 源码包翻倍（给 LLM 更多证据） |

### 3.3 逻辑类

| 文件 | 差异实质 |
|---|---|
| `data_fetcher/models.py` | 本项目 `extra="forbid"`（严格）↔ 源码包 `extra="ignore"`（宽松）。**forbid 会在 LLM 多返回字段时报错**；源码包选宽松 |
| `chart_generator/render.py` | 源码包 `resolve_ticker(..., context_map)`（实体消歧）；本项目无 |
| `data_interpreter/engine.py` | 同上（context_map 机制） |
| `fusion.py` | 源码包有 macro 专用分支；本项目依赖通用分支 |
| `report_fusion/agent.py` | 源码包从 `comps_matrix` 推导市值口径；本项目用正则替换 `22.14/2.21 万亿` |
| `chart_generator/compiler.py` | 源码包按 series 单位；本项目用 table 级 unit |
| `report_fusion/agent.py`（文本清洗） | 本项目无 ↔ 源码包新增 `_reconcile_text()` / `_clean_chart_text()` |

### 3.4 前端类

| 文件 | 差异规模 | 说明 |
|---|---|---|
| `frontend/src/components/ChartGallery.vue` | **230 行** | 源码包最新 09-26 09:12（含散点/气泡渲染、单位展示） |
| `frontend/src/components/QualityPanel.vue` | 69 行 | 源码包新增质量维度展示 |
| `frontend/src/views/CreateRunView.vue` | 09-26 09:12 | 源码包较新（可能含 `must_include_entities` 输入） |
| `frontend/src/api/types.ts` | 09-26 00:41 | 契约类型跟进 |
| `frontend/src/components/__tests__/*.spec.ts` | 2 个 | ChartGallery / QualityPanel 的 spec 同步 |

### 3.5 测试类（既有测试的改动）

`chapter-writer/tests/test_agent.py`(95)、`chart-generator/tests/test_agent.py`(37)、`report-fusion/tests/test_agent.py`(6)、`data-fetcher/tests/test_agent.py`(48)、`backend/tests/test_stage_intervention.py`(136)、`backend/tests/test_full_chain_integration.py`、`scripts/run_live_eval.py`、`scripts/verify_pipeline_health.py`、`pytest.ini`、`.gitignore`。

---

## 4. 拉取建议（分三档）

### ✅ 第一档【已执行】2026-09-26 14:36（快照 `.snapshot-20260926-1436.tar.gz`）

| 已拉取 | 内容 | 状态 |
|---|---|---|
| `remotion/` | 26 个文件（React 19 + Remotion 视频工程） | ✅ 已复制，零冲突 |
| `项目深度技术调查报告.md` | 327 行 | ✅ 已复制 |
| `scripts/run_live_frontend.js` | 190 行 | ✅ 已复制 |
| 7 个新测试文件 | 401 行 | ✅ 已复制（见下表运行状态） |
| `.gitignore` | 补 remotion 依赖排除（node_modules/.remotion/dist/out） | ✅ 已补 |

**新测试的运行状态（关键：多数红是因为"对应功能还没合并"）**：

| 测试文件 | 结果 | 说明 |
|---|---|---|
| `data-analysis/tests/test_engine_dynamic_refactor.py` | ✅ **1 passed** | 该能力本项目已具备 |
| `chapter-writer/tests/test_dynamic_segment_and_scale_consistency.py` | ⚠️ 1 passed / 1 failed | 失败项依赖源码包的分段逻辑 |
| `chart-generator/tests/test_chart_binning_and_routing_balance.py` | ❌ 2 failed | 待「章节分布提示词 + 分箱 80」合并 |
| `data-fetcher/tests/test_dependency_healing.py` | ❌ 2 failed | 待「依赖自愈」合并 |
| `data-fetcher/tests/test_macro_fusion_refactor.py` | ❌ 1 failed | 待「macro 专用解析分支」合并 |
| `report-fusion/tests/test_dynamic_cross_chapter_consistency.py` | ❌ 1 failed | 待「动态市值口径」合并 |
| `backend/tests/test_adapters_entity_extraction.py` | 🚫 **已移出收集** | 依赖源码包独有函数 `_extract_target_companies`（ImportError）→ 移至 `backend/tests/.pending_adaptation/`，待实体抽取功能合并后启用 |

> **处理原则**：能收集、能运行的失败测试**保留在位**（它们是"待合并功能"的验收测试，红是可解释的）；
> 唯一导致 **collection error** 的文件移出收集（保证全量 `--collect-only` 零 error）。
> **全量收集复验**：`235/236 collected`（1 deselected = live 标记），**零 collection error** ✓

**全量测试运行结果（拉取后，9 分 44 秒）**：**222 passed / 7 failed / 6 skipped**

| 对比项 | 拉取前 | 拉取后 |
|---|---|---|
| 全量 | 205 passed / **15 failed** / 6 skipped | **222 passed / 7 failed** / 6 skipped |
| 失败构成 | 缺陷台账 D-03…D-11 的关联用例 | **7 个全部来自新拉的「待合并功能」测试** |

**关键验证（本项目原有判据测试的现状）**：

| 判据测试文件 | 关联缺陷 | 现状 |
|---|---|---|
| `test_c1_formula_baselines.py` | D-03（comps 覆盖度 / CR3/CR5） | ✅ **已全绿** |
| `test_handoff_and_data_quality.py` | D-05（单位 / 时间元数据） | ✅ 已全绿 |
| `test_methodology_skills.py` | D-06（技能元数据） | ✅ 已全绿 |
| `test_g5_suppression.py` | D-07（抑制留痕） | ✅ 已全绿 |
| `test_a_series_closure.py` | D-08 / D-09 / D-10（图表数值 / 点级证据 / 数值段落） | ✅ 已全绿 |
| `test_l5_delivery_acceptance.py` | D-04（交付状态硬编码） | ✅ 已全绿 |
| `test_k_concurrency.py` | D-11（越界证据静默过滤） | ✅ 已全绿 |

> **结论**：拉取第一档后，**原有 15 个失败全部转为 0**（说明本项目此前的修复全部生效、且经判据测试独立验证），
> 现存 7 个失败**全部是刚拉进来的"待合并功能"验收测试** —— 即**本次拉取没有破坏任何既有能力**。
> 这 7 个红会随第二/三档功能合并而逐步转绿，可作为合并进度指标。

### 🔄 第二档【执行中】2026-09-26 14:52（快照 `.snapshot-20260926-1452.tar.gz`）

**已完成 11 个文件**（用户裁决"全部跟"）：

| 处理方式 | 文件 | 内容 |
|---|---|---|
| ✅ 直接覆盖（源码包为超集） | `chapter-writer/chapter_writer/llm.py` | JSON 自愈（剥 Markdown + 修未转义引号） |
| ✅ 直接覆盖 | `chapter-writer/chapter_writer/config.py` | `llm_timeout_seconds = max(240.0, ...)` |
| ✅ 直接覆盖 | `data-fetcher/data_fetcher/skillhub.py` | 依赖自愈（剥离失败依赖继续调度） |
| ✅ 直接覆盖 | `data-analysis/data_interpreter/agent.py` | `industry_chain_segments` 解析回填 |
| ✅ 直接覆盖 | `chart-generator/chart_generator/compiler.py` | 按 series 取单位 |
| ✅ 直接覆盖 | `data-fetcher/data_fetcher/models.py` | `must_include_entities` + `extra="ignore"` |
| ✅ 直接覆盖 | `data-fetcher/data_fetcher/agent.py` | INTENT 提示词重写（实体抽取） |
| ✅ 直接覆盖 | `report-fusion/report_fusion/agent.py` | 从 `comps_matrix` 推导统一市值口径 |
| 🔀 **合并** | `data-fetcher/data_fetcher/fusion.py` | 采用源码包 **macro 专用解析分支** + **保留你的 D-05 修复**（as_of 兜底 + issue 留痕） |

**验证结果**：

| 指标 | 拉取前 | 第二档执行中 |
|---|---|---|
| 命据测试（`backend/tests/l1`） | 46 passed / **15 failed** / 2 skipped | **61 passed / 2 skipped**（**全绿**） |
| 新拉测试的红数 | 7 | **3**（1 + 2） |
| 新功能实测 | — | 依赖自愈 ✅ 2 passed、macro 解析 ✅ 1 passed、跨章一致性 ✅ 1 passed、引擎重构 ✅ 1 passed |

**剩余 4 个文件待合并（都含我方独有内容，不能直接覆盖）**：

| 文件 | 源码包独有 | 我方独有（必须保留） | 对应待转绿测试 |
|---|---|---|---|
| `data-analysis/data_interpreter/engine.py` | `_entity`/`resolve_ticker` 的 `context_map`（实体消歧） | `NON_COMPANY_ENTITY_NAMES` / `NON_COMPANY_ENTITY_PATTERNS`（模块级共享常量） | — |
| `chart-generator/chart_generator/render.py` | `resolve_ticker(..., context_map)` | 散点/气泡 `None` 值兜底 + bubble 半径计算（含详细注释） | — |
| `chapter-writer/chapter_writer/agent.py` | 提示词第 5/9 条强化（禁捏造占位符、纯 JSON 规范） | `AEROSPACE_CANONICAL_SEGMENTS`（商业航天产业链归属字典）+ 第 7 条点名案例 | `test_dynamic_segment_and_scale_consistency`（1 个） |
| `chart-generator/chart_generator/agent.py` | 章节均衡分布提示词 + 证据分箱 40→80 + 超时放宽 | **产业链 AI 生图链路**（`generate_industry_chain_image` 调用） | `test_chart_binning_and_routing_balance`（2 个） |

### ✅ 第三阶段：缺陷修复合并（2026-09-26 15:16–15:50，依据双方修复台账）

**依据文档**：① `系统近期缺陷排查与修复加固全景记录.md`（队友 panheng 台账，含 DEF-01~04）
② `docs/P0缺陷修复记录-D03-D08.md`（我方 P0 轮）③ `docs/缺陷修复全记录-D02至D11.md`（我方 D-02~D-11 + 重冻结）

**用户裁决**：DEF 修复采用队友版；`_extract_target_companies` 拉过来；四处双方都改过的文件**逐文件合并**。

| 项 | 采用 | 验证 |
|---|---|---|
| **DEF-01** 数据清洗全域名 | 队友版（`for list_key, val in ds_json.items()`） | ✅ 已进入 `state_machine.py` |
| **DEF-02** resume 前置三件套 | 队友版（interpretation + chart + chapter） | ✅ 已进入 |
| **DEF-03** 单章重写后重置 pending | 队友版（3 处） | ✅ 已进入 |
| **DEF-04** 默认 5 阶段门禁 | 队友版（`CreateRunView.vue`） | ✅ 已覆盖 |
| **`_extract_target_companies`** | 拉入 `adapters.py`（D-04 两函数完好） | ✅ 导入通过（该函数无调用点；实测漏识「绿的谐波」，印证队友"正则脆弱"之说） |
| **`context_map` 实体消歧** | 融入 `engine.py`（4 处）+ `render.py` | ✅ 仅作用于 `extract_industry_chain`，**不影响 D-03 修复** |
| **章节均衡分布 + 超时 + 分箱 80** | 融入 `chart_generator/agent.py` | ✅ 生图链路与 D-07 回填**完好**，chart 层 **45 passed** |
| **提示词第 5/9 条强化** | 融入 `chapter_writer/agent.py` | ✅ `drop_log`（D-11）7 处 + 航天字典 2 处**完好** |
| **`_infer_chart_chapter` + 多域时序保序** | 队友 294 行函数块搬入 | ✅ 时序分组键加 domain、macro/industry 各保 2 组、预算 20→35、产业链 10→12 |

**全量测试演进（最终数字）**：

| 阶段 | 结果 |
|---|---|
| 最初（拉取前） | 205 passed / **15 failed** / 6 skipped |
| 第一档（零冲突新增） | 222 passed / 7 failed |
| 第二档（11 文件覆盖/合并） | 判据层 `61 passed / 2 skipped`（全绿） |
| **第三阶段（四处逐文件合并 + DEF）** | **228 passed / 1 failed / 6 skipped** ✅ |

**唯一剩余红（设计取向差异，非缺陷）**：
`test_dynamic_segment_and_scale_consistency.py::test_dynamic_segment_resolution_without_aerospace_defaults`
—— 队友测试要求"**无航天硬编码字典时也能动态解析分段**"（去硬编码主张），
而我方保留了 `AEROSPACE_CANONICAL_SEGMENTS`（商业航天领域定制数据）。
**未擅自改动**（涉及我方独有功能），留待用户裁决：是保留领域字典，还是采纳队友的去硬编码方案。

### ✅ 第四阶段：前端 bug 修复（2026-09-26 15:47–16:11）

| 组件 | 修的 bug | 处理 |
|---|---|---|
| `ChartGallery.vue`（229 行差） | **交互图表整片空白**（散点/气泡第 0 列中文名被当 X 坐标 → NaN） | 采用队友版重构（维度映射 + symbolSize 数值化 + tooltip 容错）；我方那 1 行仅格式差异 |
| `QualityPanel.vue`（61 行差） | **评分永远 99 分** | 采用队友版四维度加权（章节 10 + 结构 10 + 证据 40 + 数据一致 40 + 阶梯扣分）；替换我方旧"简单平均"口径 |
| `InterpretationDigest.vue` | **凭证暴露内部编号 + 风险信息不可读** | ⚠️ **不是改 spec，而是补组件功能** —— 见下 |

**`InterpretationDigest` 的真相（重要）**：3 个失败用例**不是 spec 过时**，而是**组件未消费后端已下发的可读字段**：
- spec 传入 `data.evidence_digest['R-secret-1'].label = '宁德时代 · 营业收入 2769.17亿元'`，而组件模板直接渲染 `{{ eid }}` → 显示 `凭证溯源:R-secret-1`
- 这属本项目"**后端已下发、前端未消费**"模式的**第 5 例**（前四例：`dimension_label`、`skill_label`、`evidence_index`、`consistency`）

**修复（补功能，非放水）**：
1. props 新增 `sourceRecords`（阶段一来源记录）
2. 新增 `evidenceLabel()` **三级回退**：`evidence_digest.label` → 用 `sourceRecords` 拼「实体 · 指标 值」→ 最后才回退 ID
3. 新增 `metricLabel`（`gross_margin`→毛利率 等 8 项）与 `riskKindLabel`（`cross_sectional_outlier`→同业离群 等 5 项）映射
4. 风险卡改为展示 `kind中文 + 实体·指标 + 观测值 + 参考区间`，不再裸露原始 description

**结果**：`InterpretationDigest.spec` 3 passed → **前端全量 `18/18 文件、81/81 用例` 全绿** ✅

---

### ⛔ 第四档【不动】

`storage.py`（路径穿越防护）、`adapters.py`（交付状态解析）、`data_formulation.py`（图表-A2 对齐）、
`chart_generator/models.py`（生图 8 字段）、6 个 `_meta.json`、`eval/`、`docs/`、`backend/tests/l1/` —— **保留本项目版本**。

### 📌 三个待裁决点的执行口径

用户裁决"**全部跟**"，故：`extra="forbid"` → **已改为 `"ignore"`**；超时**已放宽**（章节 max(240)）；
证据分箱 **40→80 待 `chart_generator/agent.py` 合并时生效**。

---

## 5. 待你确认的三个点

1. **`extra="forbid"` → `extra="ignore"`**：源码包放宽了 A1 请求模型校验，本项目是严格模式。要跟源码包放宽吗？（放宽的好处：LLM 多返回字段不再报错；坏处：非法字段静默通过）
2. **超时全面放宽**（章节 240s 下限、图表 120~180s）：要不要跟进？（真实跑批会更稳，但单阶段最坏耗时变长）
3. **证据分箱 40 → 80**：要不要跟进？（给 LLM 翻倍证据，可能提升质量，也可能让提示词更长更慢）
