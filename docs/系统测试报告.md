# 行业研究五智能体系统 · 系统测试报告

> 结构依据：`docs/测试落地任务书.md` §8.2（九节结构）
> 执行记录：`docs/测试执行台账.md`（快照、配置改动、缺陷、冲突逐条留痕）

---

## 1. 报告首部声明

| 项 | 值 |
|---|---|
| **被测代码快照** | `.snapshot-20260925-2300.tar.gz`（1.2 MB） |
| **快照 SHA-256** | `5b6fbf11862fa8f188ef6c5eeaa5edee3de09bbf36e7a2a0559a5e7c38c93942` |
| **主黄金样本 run_id** | `run-20260923094843-353`（completed，五阶段全通过，12 份产物含 PDF） |
| 辅助样本 | `run-20260922213739-783`（无 PDF）／`run-20260922201207-576`（多 revision 1…4）／`run-20260922225329-084`（cancelled @ data_interpret） |
| **执行日期** | 2026-09-25（CST） |
| 执行环境 | macOS（darwin）、Python 3.12.13、pytest 9.1.1 |
| 测试轮次 | R1–R10（十轮，逐轮快照见台账 §一） |

> **显式声明**：本报告"实际结果"列对应快照版本 `5b6fbf11…`。**此后的任何代码变更均未重新验证**，
> 结论仅对该快照成立。本轮唯一的生产代码改动为缺陷 D-01 的修复（`backend/app/core/storage.py`，
> 经用户明确授权，登记于台账 §二·2）。

---

## 2. 测试范围

### 2.1 本轮已执行（全部离线，零配额）

| 层 | 内容 | 执行方式 |
|---|---|---|
| **L0** | 评测器自检 fail-closed（F0-01…F0-13） | `eval/tests/test_fail_closed.py` + `test_case_schema.py` + `test_live_transport.py` |
| **L1** | 判据层规则判定：C1 公式一致性、R/G/H/D/T 规则、A2–A4 系列、N28–N30、G5 抑制语义 | `backend/tests/l1/*.py`（6 个文件，63 用例） |
| **L2** | 跨智能体交接契约 H1–H7 | `test_rules_from_golden.py`（H1/H2）+ `test_handoff_and_data_quality.py`（H4–H7） |
| **L3** | 全链路离线回放（以黄金样本为输入，验产物结构与逐阶段期望） | 同上（所有判据均锚在主样本产物上） |
| **L4c（部分）** | K1/K2/K3/K4/K6 并发与隔离 | `test_k_concurrency.py`（哨兵夹具 + mock LLM） |
| **L5** | 交付验收（MD/HTML/PDF/manifest SHA-256） | `test_l5_delivery_acceptance.py` |
| **N 系列** | N23–N30 | `real_core/`（适配版）+ `test_methodology_skills.py` |
| **前端** | 18 个既有 spec 可运行性核验 | `frontend` vitest（剥离 `NODE_OPTIONS` 后运行） |

### 2.2 明确不做（逐项列未验证 + 恢复条件）

| 项 | 为什么不做 | **恢复条件** |
|---|---|---|
| **L4b 真实 LLM 全链路** | 用户裁决本轮不跑真实 LLM；一切验证走离线 | 用户授权 + 配额确认（可复用 `eval/transport.py` 的 record 模式落盘后 replay） |
| **K5 真实加速比 / K7 超时余量 / K8 429 与重试** | 本质是模型与供应商侧行为，mock 无法体现 | 真实模型轮 |
| **L2 语义评委（judge）30% 权重 / M1–M3 方法论语义匹配** | 需模型判定 | 本轮以**规则代理判据**替代（维度关键词/模板字段命中），报告中已显式标注"代理判据，非语义评委"；语义评委需真实模型轮 |
| **真实生图调用** | V8 本轮不测生图；单次最长 600s | 用户授权（链路代码已具备，`IMAGE_API_KEY` 置空即跳过） |
| **I7 规则回退成功率** | 本项目 A1 **无规则回退路径**，LLM 未配置直接 `failed` | 判据已按 §3.4 变更为"失败语义判据"（见 §4 对账表的 I7 行） |
| **用例层逐条执行（cases_v7.json 135 条）** | 需真实模型驱动用户语句 | 见 §5 说明：本轮采用**判据层离线回放**；用例层执行属 L4b |

### 2.3 能力缺失（三层，登记为缺陷/风险而非删测试项）

| 缺失层 | 影响面 | 登记位置 |
|---|---|---|
| 鉴权与限流审计层（源库 `security/` 534 行测试） | 多用户/公网部署存在越权与滥用风险 | §8 风险项 |
| 跨端 JSON Schema 契约层（源库 `contracts/schemas/` 8 份 + `test_render_contract` 456 行） | 前后端契约漂移风险；本轮以 Pydantic 模型校验替代 | §4 对账（A5-N1 行） |
| 独立评测台（源库 `eval/` 1010 文件） | 无历史评测数据可比对；本轮新建 `eval/` 评测层（L0 自检 + 判分器骨架） | §3 环境 |

---

## 3. 测试环境

| 项 | 值 |
|---|---|
| 操作系统 | macOS（darwin，arm64） |
| Python | 3.12.13（`/Users/Zhuanz1/PycharmProjects/同花顺/backend/.venv/bin/python`，复用同花顺项目 venv） |
| 关键依赖 | pytest 9.1.1、pydantic 2.13.4、fastapi 0.139.2、pytest-asyncio 1.4.0、pymupdf、python-pptx 1.0.2（本轮新增） |
| 运行方式 | `PYTHONPATH=<项目根>:<agents_core 五子目录> <venv>/bin/python -m pytest` |
| 端口 | 后端 8000 / 前端 5173（本轮测试均为进程内调用，不依赖服务端） |
| 环境变量（**仅列变量名，不记录取值**） | `LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL`、`IWENCAI_API_KEY`、`IMAGE_API_KEY`、`IMAGE_BASE_URL`、`IMAGE_MODEL`、`IMAGE_SIZE`、`IMAGE_TIMEOUT_SECONDS`、`DATA_DIR`、`HOST`、`PORT` |
| **离线自证** | 根 `conftest.py` 的 autouse fixture 清空 `LLM_API_KEY`/`OPENAI_API_KEY`；`pytest.ini` 默认 `-m "not live"`；所有新增测试标注 `provider_mode ∈ {mock, replay}`、合成夹具标 `input_source=synthetic` |
| 前端工具链 | Node 22.22.2（托管）、vitest；**必须 `env -u NODE_OPTIONS`**（否则文件系统 broker 拦截） |

---

## 4. 功能测试项清单（逐项对账）

> 状态口径：`pass` 通过｜`fail` 已登记缺陷｜`skip` 合法跳过（含原因）｜**`未验证` 本轮未覆盖（附恢复条件）**
> 判据来源编号保留 V8 原 ID；重锚项见 `docs/测试落地任务书.md` §3.4。

### 4.1 L0 评测器自检（13 项）

| V8 ID | 本项目测试文件 | 状态 | 关键实测 |
|---|---|---|---|
| F0-01 未注册检查项必须失败 | `eval/tests/test_fail_closed.py` | pass | `run_l1_checks(checks=["NOT_IMPLEMENTED"])` → passed=False |
| F0-02 COMPLETED+error 非空必须 fail | 同上 | pass | 假 COMPLETED + stage error → verdict=fail |
| F0-03 COMPLETED 无产物必须 fail | 同上 | pass | 空 artifacts → fail |
| F0-04 正向被拦不算成功 | 同上 | pass | WAITING_REVIEW 的正向用例 → fail |
| F0-05 负向须精确匹配阶段与错误码 | 同上 | pass | 错阶段 fail／对阶段 pass |
| F0-06 模式真实性（mock 不得冒充 live） | 同上 | pass | `validate_provider_identity` 拒绝 |
| F0-07 用例集与声明注册完备 | `eval/tests/test_case_schema.py` | pass | 101 条用例 schema 校验零错误 |
| F0-08 gate 字段被 runner 消费 | 同上 | pass | `build_gate_record` → BLOCK/PASS 正确 |
| F0-09 合成覆盖生效 | 同上 | pass | duplicate_query / over_30_tasks 生效 |
| F0-10 轨迹注入器可用 | 同上 | pass | 先错后对路由生成有序事件 |
| F0-11 Q 类检查项注册完备 | `test_fail_closed.py` | pass | `QUALITY_CHECK_IDS == {Q1…Q5}` ⊆ 注册表 |
| F0-12 缺 total_score 必须 fail-closed | 同上 | pass | 不静默作 0 通过 |
| F0-13 未实现 Q 项必须 fail | 同上 | pass | Q99 判 fail |

**L0 小计：13/13 pass**（`eval` 全量 16 passed，含 `test_live_transport.py` 3 项）

### 4.2 C / R / G / D / P / T 类（判据层）

| V8 ID | 本项目测试文件 | 状态 | 关键实测 |
|---|---|---|---|
| **C1** 公式误差 ≤0.01%（**一票否决**） | `backend/tests/l1/test_c1_formula_baselines.py` | **fail** | 10 组基准：**3 pass**（gross_margin=3.53／net_margin=87.72／roe=14.85，重算误差 **0%**）｜**3 fail**（CR3/CR5 与 comps 覆盖面漂移，归因 **D-03**）｜**5 skip**（同比组量纲待接入 2 项 + 样本无输出 3 项，禁止编数） |
| **R1** 7 章 21 节结构 | `test_rules_from_golden.py` | pass | 7×3=21；基准由 `DEFAULT_OUTLINE` 推导（未写死） |
| **R2** 无违规表述（**一票否决**） | 同上 | pass | 禁词 0 命中（豁免合规免责句） |
| **R3** 数据有溯源 | 同上 | pass | 7 章 evidence_ids 非空且 ⊆ A1 池 |
| **G1** 同证据集不重复出图 | 同上 | pass | 6 张图证据集互不相同 |
| **G2** 用户多图豁免 | — | **未验证** | 需「创建任务时显式指定多图表类型」的请求 fixture；恢复条件：构造 `chart_generate_options` 配置请求后补测 |
| **G3** 产业链图 ≤1 | `test_rules_from_golden.py` | pass | 计数 0（≤1） |
| **G4** 图表数值与计算一致 | `test_a_series_closure.py` | **fail** | 三实体数值均超差（3.24≠3.53 等）→ **D-08** |
| **G5** 无数据不绘图 | `test_g5_suppression.py` | pass（部分） | 已出图 6/6 带证据、抑制与出图互斥；**抑制与数据事实自洽**（时间点中位数<3） |
| **D1** 标的主体匹配 | `test_handoff_and_data_quality.py` | pass | 五个实体域 0 缺失（reports/news 为资料条目，判据已按域细化） |
| **D2** 数据与原始接口一致（**一票否决**） | — | **未验证** | 需 `raw_sha256` 快照比对；当前产物未落盘原始响应哈希。恢复条件：A1 补落 raw 指纹后补测 |
| **D3** 时间范围符合 | `test_handoff_and_data_quality.py` | pass / **fail（覆盖度）** | 无前视数据（0 条 period_end>retrieved_at）；news 域 17/148 缺时间 → **D-05** |
| **D4** 单位完整 | 同上 | **fail** | 顶层 `unit` 覆盖率 **0/807** ＜ 阈值 60% → **D-05** |
| **P1** 数据不足停审核 | — | **未验证** | 需 blocked/partial 触发样本；恢复条件：构造 hard 需求未覆盖的请求或使用真实失败 run |
| **P2** 不伪造不补数（**一票否决**） | 由 R3 + H1 + H2 联合覆盖 | pass（代理判据） | 全部引用可溯源至 A1 池；**未做**"逐值反查原始响应"（需 D2 前置） |
| **P3** 异常提示清晰 | — | **未验证** | 样本 `state.error` 均为 None（cancelled 无错误结构）；恢复条件：需 blocked/partial 样本 |
| **P4** 前视偏差合理 | 由 D3 时点判据覆盖 | pass（部分） | `period_end ≤ retrieved_at` 成立 |
| **T1** 应调尽调（**一票否决**） | `test_rules_from_golden.py` | pass（可判部分） | 7 个 skill_id 全部匹配已注册命名规范 |
| **T2** 无错调（**一票否决**） | 由 N29（域支撑）代理 | pass（代理判据） | 无误触发嫌疑；**完整 T2 需语义匹配判定**（真实模型轮） |
| **T3** 无重复无效调用 | `test_rules_from_golden.py` | pass | 台账 8 条 ≤ 上限 24；无重复 (skill, query) |
| **T4** 参数完整正确 | — | **未验证（部分）** | `ResearchRequest` 校验器边界测试需构造请求；恢复条件：补 `ResearchRequest` 边界用例组 |
| **T5** 工具能力复用 | — | **未验证** | 需别名归一表与兜底路径样本 |
| **T6** 失败降级正确（**一票否决**） | 由 H7 + K6 代理 | pass（代理判据） | cancelled 不伪装成功；单章失败仅本地降级 |
| **T7** 调用路径最优 | — | **未验证** | 需 sources 时序分析；台账仅 8 条，样本不足以判定绕路 |
| **T8** 新技能路由正确 | — | **未验证** | 需技能级条件触发用例（zhishu/futures/astock-selector） |
| **I1–I8** | — | **未验证（重锚后待实现）** | 判据已在任务书 §3.4 完成重锚，但实现需 `RequirementCoverage` 等字段的构造夹具；恢复条件：按重锚表逐项补测 |

### 4.3 H 类交接契约（7 项）

| V8 ID | 测试文件 | 状态 | 关键实测 |
|---|---|---|---|
| **H1** A2 ⊆ A1（关键不变量） | `test_rules_from_golden.py` | **pass** | 274 条 evidence_index 全部源自 dataset → **证据单写者不变量在本项目独立验证成立** |
| **H2** A3 ⊆ A1（关键不变量） | 同上 | pass | 图表 evidence_ids + point_evidence_ids 全部在池内 |
| **H3** A2→A4 数据完整性 | `test_a_series_closure.py`（A2-02）+ R3 | pass（部分） | insights/章节证据引用有效 |
| **H4** A3→A4 图表引用有效 | `test_handoff_and_data_quality.py` | pass | 章节引用 6 个 chart_id 与 chart_result **完全一致** |
| **H5** A2/3/4→A5 载荷齐全 | 同上 | pass | report_view：7 章/6 图/48 证据目录/风险附录含 conflict+outlier |
| **H6** 审核恢复 | 同上 | pass | 样本 revisions 1…4 连续无空洞、与 state.revision 一致 |
| **H7** 任一阶段失败不伪装成功 | 同上 | **pass（关键）** | cancelled 停于 data_interpret，**下游产物全部不存在** |

### 4.4 A 系列（A2/A3/A4 可离线项）

| 判据 | 测试文件 | 状态 | 关键实测 |
|---|---|---|---|
| A2-01 Schema 合规 | `test_a_series_closure.py` | pass | 通过 `InterpretationReport` 模型校验 |
| A2-02 claim 引用存在 | 同上 | pass | 6 条 insights 证据引用全部有效 |
| A2-08 不输出买卖建议 | 同上 | pass | 禁词 0 命中 |
| A2-N1 异常检测（结构+严重度） | 同上 | pass | 92 条 anomalies 结构完整 |
| A2-N2 交叉验证 status | 同上 | pass | 枚举**从生产模型动态读取**（confirmed/contradicted/insufficient） |
| A2-03/04/05/06/09、A2-N3/N4/N5 | — | **未验证** | 需专用夹具（修订证据、方法论触发、LLM 格式修复）；恢复条件见 §8 |
| A3-01/03 证据可追溯 | `test_a_series_closure.py` | fail（点级） | 6/6 图 `point_evidence_ids` 为空 → **D-09** |
| A3-02 数值与 A2 一致 | 同上 | **fail** | → **D-08** |
| A3-04/06/09/10、A3-N1…N5 | — | **未验证** | 需 P1 图表族与产业链图专项夹具 |
| A4-01 严格 7章21节 | `test_rules_from_golden.py` | pass | 同 R1 |
| A4-02 数值段落有证据 | `test_a_series_closure.py` | **fail** | 3/83（3.61%）无证据 → **D-10** |
| A4-04 缺数据不补数 | 同上 | pass | 无推测性表述 |
| A4-05/10、A4-N1…N7 | 部分由 K1/K2/K6 与 N28–N30 覆盖 | pass/未验证并存 | 并发与结构等价已验；可读性三件套、溯源细节未验 |
| A5-01/02/08 | `test_l5_delivery_acceptance.py` | pass | **manifest 5 条 SHA-256 逐条重算全部相符** |
| A5-09/10/11、A5-N1…N4 | — | **未验证** | 深度裁剪与样式冲突需专项 fixture；契约整包校验本项目无 contracts 层（以 Pydantic 校验替代，见 §8） |

### 4.5 N 系列

| ID | 测试文件 | 状态 | 关键实测 |
|---|---|---|---|
| N23–N25 路径穿越防护 | `backend/tests/agents/real_core/test_adapter.py` | pass | `../outside`／绝对路径 run_id **均被拒**；delete 不越界 |
| N26 五包动态注册 | `real_core/test_bootstrap.py` | pass | 注入路径由 `AGENTS_CORE_DIR` 推导、五包可加载、无桌面依赖 |
| N15–N17 阶段干预 | `backend/tests/test_stage_intervention.py`（既有） | pass | 3 用例通过（revision 递增、编辑白名单） |
| N27 状态机六动作/resume | `backend/tests/test_state_machine_resume.py`（既有） | pass | 4 用例通过 |
| **N28** 触发技能均已注册 | `test_methodology_skills.py` | pass | applied_skills 7 个全部合法 |
| **N29** 不误触发 | 同上 | pass（可判部分） | 无"域无数据却触发"；**requires_signal 缺失**致 6 项不可判 → **D-06** |
| **N30** 模板完整 | 同上 | **fail** | 14/20 完备，6 个缺 `requires_signal` → **D-06** |
| N1–N6 生图链路、N7 生图消费、N8–N10 SSE、N11–N14 设置热更新、N18–N20 双模板、N21–N22 补数回路 | — | **未验证** | 均需专项夹具（mock 生图服务、SSE 客户端、配置注入）；恢复条件见 §8 |
| G5 扩展：抑制记录可审计 | `test_g5_suppression.py` | **fail** | 4/4 抑制记录缺 evidence_ids → **D-07** |

### 4.6 K 系列

| ID | 测试文件 | 状态 | 关键实测 |
|---|---|---|---|
| **K1** 输出结构等价 | `test_k_concurrency.py` | pass | fallback 与 LLM 双路径：7 章 × 3 节、ID 逐一对应 |
| **K2** 跨章污染零命中（**一票否决**） | 同上 | pass | 无任何非本章哨兵；**扩展判据 fail**：越界证据被静默过滤 → **D-11** |
| **K3** 证据引用合法 | 同上 | pass | 产物 ⊆ 夹具池 |
| **K4** 并发持久化零错误（**一票否决**，重锚） | 同上 | pass | 16 run 并发写 state.json 无半写；同 run 24 artifact 并发无覆盖/截断 |
| **K6** 单章降级语义 | 同上 | pass | 仅 CH-04 降级；其余 6 章正常且未被取消 |
| K5/K7/K8 | — | **未验证** | 见 §2.2 |

---

## 5. 测试用例表

> **口径说明（重要，避免误读）**：本轮为**判据层离线回放**，不是"逐条执行 cases_v7.json 的用户语句"。
> 原因：用户语句需真实模型驱动（属 L4b，本轮不做，见 §2.2）。
> 因此下表按**判据组**组织，五列含义为：编号 ｜ 前置条件 ｜ 判据（等价于预期结果） ｜ 实际结果 ｜ 证据指针。
> 实际结果按任务书 §6.4 格式填写（状态 ｜ 关键实测值 vs 判据 ｜ 证据指针 ｜ run_id）。

### group = 评测器自检（L0）

| 编号 | 前置条件 | 判据 | 实际结果 |
|---|---|---|---|
| F0-01…F0-13 | `eval/` 层已建；断网 fixture 生效 | 见 §4.1 | **pass** ｜ 13/13 通过、零外呼 ｜ `eval/tests/test_fail_closed.py`、`test_case_schema.py` ｜ 不依赖 run |

### group = 计算一致性（L1 · C1）

| 编号 | 前置条件 | 判据 | 实际结果 |
|---|---|---|---|
| C1-gross_margin | 黄金样本 `dataset.json` + 当前 `engine.py` | 重算 vs 产物误差 ≤0.01% | **pass** ｜ 3.53 vs 3.53，偏差 **0.00%** ｜ `interpretation_report.json#/financial_ratios/5` ｜ `run-20260923094843-353` |
| C1-net_margin | 同上 | 同上 | **pass** ｜ 87.72 vs 87.72，偏差 0.00% ｜ 同上 ｜ 同上 |
| C1-dupont_roe | 同上 | 同上 | **pass** ｜ 14.85 vs 14.85，偏差 0.00% ｜ 同上 ｜ 同上 |
| C1-cr3 / cr5 | 同上 | 同上 | **fail** ｜ 重算 100.0 vs 基准 37.602／55.5614，偏差 100% ｜ `chart_result.json`×`comps_matrix` ｜ 同上（归因 **D-03**） |
| C1-inventory/receivables/capacity | 同上 | 同上 | **skip** ｜ `baselines.expected=null`（样本无对应生产输出，禁止编数）｜ `eval/cases/baselines.json` ｜ — |

### group = 结构与表述规则（R/G/H/D/T）

| 编号 | 前置条件 | 判据 | 实际结果 |
|---|---|---|---|
| R1/R2/R3 | 主样本产物 | §4.2 | **pass ｜ pass ｜ pass** ｜ 7×3=21；禁词 0；7 章证据非空 ｜ `chapter_result.json` ｜ 主样本 |
| G1/G3/G4 | 同上 | §4.2 | **pass ｜ pass ｜ fail** ｜ G4 三实体超差 ｜ `chart_result.json` ｜ 主样本 |
| G5 | 同上 | 抑制留痕/互斥/自洽 | **fail（留痕）｜ pass（互斥）｜ pass（自洽）** ｜ 4/4 抑制缺 evidence_ids；时间点中位数<3 ｜ `chart_result.json#/suppressed_charts` ｜ 主样本 |
| D1/D3/D4 | `dataset.json` 807 条 | §4.2 | **pass ｜ pass+fail ｜ fail** ｜ unit 0/807；news 17/148 缺时间 ｜ `dataset.json` ｜ 主样本 |
| T1/T3 | `dataset.json.sources` | §4.2 | **pass ｜ pass** ｜ 7 个 skill 合规；8 条 ≤24 无重复 ｜ 同上 ｜ 主样本 |

### group = 交接契约（H1–H7）

| 编号 | 前置条件 | 判据 | 实际结果 |
|---|---|---|---|
| H1/H2 | 主样本 | A2⊆A1、A3⊆A1 | **pass ｜ pass** ｜ 274 条 A2 证据全部源自 A1；A3 含点级全部在池内 ｜ `interpretation_report.json#/evidence_index` ｜ 主样本 |
| H4/H5 | 主样本 | 图表引用有效／载荷齐全 | **pass ｜ pass** ｜ 6/6 chart_id 匹配；7 章/6 图/48 证据/风险附录 ｜ `report_view.json` ｜ 主样本 |
| H6 | `run-20260922201207-576` | revision 链连续、与 state 一致 | **pass** ｜ revisions 1…4 无空洞 ｜ `revisions/*.json` ｜ 该样本 |
| H7 | `run-20260922225329-084` | 中断不伪装成功 | **pass** ｜ 停于 data_interpret，下游 5 类产物均不存在 ｜ `state.json` ｜ 该样本 |

### group = 并发与隔离（K）

| 编号 | 前置条件 | 判据 | 实际结果 |
|---|---|---|---|
| K1/K2/K3 | 哨兵夹具（21 条合成证据）+ mock LLM | 结构等价／零污染／引用合法 | **pass ｜ pass ｜ pass** ｜ 7 章 ID 对齐；无跨章哨兵；⊆ 池 ｜ `test_k_concurrency.py` ｜ synthetic |
| K4 | `StorageManager(tmp_path)` | 并发写无损坏/无覆盖 | **pass** ｜ 16 run × state.json 全可解析；24 artifact 并发无覆盖 ｜ 同上 ｜ synthetic |
| K6 | 令 CH-04 抛错 | 仅本地降级 | **pass** ｜ fallback=[CH-04]，其余 6 章正常 ｜ 同上 ｜ synthetic |

### group = 交付验收（L5）

| 编号 | 前置条件 | 判据 | 实际结果 |
|---|---|---|---|
| A5-01 | 主样本 | 三格式存在非空 | **pass** ｜ md/html/pdf 均在且非空 ｜ `manifest.json` ｜ 主样本 |
| A5-02 | 主样本 | SHA-256 逐条相符 | **pass** ｜ **5/5 条重算全部相符**（改一字节即失败）｜ `manifest.json#/artifacts` ｜ 主样本 |
| A5-08 | 副样本（无 PDF） | MD/HTML 保留 | **pass** ｜ report.md/html 均存在非空、无 pdf ｜ `run-20260922213739-783/artifacts/` ｜ 副样本 |
| D-04 护栏 | `adapters.py` 源码 | delivery_status/大纲基准不得硬编码 | **fail ｜ fail** ｜ 命中硬编码 `"ready"` 与 6 处 7/21 ｜ `adapters.py:964/965/969/970/1196/1204/1205` ｜ — |

---

## 6. 执行结论

### 6.1 四态统计（后端判据层 + 评测层）

**全量总账**（`pytest`（`testpaths = agents_core backend eval`），单轮实测 **7 分 5 秒**）：
**`205 passed / 15 failed / 6 skipped / 0 blocked`**（共 226 项；另有 1 项 deselected = `live` 标记被默认排除）

| 集合 | pass | fail | skipped | blocked | 合计 |
|---|---|---|---|---|---|
| **全量后端（上表口径）** | **205** | **15** | **6** | **0** | **226** |
| ├ `eval`（L0 自检 + transport） | 16 | 0 | 0 | 0 | 16 |
| ├ `backend/tests/l1`（判据层） | 46 | 15 | 2 | 0 | 63 |
| ├ `backend/tests/agents/real_core`（N23–N26） | 6 | 0 | 0 | 0 | 6 |
| ├ 既有后端测试（`backend/tests` 其余 7 文件） | 13 | 0 | 4 | 0 | 17 |
| └ 既有算法层测试（`agents_core/*/tests` 19 文件） | 124 | 0 | 0 | 0 | 124 |
| 前端（vitest，18 spec） | 72 | 7 | 0 | 0 | 79 |

> **`blocked` = 0** 是门禁硬指标（任务书 §7.5）：**must_pass 用例的 blocked 数必须 = 0** → 本项**满足**。
> 15 个后端 fail **全部**为已登记缺陷（D-03×3、D-04×2、D-05×2、D-06×2、D-07×1、D-08×3、D-09×1、D-10×1、D-11×1），
> **无一条归因于"测试自身错误"或"未登记问题"**；6 个 skipped 全部带显式理由（基准缺失/量纲待接入）。

### 6.2 分层通过率

| 层 | 通过率 | 说明 |
|---|---|---|
| 后端全量 | **90.7%**（205/226） | 15 fail 全部为已登记缺陷 |
| L0 评测器自检 | **100%**（16/16） | 假阳性防护已建立（后续所有结论的有效性前提） |
| 既有算法层（`agents_core/*/tests`） | **100%**（124/124） | 重构后算法层无回归 |
| L5 交付验收 | **80%**（4/5 组） | 唯一 fail 为 D-04 回归护栏 |
| H 类交接契约 | **100%**（7/7） | 含两条关键不变量与"中断不伪装成功" |
| K 类并发与隔离 | **83%**（5/6） | 唯一 fail 为 D-11 扩展判据 |
| L1 判据层 | **73%**（46/63） | fail 全部为已登记缺陷 |
| 前端既有 spec | **91%**（72/79） | 7 个 fail 定性为用例预期偏差（非回归） |

### 6.3 pass@k / pass\*k

本轮 **k = 1**（任务书 §6.2：结果一次写死、不重跑、产出即冻结）：
`pass@1 = pass*1 = 见上表`（无重复采样，故两者相同）。
后续如需 k>1，应基于 `eval/transport.py` 的 record/replay 对同一快照重放（strict miss=fail）。

### 6.4 一票否决项命中情况（门禁）

| 一票否决项 | 结果 | 归因 |
|---|---|---|
| **C1** 公式误差 ≤0.01% | **❌ 命中（fail）** | D-03（comps 覆盖面漂移 → CR3/CR5 不可复现） |
| **G4** 图表数值与计算一致 | **❌ 命中（fail）** | D-08（数值不一致 + 缺失填 0 + 期间错位） |
| D2 数据与原始接口一致 | 未验证（无法判定） | 需 raw_sha256 快照 |
| P2 不伪造不补数 | ✅ 未命中 | 由 R3/H1/H2 代理判据通过 |
| R2 无违规表述 | ✅ 未命中 | 禁词 0 命中 |
| T1 应调尽调 | ✅ 未命中（可判部分） | 技能 ID 全部合规 |
| T2 无错调 | ✅ 未命中（代理判据） | 无误触发嫌疑 |
| T6 失败降级正确 | ✅ 未命中（代理判据） | H7 + K6 通过 |
| K1 输出结构等价 | ✅ 未命中 | 双路径结构等价 |
| K4 并发持久化 | ✅ 未命中 | 无损坏/无覆盖 |

> **门禁结论**：**命中 2 项一票否决（C1、G4），另有 1 项无法判定（D2）** →
> 按任务书 §7.5，**整体结论判 BLOCK**（详见 §9）。

---

## 7. 缺陷记录

**共 11 条：1 条已修复（D-01）、10 条待授权修复。** 每条 8 字段完整记录见 `docs/测试执行台账.md` §四；此处给汇总与归因。

### 7.1 缺陷汇总表

| ID | 被测对象 | 等级 | 现象摘要 | 根因 | 状态 |
|---|---|---|---|---|---|
| **D-01** | `backend/app/core/storage.py` | 阻断性（安全） | `run_id` 路径穿越：可越界建目录／**rmtree 外部目录** | E | **✅ 已修复并验证**（24 passed，回归 13 passed） |
| **D-02** | `data_interpreter` 比率字段 | 普通 | 量纲不统一（百分数 vs 小数率）；净利率 87.72% 可疑 | E | 待授权 |
| **D-03** | `engine.build_peer_comps_matrix` | 普通（影响否决项） | 重算 3 条 entries vs 产物 23 条；CR3/CR5 不可复现 | E | 待授权 |
| **D-04** | `adapters.py` manifest 组装 | 普通 | **合并引入退化**：`delivery_status` 硬编码、大纲基准写死 7/21（6 处） | E | 待授权 |
| **D-05** | A1 结构化落盘 | 普通（影响否决项） | 顶层 `unit` **0/807**；news 17/148 缺时间 | E | 待授权 |
| **D-06** | A2 技能包元数据 | 普通 | 6/20 缺 `requires_signal` → M2 判定盲区 | E | 待授权 |
| **D-07** | `suppressed_charts` | 普通 | 4/4 缺 `evidence_ids`，抑制决策不可审计 | E | 待授权 |
| **D-08** | A3 图表数值装配 | **普通（重）** | 图上值 ≠ A2 计算值；缺失填 0；期间错位 | E | 待授权 |
| **D-09** | `point_evidence_ids` | 普通 | 6/6 恒空，点级溯源名存实亡 | E | 待授权 |
| **D-10** | 章节数值段落 | 普通（轻微） | 3/83（3.61%）无证据（含区间值无出处） | E | 待授权 |
| **D-11** | `_audit_chapter` | 普通 | 越界证据被静默过滤、不留痕 | E | 待授权 |

### 7.2 A–E 根因归因计数

| 码 | 类别 | 计数 | 说明 |
|---|---|---|---|
| **A** | 提示词缺陷 | **0** | 本轮不跑真实模型，提示词类缺陷不可判定 |
| **B** | 模型底座缺陷 | **0（不可确认）** | 同上；**B 类只能登记不能确认**，已在 §8 注明 |
| **C** | 工具 Schema/MCP 缺陷 | **0** | 本轮无 MCP/Schema 层可测（本项目无 contracts 层） |
| **D** | 记忆/上下文缺陷 | **0** | 并发/隔离类由 K 系列覆盖，未发现上下文污染（K2 pass） |
| **E** | 业务逻辑缺陷 | **11** | D-01…D-11 全部归 E |

### 7.3 四类分流归属（任务书 §7.4）

| 类 | 例 | 本轮实际处理 |
|---|---|---|
| ① 评测器/接线缺陷 | 无 | 本轮未发现；`eval/` 自检全绿 |
| ② 用例预期偏差 | D1 判据按域细化；D3 覆盖度改阈值；A2-N2 枚举改为动态读取；A5-08 改用同名副本；前端 7 例定性 | **已直接修正**（登记于台账） |
| ③ 确定性生产 bug | D-01…D-11 | **只登记不修**（D-01 经用户授权修复） |
| ④ 模型行为 bug | — | 本轮不涉及（不跑真实模型） |

---

## 8. 风险与未验证项

### 8.1 能力缺失（三层，登记为风险）

| 缺失 | 影响面 | 建议 |
|---|---|---|
| 鉴权 / 限流 / 审计层 | 多用户或公网部署下存在越权与滥用风险；D-01 修复前连路径穿越都无防护 | 参赛演示可接受（本地单机）；若上线需补鉴权层 |
| 跨端 JSON Schema 契约层 | 前后端契约漂移风险（本轮已发现 D-04 这类"字段名与实现不符"的漂移） | 建议引入契约快照测试（如 Pydantic 导出 JSON Schema 后比对） |
| 独立评测台 | 无历史评测数据可比对、无跨轮 diff | 本轮已新建 `eval/`（L0 + 判分器骨架），后续可增量扩 |

### 8.2 未验证判据清单（覆盖率缺口，逐项附恢复条件）

| 判据组 | 未验证项 | 恢复条件 |
|---|---|---|
| G2 用户多图豁免 | 1 项 | 构造 `chart_generate_options` 配置请求 |
| D2 数据与原始接口一致（**否决项**） | 1 项 | A1 补落 `raw_sha256` 后比对 |
| P1/P3 停审核与异常提示 | 2 项 | 需 blocked/partial 触发样本（现网络/数据条件下需构造失败场景） |
| T4/T5/T7/T8 | 4 项 | 请求校验边界夹具 + 别名表 + sources 时序分析 |
| I1–I8 | 8 项 | 按 §3.4 重锚表逐项构造夹具（`RequirementCoverage` 等） |
| A2 余项（03/04/05/06/09、N3/N4/N5） | 8 项 | 修订证据、方法论触发、LLM 格式修复等专项夹具 |
| A3 余项（04/06/09/10、N1…N5） | 9 项 | P1 图表族与产业链图专项夹具 |
| A4 余项（05/10、N2…N7） | 9 项 | 章节 hash 对比夹具、可读性三件套 |
| A5 余项（09/10/11、N1…N4） | 7 项 | 深度裁剪/样式冲突/契约整包校验夹具 |
| N1–N14、N18–N22 | 19 项 | mock 生图服务、SSE 客户端、配置注入、补数回路夹具 |
| K5/K7/K8 | 3 项 | 真实模型轮 |
| 前端 2 个新组件 | `DirectEditWorkbench`、设置面板 | 待补 spec |

**统计**：判定项总数 89 项（V8 82 + H1–H7）中，**本轮有测试背书 46 项、未验证 43 项**；
另有 N 系列约 30 项中已覆盖 8 项。所有未验证项均在本表显式登记，**无静默省略**。

### 8.3 B 类根因不可确认说明

本轮为离线（mock / 回放），**A 类（提示词）与 B 类（模型底座）缺陷无法确认**。
D-08（图表数值不一致）与 D-10（区间值无出处）在真实模型轮中可能同时涉及 A/B 类因素，
当前仅能确认其**确定性部分**（E 类）。

### 8.4 待确认项（需产品/团队裁决）

1. **前端 `ChartGallery` 注入 `tooltip.valueFormatter`**：是有意增强（则更新 spec 契约描述）还是契约破坏（则需回归）？
2. **D-04 的三处合并退化是否回补**：B 版实现更完善，是否恢复 `_resolve_delivery_status` 与 `_expected_outline_units`？
3. **D-03 与 D-05 的修复优先级**：两者互为因果（时间/单位元数据缺失 → 图表能力受限），建议同批修复。

---

## 9. 结论

### 9.1 总体门禁结论

**BLOCK**（不可作为"通过"交付）。依据：命中 **2 项一票否决**（C1 公式一致性、G4 图表数值一致），
另有 1 项否决项（D2）无法判定。按任务书 §7.5，一票否决项命中即 BLOCK。

**但须同时记录三项正向事实**（避免结论被误读为"系统不可用"）：
1. 五阶段全链路在黄金样本上**成功完成**（`run-20260923094843-353`，含 PDF 与 manifest）；
2. 关键护栏与不变量**全部成立**——H1/H2 证据单写者不变量、H7 中断不伪装成功、K1/K2/K4 结构等价与并发安全、
   R2/P2 合规与可溯源、L5 交付 SHA-256 一致；
3. 全部 11 条缺陷**根因明确且修复路径清晰**，其中阻断级安全缺陷 D-01 已修复并验证。

### 9.2 按智能体逐个下结论

| 智能体 | 结论 | 依据 |
|---|---|---|
| **A1 数据获取** | **条件性不满足上线标准** | 结构性良好（T1/T3 通过、N26 包注册通过），但 **D-05**（unit 0/807、时间缺失）与 **D-06**（技能元数据缺字段）需先修；A1 是 D-03/D-05/D-08 的上游根因 |
| **A2 数据解读** | **条件性满足（含 1 条重缺陷）** | Schema/证据/异常/交叉验证结构全部合规（A2-01/02/08/N1/N2 pass、D1 pass），但 **D-03**（comps 覆盖面）导致否决项 C1 fail；**D-02**（量纲）需修 |
| **A3 图表生成** | **不满足上线标准** | **D-08**（数值失真 + 缺失填 0 + 期间错位，命中 G4）与 **D-09**（点级证据恒空）、**D-07**（抑制不可审计）三条叠加；但抑制逻辑本身与数据事实自洽（G5 pass） |
| **A4 章节写作** | **条件性满足** | R1/R2/R3、K1/K2/K3/K6、H3/H4 全部通过；瑕疵为 **D-10**（3.61% 数值段缺证据，轻微）与 **D-11**（越界证据静默过滤） |
| **A5 报告融合** | **条件性满足** | **A5-01/02/08 全部通过**（manifest SHA-256 5/5 相符、降级不丢主产物）、H5 pass；接口层受 **D-04** 影响（交付状态与大纲基准失真） |
| **编排/存储层** | **满足（修复后）** | D-01 已修复并验证；N27/N15–N17 通过；K4 并发安全 |
| **前端** | **条件性满足** | 16/18 文件可运行；7 个 fail 为用例预期偏差（非回归）；2 个新组件缺 spec |

### 9.3 建议的修复优先级

1. **P0（否决项直接相关）**：D-03（comps 覆盖度）、D-08（图表数值装配）—— 二者分别对应 C1 与 G4；
2. **P1（上游根因，修一利三）**：D-05（unit/时间元数据）—— 同时缓解 D-03 与 D-08，并解除"图表类型恒为 bar"的根因；
3. **P2（契约与可审计性）**：D-04（合并退化回补）、D-07（抑制留痕）、D-11（越界留痕）、D-09（点级证据）；
4. **P3（轻微/工程债）**：D-02、D-06、D-10；
5. 修复后须**重新冻结快照并重跑本报告全部判据**（尤其 C1 与 G4）。

---

## 附：本报告的对应证据索引

| 内容 | 位置 |
|---|---|
| 执行台账（快照/配置改动/缺陷 8 字段/冲突/进度） | `docs/测试执行台账.md` |
| L0 自检测试 | `eval/tests/test_fail_closed.py`、`test_case_schema.py`、`test_live_transport.py` |
| L1 判据层测试 | `backend/tests/l1/{test_c1_formula_baselines,test_rules_from_golden,test_l5_delivery_acceptance,test_handoff_and_data_quality,test_methodology_skills,test_g5_suppression,test_a_series_closure,test_k_concurrency}.py` |
| N23–N26 适配版测试 | `backend/tests/agents/real_core/{test_adapter,test_bootstrap}.py` |
| 基准回填工具与结果 | `eval/tools/backfill_baselines.py`、`eval/cases/baselines.json` |
| 评测层 | `eval/`（harness、provider_mode、scorers、cases） |
| 快照 | `.snapshot-20260925-2120/2135/2300.tar.gz` |
