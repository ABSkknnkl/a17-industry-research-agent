# Week 0 — 框架基线

## 目标

建立可启动、可测试、可并行开发的工程基线。本阶段不宣称完成原方案 Week 1 技术验证。

## 验收项

- [x] 前后端依赖可从锁文件/需求文件安装
- [x] 前端启动、类型检查、lint、单测和生产构建通过
- [x] 后端健康检查、契约测试、运行依赖导入和静态检查通过
- [x] 所有 Python 能力目录可正常导入
- [x] 根 `contracts/` 是跨端唯一契约源
- [x] 五名成员的目录、输入、输出和首个任务明确
- [x] 全仓 `scripts/verify.sh` 通过

验收日期：2026-07-22。前端首页及 API 代理、后端 `/health`、`/api/v1/ping`、`/docs` 均已通过真实端口验证。
