---
name: chart-readability
description: 校验图表标题、单位、坐标轴、图例、脚注、颜色和证据关联，保证静态导出可读。
---

# 图表可读性

- 标题表达图表研究目的，图例、坐标轴和单位与数据口径一致。
- 类别标签过长时压缩显示但不改写含义；系列过多时拆图或降级。
- 图例不得遮挡绘图区；颜色含义在同次运行中保持一致。
- 负值图表显示零轴；空值保持断点，不以零填补。
- 每张图必须保留至少一个有效证据 ID；归一化、缺失单位和口径差异写入脚注。
- SVG 与 HTML 使用同一结构化 Option，不在渲染层改变数值。

