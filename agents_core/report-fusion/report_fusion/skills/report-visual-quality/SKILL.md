---
name: report-visual-quality
description: 确保 Markdown、HTML 与 PDF 使用同一视图模型并具备可读层级、分页和图表表现。
---

# 报告视觉质量

- 标题、章节、正文、图表、脚注和证据目录形成稳定层级。
- HTML 与 PDF 从同一 ReportViewModel 生成，图表以内联 SVG 固化。
- 图表不得被裁切或跨页拆断；长表和长证据值允许换行、截断并保留原记录 ID。
- 页面避免孤立标题、内容重叠、不可读小字和颜色含义漂移。
- 单一格式失败时保留其他成功格式并准确报告降级原因。

