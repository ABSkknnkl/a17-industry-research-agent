# 研究边界词表（2026-09-01 最终方案 §5，业务裁决 2 落地）
#
# 命中边界词的诉求永不硬路由：进 unresolved_metrics 披露通道，走澄清门/
# 用户裁决门。这是「真数据缺口」的显式登记处——绝不把"查不到"的指标
# 混进可路由词表，绝不编造。
#
# 维护规则：
# - 新缺口确认后直接追加（热生效，不发版）；
# - 数据源验证可用后：从本表移除，并在 metric_registry 正式注册。

terms:
  # 真缺口（61 条压测确认无数据源）
  - 产销率
  - 自给率
  - 良率
  - 外采比例
  - 单瓦成本
  - 单GWh成本
  - 进口依赖度
  # 口径歧义（行业口径无公司技能可接，BUG-1 配套）
  - 行业出口占比
  - 出口占比
  # 业务裁决 2：产能爬坡语义拆分——爬坡周期/爬坡进度为研究边界词，
  # 判定派生诉求，不走指标取数；释放/落地/扩张继续留在派生否定表。
  - 爬坡周期
  - 爬坡进度
