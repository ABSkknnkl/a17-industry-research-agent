# 文字型报告方向

适用于图表较少、解释链较长、证据和表格较多的报告。

- 以 `prose_flow` 为基础，让正文拥有舒适行长和明显段落层级。
- 引用密集时使用 `evidence_split`，把证据提示放在旁侧，但正文列不得变窄。
- 跨对象、跨口径或跨情景对照用 `table_story` 或 `comparison_board`，不要用装饰卡片替代表格。
- 少量图表紧邻其证明的段落；一张主图用 `chart_focus`，产业链或关系图用 `diagram_story`。
- 长章节通过导语、关键句、表格和小标题建立节奏，不通过删字或大块空白制造节奏。
